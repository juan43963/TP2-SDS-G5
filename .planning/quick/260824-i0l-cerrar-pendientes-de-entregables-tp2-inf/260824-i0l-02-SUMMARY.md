---
plan: 260824-i0l-02
status: complete
---

# Summary — Plan 02: Carátula, placeholders de video y overflow (presentación)

**Tasks:** 3/3 complete

**Commits (merged into main via `merge: quick task 260824-i0l plan 02`):**
- `fbbfa1e`: feat(260824-i0l-02): portada Beamer real con autores, legajos y afiliacion ITBA
- `509f2d2`: fix(260824-i0l-02): reemplazar placeholder de link de video por TODO no renderizado
- `3b39d12`: fix(260824-i0l-02): eliminar overflow vertical real en 4 frames del Beamer

## Key files

- `TP2/presentacion/presentacion.tex`
- `TP2/presentacion/presentacion.pdf` (recompilado)

## Verification (final, 2-pass pdflatex, `-halt-on-error`)

- `grep -c "PEGAR LINK" presentacion.tex` → 0
- `grep -c "TODO(usuario)" presentacion.tex` → 2
- `grep -c "\inst{1}" presentacion.tex` → 2, `grep -c "\[LEGAJO\]" presentacion.tex` → 2
- `grep -c "too high) detected" presentacion.log` → 0

## Deviations (as reported by the executor)

1. Las adiciones de portada de la Task 1 (subtítulo + bloque de 3 autores) inicialmente desbordaban el frame Warsaw de título (~46pt). Arreglado en el mismo commit vía overrides `\setbeamerfont`, sin quitar contenido.
2. Este worktree no tenía los artefactos generados gitignored (`TP2/data/plots/*.png`, `TP1/cim`, `TP2/tp2`). Se copiaron los PNG reales desde el checkout principal; `benchmark_tp1_vs_tp2.png` no existe en ningún lado del repo y los binarios copiados no son ejecutables en Windows (sin toolchain C++ en este entorno), así que se generó un placeholder sintético local (gitignored, nunca comprometido) solo para verificar que el fix de ancho elimina el overflow real. Registrado como pendiente en `.planning/WINDOWS.md` — el usuario debe regenerar el `benchmark_tp1_vs_tp2.png` real (`py TP2/python/benchmark.py`) en un entorno con los binarios compilados antes de la entrega real.

No se comprometieron `STATE.md`/`ROADMAP.md`/metadata de plan, según instrucción del orquestador.
