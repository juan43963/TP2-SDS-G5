---
phase: 260824-i0l-cerrar-pendientes-de-entregables-tp2-inf
plan: 03
type: execute
wave: 2
depends_on: ["260824-i0l-01"]
files_modified:
  - TP2/informe/informe.tex
autonomous: true
requirements: [AUDIT-03, AUDIT-05]

estimate:
  tokens: 17000
  raw_tokens: 17000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "informe.pdf tiene un indice generado por \\tableofcontents antes de la Introduccion"
    - "informe.pdf tiene un resumen/abstract cualitativo antes del indice, describiendo objetivo, metodo y conclusiones principales"
    - "informe.pdf incluye los dos fotogramas de animacion (frame_vicsek_rho2.png, frame_voter_rho2.png) al inicio de Resultados, con el mismo criterio de placeholder de link de video (TODO no renderizado) usado en la presentacion"
  artifacts:
    - TP2/informe/informe.tex
  key_links:
    - "El bloque resumen+indice se inserta despues del \\end{titlepage} agregado por el Plan 01 (wave 1) y antes de \\section{Introduccion}, sin alterar ese plan previo"
    - "Las imagenes de frame se referencian con ruta relativa ../presentacion/frame_*.png explicita en el \\includegraphics (el \\graphicspath del documento solo apunta a ../data/plots/, no a ../presentacion/)"
---

<objective>
Cerrar el item 3 (indice + resumen, D-03) y el item 5 (fotogramas de animacion con link, D-05) del pendiente de entregables del informe de TP2.

Purpose: GuiaInformes.md exige que informe y presentacion sean autocontenidos y sigan una estructura de secciones similar a la presentacion (que ya tiene su seccion "Resultados" con la animacion caracteristica primero, per GuiaPresentaciones.md 2.4.1); el informe actual no tiene indice ni resumen, y no muestra ningun fotograma de la animacion pese a que los archivos ya existen (TP2/presentacion/frame_vicsek_rho2.png, frame_voter_rho2.png).
Output: TP2/informe/informe.tex con \tableofcontents, un resumen, y los dos fotogramas embebidos al inicio de Resultados.

Reversibilidad: ediciones de texto puro en un .tex versionado en git, reversibles con git revert. No hay checkpoints.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@docs/GuiaPresentaciones.md

**Nota de secuencia:** este plan corre en la Wave 2, despues de 260824-i0l-01-PLAN.md (Wave 1), porque ambos modifican TP2/informe/informe.tex. El Plan 01 ya reemplazo `\maketitle` por un bloque `\begin{titlepage}...\end{titlepage}` -- este plan inserta contenido nuevo INMEDIATAMENTE DESPUES de `\end{titlepage}` y ANTES de `\section{Introduccion}`, sin tocar la caratula en si.

**Convencion de placeholder de video (misma que 260824-i0l-02-PLAN.md, D-04/D-05, definida aqui de forma autocontenida -- no hace falta que el Plan 02 ya se haya ejecutado):** el planner/executor NUNCA inventa una URL real. El patron es: un comentario LaTeX `% TODO(usuario): ...` con instrucciones exactas de que archivo subir y donde reemplazar el link, seguido de una unica linea que SI se renderiza con una nota discreta en italica (NO texto en mayusculas entre corchetes tipo "[PEGAR LINK AQUI]").

**Fotogramas ya existentes (no crear, solo referenciar):** TP2/presentacion/frame_vicsek_rho2.png (modelo Vicsek, $\rho=2$, $t=700$, "borde ordenado de la transicion" per el epigrafe ya usado en presentacion.tex) y TP2/presentacion/frame_voter_rho2.png (modelo votante, $\rho=2$, $t=500$). El \graphicspath actual de informe.tex es `{../data/plots/}` unicamente -- estas dos imagenes NO estan ahi, hay que referenciarlas con ruta relativa explicita `../presentacion/frame_vicsek_rho2.png` / `../presentacion/frame_voter_rho2.png` dentro del \includegraphics (no modificar \graphicspath).

**GuiaPresentaciones.md 2.4.1 (aplicado por analogia al informe, per GuiaInformes.md "secciones similares a las presentaciones"):** "para cada input o parametro a estudiar, primero mostrar una animacion caracteristica del sistema... para situar el contexto de los resultados a mostrar" -- por eso los fotogramas van al INICIO de la seccion Resultados, antes de la Fig. va-eta.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Indice y resumen</name>
  <files>TP2/informe/informe.tex</files>
  <read_first>TP2/informe/informe.tex (bloque `\begin{titlepage}...\end{titlepage}` agregado por el Plan 01, y la linea `\section{Introduccion}` inmediatamente despues)</read_first>
  <action>
    En TP2/informe/informe.tex, inmediatamente despues de `\end{titlepage}` y antes de `\section{Introduccion}`, insertar en este orden: (1) un entorno `\begin{abstract}...\end{abstract}` (la clase `article` lo soporta de forma nativa) con un parrafo de 100-180 palabras en español que resuma: que se implementa (Vicsek + modelo de votante, CIM de TP1 reutilizado, caja periodica L=10), que se mide (transicion orden-desorden via $v_a$ y $S$ en funcion de $\eta$, para $\rho=2,4,8$), la conclusion cualitativa principal (Vicsek sostiene el orden hasta $\eta$ mayores que el votante, y ese umbral crece con la densidad en ambos modelos), la explicacion de por que $S$ satura cerca de 1 en las tres densidades del enunciado (el numero medio de vecinos supera el umbral de percolacion continua) y que se agrega un barrido complementario a densidades menores para mostrar esa transicion empiricamente, y una mencion final a la verificacion del escalado del CIM. No citar valores numericos de $\eta_c$ especificos (esos numeros los fija el barrido completo, ya documentado mas abajo en el propio informe) -- mantener el resumen cualitativo para que seguir siendo valido sin importar el orden de ejecucion de los planes siguientes de esta tanda. (2) Un `\clearpage`. (3) `\tableofcontents`. (4) Otro `\clearpage`. Confirmar que `\section{Introduccion}` y todo el contenido posterior queden exactamente como estaban.
  </action>
  <verify>
    <automated>cd TP2/informe && grep -c "begin{abstract}" informe.tex; grep -c "tableofcontents" informe.tex; pdflatex -interaction=nonstopmode -halt-on-error informe.tex > /dev/null 2>&1 && pdflatex -interaction=nonstopmode -halt-on-error informe.tex > /dev/null 2>&1; echo "EXIT=$?"</automated>
  </verify>
  <done>informe.pdf compila (2 pasadas, para resolver el indice) sin error, muestra un resumen antes del indice y un indice generado con las 6 secciones existentes (Introduccion, Modelo, Implementacion, Simulaciones, Resultados, Conclusiones), y el resto del documento no cambio.</done>
</task>

<task type="auto">
  <name>Task 2: Fotogramas de animacion + link al inicio de Resultados</name>
  <files>TP2/informe/informe.tex</files>
  <read_first>TP2/informe/informe.tex (linea `\section{Resultados}` y el parrafo/figura que sigue, "La Fig.~\ref{fig:va-eta} muestra...")</read_first>
  <action>
    En TP2/informe/informe.tex, inmediatamente despues de `\section{Resultados}` y antes del primer parrafo existente ("La Fig.~\ref{fig:va-eta} muestra la polarizacion media..."), insertar: un parrafo breve introduciendo que, antes de los observables agregados, se muestra un fotograma caracteristico de la dinamica simulada para cada modelo a modo de contexto visual, aclarando que el video completo de la animacion fue generado por el modulo de animacion en Python a partir de la trayectoria completa que el motor de TP2 escribe en texto plano (desacoplando la velocidad de la animacion de la velocidad de la simulacion, tal como exige el enunciado); luego el bloque de placeholder de video (comentario `% TODO(usuario):` + nota italica discreta renderizada, segun la convencion del contexto de este plan, mencionando ambos archivos `data/plots/animation_vicsek_rho2.gif` y `data/plots/animation_voter_rho2.gif`); luego un `\begin{figure}[htbp]` con dos `\includegraphics[width=0.45\textwidth]` uno al lado del otro (rutas explicitas `../presentacion/frame_vicsek_rho2.png` y `../presentacion/frame_voter_rho2.png`, SIN modificar `\graphicspath`), con un `\caption` describiendo "Fotograma caracteristico de la animacion, $\rho=2$. Izquierda: modelo de Vicsek, $t=700$. Derecha: modelo de votante, $t=500$. Cada flecha representa la velocidad instantanea de una particula." y un `\label{fig:frames}`. No modificar el parrafo ni la figura de va-eta que sigue.
  </action>
  <verify>
    <automated>cd TP2/informe && grep -c "frame_vicsek_rho2" informe.tex; grep -c "frame_voter_rho2" informe.tex; grep -c "PEGAR LINK" informe.tex; pdflatex -interaction=nonstopmode -halt-on-error informe.tex > /dev/null 2>&1; echo "EXIT=$?"; test -f informe.pdf && echo "PDF_OK"</automated>
  </verify>
  <done>informe.pdf compila sin error; la seccion Resultados abre con los dos fotogramas (Vicsek y votante) antes de la Fig. va-eta, con un placeholder de link de video convertido a comentario TODO + nota discreta (grep de "PEGAR LINK" = 0), sin URLs inventadas.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Ninguna | Edicion de texto local en un .tex versionado en git, sin entrada externa ni datos no confiables |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-QI03-01 | Tampering | Placeholder de link de video en el informe | low | mitigate | Misma mitigacion que T-QI02-02: no se inventa ninguna URL, el TODO queda explicito en comentario, el texto renderizado es una nota discreta |

</threat_model>

<verification>
`pdflatex -interaction=nonstopmode -halt-on-error informe.tex` (2 pasadas, cwd TP2/informe) compila sin error; grep confirma \tableofcontents, \begin{abstract}, las dos referencias a frame_*.png, y cero ocurrencias de "PEGAR LINK".
</verification>

<success_criteria>
informe.pdf tiene indice y resumen antes de la Introduccion (item 3), y la seccion Resultados abre con los dos fotogramas de animacion y su link (aun sin resolver, marcado como TODO) antes de cualquier observable agregado (item 5).
</success_criteria>

<output>
Create `.planning/quick/260824-i0l-cerrar-pendientes-de-entregables-tp2-inf/260824-i0l-03-SUMMARY.md` when done
</output>
