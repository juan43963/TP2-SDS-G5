---
phase: 260824-i0l-cerrar-pendientes-de-entregables-tp2-inf
plan: 04
type: execute
wave: 3
depends_on: ["260824-i0l-03"]
files_modified:
  - TP2/python/analyze.py
  - TP2/informe/informe.tex
  - TP2/data/sweep/vicsek/rho2/eta0.785398/
  - TP2/data/sweep/vicsek/rho4/eta0.785398/
  - TP2/data/sweep/vicsek/rho8/eta0.785398/
  - TP2/data/sweep/voter/rho2/eta0.112200/
  - TP2/data/sweep/voter/rho4/eta0.112200/
  - TP2/data/sweep/voter/rho8/eta0.112200/
  - TP2/data/plots/
autonomous: true
requirements: [AUDIT-06]

estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "pick_representative_eta nunca devuelve eta=0 salvo que el grupo (rho,model) no tenga ningun punto con eta>0 en absoluto"
    - "Las 4 figuras de evolucion temporal rho=2 embebidas en el informe (va_t_vicsek_rho2, va_t_voter_rho2, S_t_vicsek_rho2, S_t_voter_rho2) se generan a partir de corridas completas de 2000 pasos, no de archivos truncados a 500 pasos dejados por el mini-barrido exploratorio"
    - "eta_c_table.csv es identico antes y despues del cambio de criterio, confirmado por un diff explicito documentado en el SUMMARY"
    - "El parrafo del informe sobre la convergencia del votante ya no afirma que 'solo alcanza el regimen ordenado hacia el final de la ventana simulada' sin respaldo -- se reformula en terminos de la dispersion real de tiempos de convergencia entre semillas"
  artifacts:
    - TP2/python/analyze.py
    - TP2/informe/informe.tex
    - TP2/data/sweep/summary.csv (sin cambios de contenido, solo re-verificado)
  key_links:
    - "pick_representative_eta solo determina que archivo de log elige _representative_log_path para las 4 figuras de evolucion temporal -- compute_chi/compute_eta_c_table leen data/sweep/summary.csv directamente y nunca pasan por esa funcion, por eso no pueden cambiar"
---

<objective>
Cerrar el item 6 del pendiente de entregables (D-06): el bug en `pick_representative_eta` (TP2/python/analyze.py) que siempre selecciona el caso trivial $\eta=0$ para ilustrar el punto (b) del enunciado, dejado documentado en Auditoria_TP2.md hallazgos E0.5/E0.6/E0.7, y la afirmacion no respaldada del informe sobre la convergencia del votante que ese bug produjo.

Purpose: `pick_representative_eta` busca "el eta mas chico con va_mean >= 0.8", pero como la grilla arranca en $\eta=0$ y ahi ambos modelos ya estan practicamente ordenados, esa condicion se cumple trivialmente en el primer punto -- la funcion SIEMPRE devuelve $\eta=0$, pese a que su propio docstring dice "no el caso trivial eta=0". Esto ilustra el punto (b) del enunciado ("mostrar evoluciones temporales caracteristicas para indicar los criterios usados") con el caso menos representativo, y llevo a una afirmacion en el informe (voter "solo alcanza el regimen ordenado hacia el final de la ventana simulada") que la propia auditoria midio como falsa para ese caso trivial.
Output: analyze.py con el criterio corregido; los 4 PNG de evolucion temporal rho=2 regenerados con casos $\eta>0$ reales; informe.tex con las 4 leyendas y el parrafo descriptivo actualizados con numeros medidos, no inventados.

Reversibilidad: cambio de codigo + regeneracion de artefactos derivados (PNG, no versionados en git), reversible con git revert sobre analyze.py/informe.tex. Los datos crudos regenerados (data/sweep/.../seed*.txt) estan gitignored, no hay riesgo de commit accidental. No hay checkpoints.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@docs/docs/Auditoria_TP2.md

**Hallazgos E0.5/E0.6/E0.7 (leer esta seccion primero en Auditoria_TP2.md, "Etapa 0"):** `pick_representative_eta` en `TP2/python/analyze.py` (funcion `pick_representative_eta`, cerca de la linea 323 al momento de escribir este plan) siempre retorna $\eta=0$; el docstring ya documenta la intencion correcta ("no el caso trivial eta=0") pero el codigo no la cumple; el informe (`informe.tex`, parrafo que empieza "Las Figs.~\ref{fig:va-t-vicsek} y~\ref{fig:va-t} muestran la evolucion temporal...") afirma sobre el votante que "solo alcanza el regimen ordenado hacia el final de la ventana simulada", medido como falso para $\eta=0$ (va$\geq$0.99 en t=409 de 2000, 20% de la corrida).

**Datos ya verificados por el planner directamente sobre data/sweep/summary.csv (no volver a derivar el criterio, solo aplicarlo):** con el fix propuesto abajo, el eta representativo nuevo es $\eta=0.785398$ ($\pi/4$) para las 3 densidades de Vicsek, y $\eta=0.112200$ para las 3 densidades del votante (unico eta>0 de la grilla gruesa con va_mean>=0.8 para vicsek; para voter cae al fallback "max va_mean entre eta>0" ya que ningun eta>0 llega a 0.8, pero aun asi es $\eta=0.1122$ para las tres densidades). Para $\rho=2$ especificamente (las 4 figuras embebidas en el informe): Vicsek $\eta=0.785398$ (va\_mean=0.954), votante $\eta=0.112200$ (va\_mean=0.841).

**Riesgo de datos ya confirmado por el planner (no asumir, regenerar de todas formas per Task 1):** el mini-barrido exploratorio (`explore_transition`, 500 pasos) y el barrido completo (2000 pasos) usan la MISMA formula `derive_seed(rho,eta,model,repeat_index)` y el MISMO `sweep_output_path`, asi que para puntos de la grilla gruesa (como $\eta=0.785398=\pi/4$, que es el punto i=1 de la grilla gruesa de 9 puntos) ambos escriben al mismo archivo. En el disco actual, para (vicsek, $\rho=2$, $\eta=0.785398$) el archivo que corresponde a repeat\_index=0 (`data/sweep/vicsek/rho2/eta0.785398/seed4401168745193486683.txt`) HABIA QUEDADO TRUNCADO a 501 lineas (corrida exploratoria de 500 pasos, no la de 2000 del barrido completo) -- el planner ya lo regenero manualmente durante la investigacion de este plan (ahora tiene 2001 lineas), pero Task 1 DEBE regenerar los 6 puntos representativos de todas formas de forma reproducible como parte del plan (no confiar en que quedo bien porque el planner lo toco una vez).

**Numeros ya medidos por el planner sobre los 5 seeds oficiales de cada punto (ambos ya regenerados/confirmados a 2001 lineas, usar estos numeros salvo que Task 1 obtenga otros al re-verificar):**
- Vicsek $\rho=2$, $\eta=0.785398$: $v_a \geq 0.8$ cruzado entre $t=39$ y $t=160$ segun la semilla (rango sobre 5 seeds).
- Votante $\rho=2$, $\eta=0.112200$: $v_a \geq 0.8$ cruzado entre $t=45$ y $t=546$ segun la semilla; $v_a$ NUNCA alcanza $\geq 0.99$ en los 2000 pasos, en ninguna de las 5 semillas.
- La ventana de estado estacionario arranca en $t=1000$ (50% de 2000 pasos) en ambos casos.

Path de pdflatex confirmado en PATH: `pdflatex`. WSL con Ubuntu-24.04 tiene g++/make/python3 (con matplotlib 3.11.1, numpy 2.5.1) y el binario compilado `TP2/tp2` (ELF Linux) ya corre correctamente ahi -- usar SIEMPRE `wsl.exe -- bash -lc "cd '/mnt/c/Users/Lucas Di Candia/Desktop/SDS/TP2-SDS-G5/TP2' &amp;&amp; ..."` para invocar tp2/python3 desde este entorno Windows, tal como ya se hace en 05-04-PLAN.md.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix de pick_representative_eta + regeneracion de datos y figuras</name>
  <files>TP2/python/analyze.py, TP2/data/sweep/{vicsek,voter}/rho{2,4,8}/eta{0.785398,0.112200}/seed*.txt, TP2/data/plots/*.png, TP2/data/plots/eta_c_table.csv</files>
  <behavior>
    - `pick_representative_eta(rows, rho=2.0, model="vicsek")` sobre un grupo donde el eta=0 tiene va_mean=1.0 y hay al menos un eta&gt;0 con va_mean&gt;=0.8: debe devolver ese eta&gt;0, nunca 0.0.
    - `pick_representative_eta(rows, rho, model)` sobre un grupo donde NINGUN eta&gt;0 alcanza va_mean&gt;=0.8 (caso votante): debe devolver el eta&gt;0 de mayor va_mean del grupo (nunca eta=0, salvo que el grupo no tenga NINGUN eta&gt;0).
    - `pick_representative_eta(rows, rho, model)` sobre un grupo donde el UNICO punto es eta=0 (caso degenerado, no deberia ocurrir en la grilla real pero debe estar cubierto): debe devolver eta=0 como ultimo recurso, sin lanzar excepcion.
  </behavior>
  <read_first>TP2/python/analyze.py (funcion `pick_representative_eta`, y `_representative_log_path` que la consume); TP2/data/sweep/summary.csv (columnas model,rho,eta,va_mean,va_std,S_mean,S_std,n_seeds)</read_first>
  <action>
    En TP2/python/analyze.py, reescribir `pick_representative_eta` para que excluya explicitamente $\eta=0$ (E0.6: el docstring ya dice "no el caso trivial eta=0", el codigo debe cumplirlo): filtrar primero el grupo a `eta > 0`; recorrer ese subconjunto ordenado por eta buscando el primero con `va_mean >= 0.8`, igual que antes; si ninguno alcanza el umbral, hacer fallback al `max(..., key=va_mean)` PERO sobre el subconjunto `eta > 0` (no sobre el grupo completo); solo si el subconjunto `eta > 0` esta vacio, hacer fallback final al grupo completo (incluyendo eta=0) para no lanzar excepcion en un caso degenerado. Actualizar el docstring para reflejar el nuevo comportamiento con precision. NO tocar `_representative_log_path`, `plot_scalar_timeseries`, `compute_chi` ni `compute_eta_c_table` (esas funciones no cambian). Escribir el test de comportamiento descrito en `&lt;behavior&gt;` como parte de la seccion de verificaciones internas del modulo (si existe un patron de selftest reusable en el proyecto, seguirlo; si no, un bloque `if __name__ == "__main__":`-adyacente o assertions inline ejecutadas por el comando de verify de esta task es aceptable dado el alcance quick). Luego, via WSL, regenerar de forma reproducible los 5 seeds (repeat_index 0-4, formula `derive_seed(rho,eta,model,repeat_index)` de `sweep.py`) para los 6 puntos representativos exactos: (vicsek, 2.0, 0.785398), (vicsek, 4.0, 0.785398), (vicsek, 8.0, 0.785398), (voter, 2.0, 0.112200), (voter, 4.0, 0.112200), (voter, 8.0, 0.112200) -- reusando `run_one` y `derive_seed` de `sweep.py` (import, no reimplementar), con `steps=2000` (el default), para garantizar que ninguno quede con datos truncados del mini-barrido exploratorio de 500 pasos. Despues correr `python3 python/analyze.py` completo (via WSL) para regenerar TODOS los PNG + `eta_c_table.csv` a partir del `pick_representative_eta` corregido y del `summary.csv` sin cambios. Guardar una copia de `eta_c_table.csv` ANTES de este re-run (ya existe en disco de la ejecucion anterior) para poder diffearla en Task 2.
  </action>
  <verify>
    <automated>wsl.exe -- bash -lc "cd '/mnt/c/Users/Lucas Di Candia/Desktop/SDS/TP2-SDS-G5/TP2' && python3 -c \"import sys; sys.path.insert(0,'python'); from analyze import pick_representative_eta; rows=[{'rho':2.0,'model':'vicsek','eta':0.0,'va_mean':1.0},{'rho':2.0,'model':'vicsek','eta':0.5,'va_mean':0.9}]; assert pick_representative_eta(rows,2.0,'vicsek')==0.5, 'debe excluir eta=0'; print('PICK_ETA_OK')\" && python3 python/analyze.py && wc -l data/sweep/vicsek/rho2/eta0.785398/*.txt data/sweep/voter/rho2/eta0.112200/*.txt"</automated>
  </verify>
  <acceptance_criteria>PICK_ETA_OK se imprime; analyze.py corre sin error y regenera todos los PNG + eta_c_table.csv; todos los archivos seed*.txt de los 6 puntos representativos tienen 2001 lineas (ningun truncado a 501).</acceptance_criteria>
  <done>pick_representative_eta excluye eta=0 salvo en el caso degenerado sin ningun eta&gt;0; los 6 puntos representativos tienen datos completos de 2000 pasos; los PNG de evolucion temporal y eta_c_table.csv quedan regenerados a partir del criterio corregido.</done>
</task>

<task type="auto">
  <name>Task 2: Actualizar informe.tex con los nuevos casos representativos</name>
  <files>TP2/informe/informe.tex</files>
  <read_first>TP2/informe/informe.tex (parrafo "Las Figs.~\ref{fig:va-t-vicsek} y~\ref{fig:va-t} muestran la evolucion temporal..." y las 4 leyendas \caption de las Figs. va-t-vicsek, va-t, s-t-vicsek, s-t-voter); TP2/data/plots/eta_c_table.csv (version regenerada por Task 1) vs. la version previa a Task 1 guardada por esa misma task</read_first>
  <action>
    En TP2/informe/informe.tex, localizar el parrafo que introduce las Figs.~\ref{fig:va-t-vicsek} y~\ref{fig:va-t} (describe una corrida caracteristica a ruido nulo, termina mencionando la mayor dispersion del votante en las Figs.~\ref{fig:va-eta} y~\ref{fig:s-eta}) y reemplazarlo completo por un parrafo nuevo que: (1) aclare que se ilustra un punto no trivial de la transicion ($\eta&gt;0$), evitando el caso de ruido nulo donde ambos modelos ordenan de forma casi instantanea; (2) para Vicsek ($\rho=2$, $\eta=0.7854$) reporte que $v_a(t)$ crece de forma suave, superando $v_a\geq0.8$ entre $t=39$ y $t=160$ segun la semilla (rango sobre las $K=5$ semillas oficiales del punto), muy por debajo del inicio de la ventana de estado estacionario en $t=1000$; (3) para el votante ($\rho=2$, $\eta=0.1122$) reporte que $v_a(t)$ crece de forma mas ruidosa y dispersa entre semillas -- consistente con \emph{coarsening} por copia local --, superando $v_a\geq0.8$ recien entre $t=45$ y $t=546$ segun la semilla, y sin alcanzar $v_a\geq0.99$ dentro de los 2000 pasos en ninguna de las 5 semillas, lo que explica la mayor dispersion (barras de error) observada para el votante en las Figs. va-eta y S-eta. Usar los numeros del contexto de este plan, PERO si Task 1 re-verifico numeros distintos al regenerar, usar esos en su lugar. Actualizar las 4 leyendas `\caption` de las Figs. va-t-vicsek, va-t (voter), s-t-vicsek, s-t-voter: cada una menciona actualmente el mismo ruido nulo del parrafo de arriba -- cambiar esa mencion al nuevo valor no trivial correspondiente (el de Vicsek para las dos figuras de Vicsek, el del votante para las dos del votante). NO tocar el parrafo siguiente sobre las Figs. s-t-vicsek/s-t-voter (el que dice "En Vicsek, $S$ salta a $\approx 1$ casi inmediatamente...") -- ya se verifico que sigue siendo una descripcion correcta para los nuevos puntos representativos. Finalmente, diffear `data/plots/eta_c_table.csv` (antes vs. despues de Task 1) y, si son identicos (esperado, ya que compute_eta_c_table no depende de pick_representative_eta), agregar una oracion breve al final del parrafo sobre la Tabla~\ref{tab:eta_c} confirmando explicitamente que $\eta_c$ y $\chi(\eta)$ no cambiaron con la correccion del criterio de seleccion del caso ilustrativo, ya que ambos se calculan directamente sobre la grilla completa de `summary.csv` sin depender de que corrida particular se grafica. Si el diff SI muestra diferencias, no agregar esa oracion y en su lugar documentar la discrepancia en el SUMMARY para revision humana.
  </action>
  <verify>
    <automated>cd TP2/informe && grep -c "eta=0.7854" informe.tex; grep -c "eta=0.1122" informe.tex; grep -c "\$\\\\rho=2\\$, \$\\\\eta=0\$" informe.tex; pdflatex -interaction=nonstopmode -halt-on-error informe.tex > /dev/null 2>&1; echo "EXIT=$?"</automated>
  </verify>
  <done>informe.pdf compila sin error; las 4 leyendas y el parrafo descriptivo citan los nuevos etas no triviales con numeros de convergencia medidos (grep de "$\rho=2$, $\eta=0$" en el cuerpo de esas figuras = 0); el SUMMARY documenta si eta_c_table.csv cambio o no.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WSL subprocess (tp2 binario) | `run_one`/`run_sweep` de sweep.py invocan el binario compilado con argumentos en lista (sin shell=True) -- sin entrada externa, todos los valores (rho/eta/seed) son constantes internas de este plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-QI04-01 | Tampering | Regeneracion de datos de barrido | low | accept | Se reusan exactamente `run_one`/`derive_seed` ya testeados de sweep.py (misma formula de semilla determinista), no se reimplementa logica nueva de simulacion |
| T-QI04-02 | Repudiation | Afirmacion del informe sobre convergencia del votante | medium | mitigate | La nueva redaccion cita numeros medidos directamente sobre los 5 seeds oficiales del barrido (reproducibles via semilla determinista), reemplazando una afirmacion sin respaldo empirico ya detectada por la auditoria (E0.7) |

</threat_model>

<verification>
`python3 python/analyze.py` (WSL) corre sin error tras el fix; los 6 puntos representativos tienen logs de 2001 lineas; `pdflatex -interaction=nonstopmode -halt-on-error informe.tex` compila sin error tras Task 2; grep confirma que ninguna de las 4 figuras de evolucion temporal rho=2 sigue citando "$\eta=0$".
</verification>

<success_criteria>
El criterio de seleccion del caso representativo del punto (b) ya no es trivial (item 6a); las 4 figuras/leyendas afectadas quedan regeneradas con datos completos y no truncados (item 6b); la afirmacion no respaldada sobre el votante queda reformulada en terminos de dispersion de tiempos de convergencia medida (item 6c); se confirma explicitamente (no se asume) si eta_c/chi cambiaron (item 6d).
</success_criteria>

<output>
Create `.planning/quick/260824-i0l-cerrar-pendientes-de-entregables-tp2-inf/260824-i0l-04-SUMMARY.md` when done
</output>
