---
phase: 260824-i0l-cerrar-pendientes-de-entregables-tp2-inf
plan: 05
type: execute
wave: 4
depends_on: ["260824-i0l-04"]
files_modified:
  - TP2/python/analyze.py
  - TP2/data/sweep/percolation_summary.csv
  - TP2/data/plots/S_rho.png
  - TP2/informe/informe.tex
autonomous: true
requirements: [AUDIT-07]

estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "El informe incluye la tabla analitica <k>=rho*pi*rc^2 comparada contra el umbral de percolacion continua (aprox 4.51), para las densidades del enunciado y las de la consulta C1"
    - "El informe incluye una figura S(rho) generada a partir de un barrido real ejecutado en esta maquina (no simulado, no inventado), a densidades rho<1.5"
    - "RHO_COLORS/RHO_MARKERS en analyze.py ya no lanzan ValueError para las densidades nuevas del barrido de percolacion"
  artifacts:
    - TP2/data/sweep/percolation_summary.csv
    - TP2/data/plots/S_rho.png
    - TP2/informe/informe.tex
  key_links:
    - "percolation_summary.csv se genera reusando run_sweep/aggregate_to_csv de sweep.py (no codigo nuevo de orquestacion de barrido); plot_S_rho en analyze.py lo consume via --percolation-summary y se omite con un aviso (no crashea) si el archivo no existe"
---

<objective>
Cerrar el item 7 del pendiente de entregables: agregar al informe el calculo analitico de percolacion geometrica (<k> = rho*pi*rc^2 vs. el umbral ~4.51) como justificacion teorica de por que S satura cerca de 1 en rho=2,4,8, y complementarlo con un barrido empirico real a densidades rho<1.5 que muestre la transicion de percolacion en S(rho).

Purpose: Auditoria_TP2.md (hallazgo E0.10) y docs/docs/consultas-tp.md (consulta C1) ya diagnosticaron que con rho=2,4,8 el numero medio de vecinos geometricos supera ampliamente el umbral de percolacion continua 2D, por lo que S(eta) queda plano cerca de 1 y el punto (d) del enunciado no es informativo en esas densidades -- el usuario aprobo cerrar esto con AMBOS: la justificacion analitica y un barrido empirico nuevo.
Output: analyze.py con `plot_S_rho` + RHO_COLORS/RHO_MARKERS extendidos; TP2/data/sweep/percolation_summary.csv (barrido real); TP2/data/plots/S_rho.png; informe.tex con la tabla analitica + la figura S(rho) + una mencion en Conclusiones.

Reversibilidad: codigo + datos regenerables (percolation_summary.csv y S_rho.png no estan versionados en git, TP2/data/ esta gitignored); analyze.py/informe.tex son reversibles con git revert. No hay checkpoints.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@docs/docs/Auditoria_TP2.md
@docs/docs/consultas-tp.md

**Hallazgo E0.10 / consulta C1 (leer ambos primero):** con rc=1, el numero medio de vecinos geometricos es &lt;k&gt; = rho*pi*rc^2. Para rho=2,4,8 (enunciado) da 6.28/12.6/25.1, muy por encima del umbral de percolacion continua bidimensional para discos (&lt;k&gt;_c aprox 4.51, valor estandar de teoria de percolacion continua -- no hay una cita bibliografica formal disponible en este repositorio para ese numero, no inventar una referencia numerada en Referencias por esto). Para las densidades de la consulta C1 (1/pi, 1/(2pi), 1/(3pi), aprox 0.318/0.159/0.106) da 1.00/0.50/0.33, por debajo del umbral -- esas tres densidades quedaron documentadas en la consulta pero, per E0.10, NUNCA se corrieron.

**Diseno del barrido de percolacion ya decidido por el planner (D-07b), ejecutar exactamente asi:** grilla fina `rho = [0.15*i for i in range(1, 11)]` (0.15, 0.30, 0.45, 0.60, 0.75, 0.90, 1.05, 1.20, 1.35, 1.50 -- 10 valores, N entre 15 y 150 con L=10), ambos modelos (vicsek, voter), ruido fijo eta=0 (percolacion geometrica pura, no un barrido de eta), K=5 semillas, steps=2000 (el default de sweep.py). El umbral analitico en terminos de rho es rho_c = 4.51/(pi*rc^2) = 4.51/pi aprox 1.4355, que cae dentro del rango barrido (entre 1.35 y 1.50) -- por eso el rango 0.15-1.50 fue elegido, para mostrar la transicion completa. Esta grilla NO pisa ningun dato existente (rho=2,4,8 y las tres de C1 no se tocan).

**Costo ya verificado por el planner (WSL, Ubuntu-24.04, mismo entorno que 260824-i0l-04):** 10 runs secuenciales de N=150 (el caso mas caro de esta grilla), 2000 pasos, tardaron 2.486s totales dentro de una sola sesion WSL -- el barrido completo (10 rho x 2 modelos x 5 semillas = 100 corridas) toma bajo 1 minuto. Usar SIEMPRE `wsl.exe -- bash -lc "cd '/mnt/c/Users/Lucas Di Candia/Desktop/SDS/TP2-SDS-G5/TP2' &amp;&amp; ..."` para invocar python3/tp2 desde este entorno Windows (mismo patron que 05-04-PLAN.md y 260824-i0l-04-PLAN.md).

**Reuso obligatorio (D-07b, "reusar sweep.py y analyze.py"):** NO escribir un script nuevo de orquestacion de barrido. `run_sweep(tasks, steps=...)` y `aggregate_to_csv(results, csv_path)` de `TP2/python/sweep.py` ya implementan exactamente lo necesario (pool de procesos, aislamiento de fallos por combinacion, agregacion K-semillas -> CSV con el mismo esquema `model,rho,eta,va_mean,va_std,S_mean,S_std,n_seeds` que `data/sweep/summary.csv`) -- importarlas y llamarlas con `tasks = [(model, rho, 0.0, repeat_index) for model in (...) for rho in (...) for repeat_index in range(5)]`, sin reimplementar nada de eso.

**analyze.py ya tiene los bloques de color/estilo reusables:** `COLOR_VICSEK`/`COLOR_VOTER` (para distinguir modelo por color, igual que en las funciones existentes) y `MARKER_VICSEK_SCATTER`/`MARKER_VOTER_SCATTER` (marcador por modelo). `RHO_COLORS`/`RHO_MARKERS` (cerca de la linea 52-55 al momento de escribir este plan) son un diccionario fijo que hoy solo cubre {2.0, 4.0, 8.0} y lanza ValueError para cualquier otro rho -- extenderlos con las 10 densidades nuevas (no hace falta que `plot_S_rho`, que colorea por modelo, los use directamente; se extienden igual porque otras funciones existentes (`plot_va_eta`, `plot_S_eta`, `plot_va_vs_S`) fallarian si alguna vez se les pasan filas de percolacion, y la auditoria pidio explicitamente extender estos diccionarios).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extender analyze.py (RHO_COLORS/RHO_MARKERS + plot_S_rho + CLI)</name>
  <files>TP2/python/analyze.py</files>
  <read_first>TP2/python/analyze.py (RHO_COLORS/RHO_MARKERS, COLOR_VICSEK/COLOR_VOTER, MARKER_VICSEK_SCATTER/MARKER_VOTER_SCATTER, plot_va_vs_S como referencia de estructura, y la funcion main() con su argparse)</read_first>
  <action>
    En TP2/python/analyze.py: (1) Extender los diccionarios `RHO_COLORS` y `RHO_MARKERS` con una entrada para cada uno de los 10 valores de la grilla de percolacion del contexto (0.15, 0.30, ..., 1.50), generando colores distintos entre si de forma programatica (por ejemplo muestreando un colormap de matplotlib como `plt.cm.viridis` en 10 puntos equiespaciados y convirtiendo a hex) y un marcador comun ("d", diamante, distinto de los "o"/"s"/"^" ya usados por {2,4,8}) -- mantener el comportamiento existente de ValueError para cualquier rho fuera de ambos conjuntos (no reemplazar por un fallback silencioso). (2) Agregar una constante `RHO_C_PERCOLATION = 4.51 / math.pi` (umbral analitico de percolacion continua en terminos de rho, rc=1). (3) Agregar una funcion nueva `plot_S_rho(rows, out_path=None, show=False)` con la misma forma general que `plot_va_vs_S` (mismo patron de creacion de figura, mismo guardado condicional a `show`), pero agrupando `rows` SOLO por `model` (no por rho, ya que aqui rho es el eje x): para cada modelo, ordenar el grupo por rho ascendente y graficar `ax.errorbar(rhos, S_mean, yerr=S_std, color=COLOR_VICSEK o COLOR_VOTER, marker=MARKER_VICSEK_SCATTER o MARKER_VOTER_SCATTER, capsize=3, label=model)`; agregar `ax.axvline(RHO_C_PERCOLATION, color="gray", linestyle=":", label=...)` con el valor del umbral en la etiqueta; ejes "rho"/"S", titulo describiendo S vs rho a eta=0, leyenda, guardar por defecto en `PLOTS_DIR / "S_rho.png"`. (4) En `main()`, agregar el argumento `--percolation-summary` (tipo Path, default `TP2_DIR / "data" / "sweep" / "percolation_summary.csv"`) y, despues de generar los artefactos existentes, si ese archivo existe cargarlo con `load_summary` y llamar `plot_S_rho`, imprimiendo la ruta generada; si NO existe, imprimir un aviso indicando que hay que correr primero el barrido de percolacion (Task 2 de este plan), sin lanzar excepcion ni abortar el resto de `main()`.
  </action>
  <verify>
    <automated>wsl.exe -- bash -lc "cd '/mnt/c/Users/Lucas Di Candia/Desktop/SDS/TP2-SDS-G5/TP2' && python3 -c \"import sys; sys.path.insert(0,'python'); import analyze; assert analyze.RHO_COLORS.get(1.5) is not None, 'falta 1.5 en RHO_COLORS'; assert analyze.RHO_MARKERS.get(0.15) is not None, 'falta 0.15 en RHO_MARKERS'; assert abs(analyze.RHO_C_PERCOLATION - 1.4355) &lt; 0.001, analyze.RHO_C_PERCOLATION; assert callable(analyze.plot_S_rho); print('ANALYZE_EXT_OK')\""</automated>
  </verify>
  <done>RHO_COLORS/RHO_MARKERS cubren las 10 densidades nuevas sin perder el ValueError para valores realmente inesperados; RHO_C_PERCOLATION esta definido con el valor correcto; plot_S_rho existe y sigue el mismo patron de las funciones plot_* existentes; main() genera S_rho.png automaticamente si percolation_summary.csv existe, y avisa (sin crashear) si no.</done>
</task>

<task type="auto">
  <name>Task 2: Correr el barrido de percolacion real + regenerar S_rho.png</name>
  <files>TP2/data/sweep/percolation_summary.csv, TP2/data/plots/S_rho.png</files>
  <precondition>Task 1 de este mismo plan ya agrego plot_S_rho y el flag --percolation-summary a analyze.py.</precondition>
  <read_first>TP2/python/sweep.py (run_sweep, aggregate_to_csv -- funciones a reusar sin modificar)</read_first>
  <action>
    Via WSL, ejecutar un unico comando `python3 -c "..."` (cwd TP2/) que: importe `run_sweep` y `aggregate_to_csv` de `sweep.py`; construya `rhos = [round(0.15*i, 2) for i in range(1, 11)]`; construya `tasks = [(model, rho, 0.0, repeat_index) for model in ("vicsek", "voter") for rho in rhos for repeat_index in range(5)]` (100 tareas); llame `results, failures = run_sweep(tasks, steps=2000)`; llame `aggregate_to_csv(results, Path("data/sweep/percolation_summary.csv"))`; imprima la cantidad de resultados OK y de fallos. Si `failures` no esta vacio, no continuar a la regeneracion de la figura sin antes documentar en el SUMMARY cuales combinaciones fallaron y por que (releyendo el mensaje de error de cada fallo). Con `percolation_summary.csv` generado (20 filas esperadas: 10 rho x 2 modelos), correr `python3 python/analyze.py` (sin argumentos adicionales, ya que `--percolation-summary` tiene el default correcto) para regenerar todos los artefactos existentes MAS `S_rho.png` como efecto de la Task 1.
  </action>
  <verify>
    <automated>wsl.exe -- bash -lc "cd '/mnt/c/Users/Lucas Di Candia/Desktop/SDS/TP2-SDS-G5/TP2' && python3 -c \"import sys; sys.path.insert(0,'python'); from pathlib import Path; from sweep import run_sweep, aggregate_to_csv; rhos=[round(0.15*i,2) for i in range(1,11)]; tasks=[(m,r,0.0,i) for m in ('vicsek','voter') for r in rhos for i in range(5)]; results,failures=run_sweep(tasks,steps=2000); aggregate_to_csv(results, Path('data/sweep/percolation_summary.csv')); print('PERC_SWEEP_OK', len(results), len(failures))\" && python3 python/analyze.py && wc -l data/sweep/percolation_summary.csv && test -s data/plots/S_rho.png && echo S_RHO_PNG_OK"</automated>
  </verify>
  <acceptance_criteria>PERC_SWEEP_OK se imprime con 100 resultados y 0 fallos (o, si hay fallos, quedan documentados explicitamente en el SUMMARY); percolation_summary.csv tiene 21 lineas (20 filas + header); S_RHO_PNG_OK se imprime.</acceptance_criteria>
  <done>data/sweep/percolation_summary.csv contiene el barrido real (10 densidades x 2 modelos, eta=0, K=5 semillas cada uno) y data/plots/S_rho.png se genero a partir de esos datos reales, no de valores inventados.</done>
</task>

<task type="auto">
  <name>Task 3: Agregar al informe la tabla analitica y la figura S(rho)</name>
  <files>TP2/informe/informe.tex</files>
  <read_first>TP2/informe/informe.tex (parrafo que introduce la Fig.~\ref{fig:s-eta} y el que sigue introduciendo la Fig.~\ref{fig:va-vs-s}; seccion Conclusiones, parrafo sobre la fraccion del cluster gigante)</read_first>
  <action>
    En TP2/informe/informe.tex, inmediatamente despues del parrafo que introduce y comenta la Fig.~\ref{fig:s-eta} (el que explica que S se mantiene cercano a 1 salvo en Vicsek rho=2) y ANTES del parrafo que introduce la Fig.~\ref{fig:va-vs-s}, insertar: (1) un parrafo explicando que la saturacion de S se debe a que el numero medio de vecinos geometricos &lt;k&gt; = rho*pi*rc^2 supera ampliamente el umbral de percolacion continua bidimensional (aprox 4.51) en las tres densidades del enunciado; (2) una tabla LaTeX (booktabs, igual estilo que la Tabla~\ref{tab:eta_c} ya existente) con columnas rho, N=rho*L^2 (L=10), &lt;k&gt;=rho*pi*rc^2, Regimen, listando rho=8,4,2 (supercritico) y las tres densidades de la consulta C1 -- 1/pi, 1/(2pi), 1/(3pi) -- (subcritico), con una fila intermedia marcando el umbral aprox 4.51; (3) un parrafo describiendo el barrido complementario real corrido en Task 2 de este plan (rho entre 0.15 y 1.50, 10 valores, ambos modelos, eta=0, K=5 semillas, N entre 15 y 150), reusando sweep.py/analyze.py, y presentando la Fig. S(rho) resultante (`\includegraphics` de `S_rho.png`, ya alcanzable via el `\graphicspath` existente de `../data/plots/`) con un `\caption` que mencione el umbral analitico marcado en la figura (linea vertical punteada, rho_c aprox 1.44) y un `\label{fig:s-rho}`. Cruzar referencias entre el parrafo (3) y la tabla de (2) usando `\ref{}`. Finalmente, en la seccion Conclusiones, en el parrafo que ya discute la fraccion del cluster gigante S, agregar una oracion breve al final resumiendo la explicacion geometrica (numero medio de vecinos vs. umbral de percolacion) y mencionando que el barrido complementario a densidades menores confirma empiricamente la transicion. No modificar ningun otro parrafo de Resultados ni Conclusiones.
  </action>
  <verify>
    <automated>cd TP2/informe && grep -c "S_rho.png" informe.tex; grep -c "fig:s-rho" informe.tex; grep -c "4.51" informe.tex; pdflatex -interaction=nonstopmode -halt-on-error informe.tex > /dev/null 2>&1 && pdflatex -interaction=nonstopmode -halt-on-error informe.tex > /dev/null 2>&1; echo "EXIT=$?"</automated>
  </verify>
  <done>informe.pdf compila (2 pasadas) sin error; incluye la tabla analitica &lt;k&gt; vs. umbral de percolacion, la Fig. S(rho) con su umbral marcado, y una mencion en Conclusiones -- ambos sub-items del item 7 (analitico y empirico) presentes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WSL subprocess (tp2 binario, pool de procesos de sweep.py) | run_sweep invoca el binario compilado con argumentos en lista (sin shell=True) desde multiples procesos worker -- sin entrada externa, todos los valores (modelo/rho/eta/seed) son constantes internas de este plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-QI05-01 | Tampering | Barrido de percolacion (subprocess args) | low | accept | Se reusa exactamente run_sweep/aggregate_to_csv de sweep.py (mismo pool de procesos, misma construccion de argumentos en lista ya usada en 510+ corridas del barrido principal), sin codigo nuevo de orquestacion |
| T-QI05-02 | Denial of Service | Costo computacional del barrido nuevo (100 corridas) | low | accept | Costo medido por el planner antes de escribir este plan (bajo 1 minuto en WSL, N maximo 150); muy por debajo del limite RUN_TIMEOUT_S=300s por corrida ya definido en sweep.py |

</threat_model>

<verification>
`python3 python/analyze.py` (WSL) corre sin error y confirma RHO_COLORS/RHO_MARKERS/plot_S_rho; el barrido de percolacion produce percolation_summary.csv con 20 filas y 0 fallos (o fallos documentados); S_rho.png se genera a partir de esos datos reales; `pdflatex -interaction=nonstopmode -halt-on-error informe.tex` (2 pasadas) compila sin error tras Task 3.
</verification>

<success_criteria>
El informe justifica analiticamente por que S satura en rho=2,4,8 (item 7a) y muestra empiricamente la transicion de percolacion en S(rho) con un barrido real ejecutado en esta maquina, no simulado ni inventado (item 7b) -- cerrando el ultimo pendiente de la tanda de items 1-7.
</success_criteria>

<output>
Create `.planning/quick/260824-i0l-cerrar-pendientes-de-entregables-tp2-inf/260824-i0l-05-SUMMARY.md` when done
</output>
