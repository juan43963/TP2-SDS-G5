---
plan: 260824-i0l-01
status: complete
---

# Summary — Plan 01: Parámetros explícitos + carátula real (informe)

**Tasks:** 2/2 complete

**Commits (merged into main via `merge: quick task 260824-i0l plan 01`):**
- `1b054f7`: docs(260824-i0l-01): agregar parametros explicitos rc/v0/dt en seccion Modelo
- `98b93a5`: docs(260824-i0l-01): agregar caratula real (ITBA, integrantes, fecha de entrega)

## What was built

- `TP2/informe/informe.tex`: sección Modelo ahora declara explícitamente L=10, rc=1, v0=0.03, dt=1. Preámbulo agrega `\usepackage{hyperref}` + `\hypersetup{hidelinks}`; `\title`/`\author`/`\date`/`\maketitle` reemplazados por un `\begin{titlepage}` completo (ITBA, Trabajo Práctico N°2, 72.25 Simulación de Sistemas, Autómatas Celulares, título real, Grupo 5, tres integrantes con emails reales y placeholders `[LEGAJO]` para los dos legajos desconocidos, fecha de entrega 04/09/2026, "2C -- 2025").
- `TP2/informe/informe.pdf`: recompilado, 10 páginas, `pdflatex -halt-on-error` sale con código 0 en dos pasadas.

## Deviations (as reported by the executor)

1. `TP2/data/plots/` estaba vacío en el worktree aislado (dir gitignored) — se copiaron los 16 PNG ya generados desde el checkout principal para poder compilar.
2. `TP2/data/plots/benchmark_tp1_vs_tp2.png` no existe en disco en ningún lado (artefacto efímero gitignored, perdido tras la Fase 05). Recuperado byte-a-byte vía `pdfimages` desde el `informe.pdf` ya comprometido en `c7e12f1` — no es un dato inventado, pero tampoco es una regeneración fresca.
3. Se agregó `\hypersetup{hidelinks}` para quitar los recuadros de color de hyperref alrededor de los `\href{mailto:...}` de la carátula (defecto visual).

Ninguno de los cambios en `TP2/data/` está trackeado en git (gitignored).

**Nota para el usuario:** antes de la entrega real del 04/09/2026, regenerar `TP2/data/plots/` completo (pipeline real de simulación + benchmark) — el `benchmark_tp1_vs_tp2.png` usado acá es una copia recuperada de la figura previa aprobada, no una corrida fresca.
