---
phase: 01-motor-y-grid-persistente
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - TP2/src/include/particle.h
  - TP2/src/include/grid.h
  - TP2/src/methods/cell_index_grid.cpp
  - TP2/src/include/generator.h
  - TP2/src/utils/generator.cpp
  - TP2/src/selftest.cpp
  - TP2/Makefile
  - TP2/.gitignore
autonomous: true
requirements: [ENGINE-01, ENGINE-02]

estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The particle struct exposes orientation (theta) in addition to position (x, y), extending TP1's radius-only Particle."
    - "The persistent Grid reuses its cell and neighbor-list buffers across rebuild() calls instead of reallocating a fresh grid per query, unlike TP1's computeCIM."
    - "A self-test binary (tp2_test) builds independently from TP2/ and make test exits 0."
    - "The new point-distance neighbor predicate does not add particle radii to the interaction reach, unlike TP1's areNeighbors."
  artifacts:
    - path: "TP2/src/include/particle.h"
      provides: "VicsekParticle struct (id, x, y, theta), periodicWrap, periodicDelta, withinRadius, headingToVelocity"
    - path: "TP2/src/include/grid.h"
      provides: "NeighborList alias, maxValidGridM declaration, Grid struct declaration"
    - path: "TP2/src/methods/cell_index_grid.cpp"
      provides: "Grid::rebuild() persistent-buffer implementation adapted from TP1's computeCIM"
    - path: "TP2/src/utils/generator.cpp"
      provides: "generateVicsekParticles(N, L, seed) uniform point-particle generator"
    - path: "TP2/Makefile"
      provides: "tp2 / tp2_test build targets, CORE_SRC source list"
  key_links:
    - from: "TP2/src/methods/cell_index_grid.cpp"
      to: "TP2/src/include/particle.h"
      via: "Grid::rebuild uses withinRadius (no radius term) instead of TP1's areNeighbors"
      pattern: "withinRadius\\("
    - from: "TP2/src/selftest.cpp"
      to: "TP2/src/include/grid.h"
      via: "self-test constructs a Grid and calls rebuild() directly to prove structural correctness and buffer persistence"
      pattern: "Grid\\s+grid|grid\\.rebuild\\("
---

<objective>
Stand up the TP2/ engine skeleton: a point-particle domain model carrying orientation (VicsekParticle), and a persistent-buffer Cell Index Method Grid adapted from TP1's computeCIM — proven correct via a self-test binary that builds and runs independently from TP2/.

Purpose: Every later phase (Vicsek/voter rules in Phase 2, clustering, the parameter sweep) depends on this Grid's neighbor-query contract being correct and its buffers genuinely reused across timesteps (not reallocated per call, per CONCERNS.md's flagged TP1 friction point). Getting this wrong is expensive to discover after a multi-hour sweep has already run on top of it (PITFALLS.md Pitfall 3/4).
Output: TP2/src/include/particle.h, TP2/src/include/grid.h, TP2/src/methods/cell_index_grid.cpp, TP2/src/include/generator.h, TP2/src/utils/generator.cpp, TP2/src/selftest.cpp, TP2/Makefile, TP2/.gitignore — a buildable, testable `tp2_test` binary.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Point-particle domain model + persistent Grid, wired end-to-end through one self-test</name>
  <read_first>
    TP1/src/include/particle.h
    TP1/src/methods/cell_index_method.cpp
    TP1/src/include/neighbor_method.h
    TP1/src/selftest.cpp
    TP1/Makefile
    .planning/research/ARCHITECTURE.md
    .planning/codebase/CONCERNS.md
  </read_first>
  <reversibility rating="costly">The Grid/NeighborList persistent-buffer contract (rebuild() + neighbors() accessor) is the geometric foundation every later phase (Vicsek/voter rules, clustering, the sweep driver) builds on. Getting the contract shape wrong means touching every call site across Phases 2-3 to fix it, though nothing here is an externally published format or on-disk contract, so it is costly, not a migration.</reversibility>
  <files>TP2/src/include/particle.h, TP2/src/include/grid.h, TP2/src/methods/cell_index_grid.cpp, TP2/src/selftest.cpp, TP2/Makefile, TP2/.gitignore</files>
  <action>
    Create TP2/src/include/particle.h (header-only, #pragma once, includes &lt;cmath&gt;) defining: struct VicsekParticle with fields int id; double x, y, theta; (point particle, no radius — per Anti-Pattern 3 in ARCHITECTURE.md, do not reuse TP1's radius-bearing Particle). Add inline double periodicWrap(double coord, double L) implemented as coord - L * std::floor(coord / L) (robust to negative coord, per PITFALLS.md Pitfall 3 — do not use std::fmod). Add inline double periodicDelta(double d, double L) implemented as d - L * std::round(d / L), copied from TP1's particle.h since the minimum-image formula is identical. Add inline bool withinRadius(const VicsekParticle&amp; a, const VicsekParticle&amp; b, double L, double rc, bool periodic) computing dx=a.x-b.x, dy=a.y-b.y, applying periodicDelta to both when periodic, and returning dx*dx+dy*dy &lt; rc*rc — a plain cutoff with no per-particle radius term added to rc, unlike TP1's radius-inclusive predicate in TP1/src/include/particle.h (Anti-Pattern 3 in ARCHITECTURE.md). Add inline void headingToVelocity(double theta, double v0, double&amp; vx, double&amp; vy) setting vx = v0*std::cos(theta), vy = v0*std::sin(theta).

    Create TP2/src/include/grid.h (#pragma once, includes &lt;vector&gt; and "particle.h") declaring: using NeighborList = std::vector&lt;std::vector&lt;int&gt;&gt;;  int maxValidGridM(double L, double rc); (point-particle form of TP1's maxValidM with rMax hardcoded to 0.0 — do not add a radius parameter). Declare struct Grid with public members int M; double L, rc; bool periodic; std::vector&lt;std::vector&lt;int&gt;&gt; cells; NeighborList neighbors_; and: constructor Grid(int M, double L, double rc, bool periodic) that validates (throwing std::invalid_argument with Spanish messages matching TP1's exact wording style: M debe ser >= 1, M supera M_max, L y rc deben ser > 0, con contorno periodico se requiere L/2 > rc) and sizes cells to M*M once; method void rebuild(const std::vector&lt;VicsekParticle&gt;&amp; particles); method const NeighborList&amp; neighbors() const returning neighbors_.

    Create TP2/src/methods/cell_index_grid.cpp implementing maxValidGridM(L, rc) by porting TP1's maxValidM formula verbatim with rMax=0.0 substituted (rcEff=rc; if L&lt;=0 or rcEff&lt;=0 return 1; limit=L/rcEff; m=floor(limit); if m&gt;0 and limit-m&lt;1e-12 then m-=1; return max(1,m)). Implement the Grid constructor validating M against maxValidGridM(L,rc) and the periodic guard L/2 &lt;= rc. Implement Grid::rebuild(particles): for each of the M*M entries in cells, call .clear() (not reassignment — this is the buffer-reuse fix for CONCERNS.md's flagged per-call reallocation) then re-populate by iterating particles and computing each one's cell via the same cellIndex/wrap helpers TP1 uses (floor(coord/cellSize) clamped to [0,M-1]); resize neighbors_ to particles.size() only if its current size differs, otherwise clear() each row in place; then run the identical HALF/FULL stencil sweep from TP1's computeCIM (same HALF[5][2]/FULL[9][2] offset tables, same halfNeighborhood = !periodic || M&gt;=3 logic, same dedup-candidates-then-pairwise-check structure) but call withinRadius(particles[i], particles[j], L, rc, periodic) instead of TP1's radius-inclusive predicate (TP2's VicsekParticle has no radius field, so that TP1 function is structurally inapplicable here), and push into neighbors_[i]/neighbors_[j] instead of a freshly-allocated NeighborList.

    Create TP2/src/selftest.cpp with the same non-fatal check() helper and checks/failures counters as TP1/src/selftest.cpp (copy the pattern, do not import TP1's file). Add one test function testGridStructural() with a hardcoded 4-particle configuration: p0={0,1.0,1.0,0.0}, p1={1,3.0,1.0,0.0}, p2={2,1.0,3.0,0.0}, p3={3,8.0,8.0,0.0} (p3 isolated, far from the others), L=10.0, rc=1.5, M=maxValidGridM(10.0,1.5). Build a Grid, call rebuild() with these 4 particles for periodic=false and periodic=true, and assert: p0 and p1 are mutual neighbors (distance 2.0... note rc=1.5 means p0/p1 at distance 2.0 are NOT neighbors — recompute positions so p0={1,1},p1={2,1} are within rc=1.5, p2={1,2.4} is within rc=1.5 of p0 but not p1, p3={8,8} isolated), no particle is its own neighbor, every edge is symmetric (i in neighbors[j] iff j in neighbors[i]), and p3's neighbor list is empty in both modes. Write a main() that prints "Self-test del motor TP2" then runs testGridStructural(), prints the checks/failures summary exactly like TP1's selftest.cpp, and returns failures==0 ? 0 : 1.

    Create TP2/Makefile mirroring TP1/Makefile exactly in style: CXX ?= c++; CXXFLAGS ?= -std=c++20 -O2 -Wall -Wextra -pedantic -Isrc/include; BUILD := build; SRC := src; CORE_SRC := $(SRC)/methods/cell_index_grid.cpp (only this file for now); CORE_OBJ derived the same way; targets tp2_test (linking CORE_OBJ + build/selftest.o) and a placeholder `all: tp2_test` target (the `tp2` binary target is added in Plan 02 once main.cpp exists — do not create an empty main.cpp here); pattern rule $(BUILD)/%.o: $(SRC)/%.cpp with @mkdir -p $(dir $@); test: tp2_test then ./tp2_test; clean: rm -rf $(BUILD) tp2_test data/*.

    Create TP2/.gitignore listing: build/, tp2, tp2_test, data/.
  </action>
  <verify>
    <automated>cd TP2 && make test 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - TP2/src/include/particle.h contains the exact field declaration "double x, y, theta;" inside VicsekParticle (source assertion — grep -c 'double x, y, theta' TP2/src/include/particle.h returns at least 1).
    - TP2/src/methods/cell_index_grid.cpp contains no call to a function named areNeighbors (grep -c 'areNeighbors' TP2/src/methods/cell_index_grid.cpp returns 0).
    - Running `cd TP2 && make test` exits with status 0 and its output ends with a line matching "0 fallas" or "OK" (mirroring TP1's cim_test summary convention).
    - `git status --porcelain -- TP1/` produces no output (TP1/ untouched by this task).
  </acceptance_criteria>
  <done>TP2/ contains a buildable tp2_test binary; make test passes with zero failures; the 4-particle structural self-test proves the Grid's neighbor query is correct and radius-free.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Point-particle generator + comprehensive Grid validation (N sweep, periodic modes, persistent-buffer proof)</name>
  <read_first>
    TP1/src/utils/generator.cpp
    TP1/src/selftest.cpp
    .planning/research/PITFALLS.md
    TP2/src/include/grid.h
    TP2/src/include/particle.h
  </read_first>
  <files>TP2/src/include/generator.h, TP2/src/utils/generator.cpp, TP2/src/selftest.cpp, TP2/Makefile</files>
  <behavior>
    - generateVicsekParticles(50, 10.0, 42) called twice with the same seed produces bit-identical (x, y, theta) for every particle (determinism, mirroring TP1's testGenerator seed check).
    - generateVicsekParticles(N, L, seed) returns exactly N particles, each with x in [0, L), y in [0, L), theta in [-pi, pi), and id equal to its index.
    - testGridStructural, extended: for N in {10, 100} and periodic in {false, true}, for every valid M in [1, maxValidGridM(L, rc)], Grid::rebuild()'s neighbor list matches an inline brute-force reference (all-pairs withinRadius check) computed independently in the test, and passes the no-self-neighbor / no-duplicate / symmetric structural invariants.
    - testGridPersistentBuffers: calling grid.rebuild() twice in a row with two different particle configurations does not change the memory address of grid.cells.data() (the outer cells container is never reallocated across rebuild() calls).
  </behavior>
  <action>
    Create TP2/src/include/generator.h (#pragma once, includes &lt;vector&gt;, "particle.h") declaring std::vector&lt;VicsekParticle&gt; generateVicsekParticles(int N, double L, unsigned long long seed);. Create TP2/src/utils/generator.cpp implementing it: construct std::mt19937_64 rng(seed) once (never clock-seeded, per PITFALLS.md Pitfall 5), std::uniform_real_distribution&lt;double&gt; posDist(0.0, L) and angleDist(-M_PI, M_PI) (define local constexpr double PI = 3.14159265358979323846 if M_PI is unavailable under -pedantic), loop i from 0 to N-1 assigning particles[i] = {i, posDist(rng), posDist(rng), angleDist(rng)}. No overlap-rejection is needed (unlike TP1's generator) since Vicsek point particles have no collision constraint.

    Extend TP2/src/selftest.cpp: add testGenerator() asserting the determinism property (two calls with seed=42 produce identical x/y/theta arrays) and the containment/id-as-index invariants for N=50, L=10.0, both periodic values not applicable to generation itself (generation is boundary-agnostic; wrapping is Plan 02's concern). Extend testGridStructural() (or add testGridMatchesBruteForce()) with the nested-loop sweep pattern from TP1's testCimMatchesBruteForce: for N in {10, 100}, for periodic in {false, true}, generate particles via generateVicsekParticles(N, 10.0, a fixed seed per case e.g. 7), compute an inline brute-force reference NeighborList by checking withinRadius for every pair, then for every M from 1 to maxValidGridM(10.0, rc) with rc=1.0, build a Grid, call rebuild(), sort both neighbor lists' rows, and assert equality plus the structural invariants (reuse Task 1's checkStructure-style helper). Add testGridPersistentBuffers(): construct one Grid, call rebuild() with a 10-particle set, capture cellsPtr1 = static_cast&lt;const void*&gt;(grid.cells.data()), call rebuild() again with a different 10-particle set (different seed), capture cellsPtr2, and assert cellsPtr1 == cellsPtr2 (the outer cells vector was never reallocated between calls). Register all new test functions in selftest.cpp's main() in the same style as TP1 (one printf line per test phase).

    Extend TP2/Makefile: add $(SRC)/utils/generator.cpp to CORE_SRC.
  </action>
  <verify>
    <automated>cd TP2 && make test 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `cd TP2 && make test` exits 0 with a summary line reporting 0 failures.
    - TP2/src/utils/generator.cpp contains "mt19937_64" (source assertion confirming the RNG idiom, not std::rand).
    - TP2/src/selftest.cpp contains a call to a function whose name contains "PersistentBuffer" (source assertion that the buffer-reuse proof exists).
    - Re-running `cd TP2 && make test` a second time produces byte-identical pass/fail counts (determinism, no flaky RNG-driven false negatives).
  </acceptance_criteria>
  <done>Grid::rebuild() is cross-validated against an independent brute-force reference across N in {10,100}, both boundary modes, and every valid M; the persistent-buffer property is proven via pointer-stability assertion; make test passes with zero failures.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI/self-test numeric input → Grid constructor | Locally-supplied N/M/L/rc values (from selftest.cpp's own hardcoded cases in this plan; CLI flags arrive in Plan 02) could be malformed (negative, zero, huge) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Tampering | Grid constructor (TP2/src/methods/cell_index_grid.cpp) | low | mitigate | Grid constructor validates M>=1, M<=maxValidGridM(L,rc), L>0, rc>0, and the periodic minimum-image guard (L/2>rc), throwing std::invalid_argument exactly like TP1's computeCIM — caught by the top-level try/catch added in Plan 02's main.cpp |
| T-01-02 | Denial of Service | Grid::cells sizing (M*M allocation) | low | accept | This is an offline, single-user, local-only C++ simulation engine with no network exposure, no external input beyond CLI flags (added in Plan 02), and no persisted secrets. A user requesting an extreme M only exhausts their own local process memory; no other user or system is affected, so no ASVS L1-relevant threat beyond the input-validation already covered by T-01-01 exists for this phase. |
</threat_model>

<verification>
`cd TP2 && make test` exits 0 after both tasks. `git status --porcelain -- TP1/` and `git diff --stat -- TP1/` both produce no output, confirming TP1/ was never touched.
</verification>

<success_criteria>
VicsekParticle carries orientation (theta) alongside position; Grid::rebuild() reuses its `cells`/`neighbors_` buffers across calls (proven via pointer-stability test) instead of reallocating per TP1's original per-call pattern; the persistent Grid's neighbor query matches an independent brute-force reference across N∈{10,100}, both boundary modes, and every valid M; TP1/ remains unmodified.
</success_criteria>

<output>
Create `.planning/phases/01-motor-y-grid-persistente/01-01-SUMMARY.md` when done
</output>
