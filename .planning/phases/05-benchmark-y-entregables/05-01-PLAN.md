---
phase: 05-benchmark-y-entregables
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - TP2/python/benchmark.py
  - TP2/data/plots/benchmark_tp1_vs_tp2.png
  - TP2/data/plots/benchmark_timings.csv
  - TP2/.gitignore
autonomous: true
requirements: [BENCH-01]

estimate:
  tokens: 2400
  raw_tokens: 2400
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Existe una medicion real (no estimada ni hardcodeada) de tiempos de TP1 (busqueda de vecinos del CIM, via TP1/python/benchmark.py --study n sin modificar el script) y de TP2 (paso completo del motor, tp2 --steps cronometrado externamente) para el mismo conjunto de N, ambas series en un unico grafico log-log y un unico CSV"
    - "TP2/python/benchmark.py nunca modifica archivos dentro de TP1/ -- solo lo invoca como subprocess y parsea su CSV de salida"
  artifacts:
    - TP2/python/benchmark.py
    - TP2/data/plots/benchmark_tp1_vs_tp2.png
    - TP2/data/plots/benchmark_timings.csv
  key_links:
    - "TP2/python/benchmark.py invoca `python3 TP1/python/benchmark.py --study n` (cwd=TP1/, sin editar el script) y parsea TP1/data/bench_punto4.csv filtrando study=='punto4.1' para los tiempos de TP1"
    - "TP2/python/benchmark.py invoca TP2/tp2 --N <N> --L 10 --rc 1 --steps <STEPS_BENCH> --seed <SEED_BENCH> --out /dev/null, cronometrado con time.perf_counter() alrededor del subprocess, para los tiempos de TP2"
---

<objective>
Medir y comparar los tiempos de ejecucion del CIM de TP1 (busqueda de vecinos pura, via `TP1/python/benchmark.py --study n` sin modificarlo) contra los tiempos de TP2 (paso completo del motor: rebuild de grilla CIM + busqueda de vecinos + integracion, cronometrado externamente sobre el binario `tp2` real) para el mismo conjunto de N que usa TP1 por defecto, satisfaciendo BENCH-01 y el punto (g) del enunciado.

Purpose: `docs/TP2_Enunciado.md` punto (g) exige "tomar algunas simulaciones que tengan un numero de particulas similar a las estudiadas en el TP1 y registrar los tiempos de ejecucion del CIM. Luego compararlas con los tiempos obtenidos en el TP1." No hay datos historicos de TP1 persistidos en el repo (`TP1/data/` esta gitignoreado), asi que ambos lados se miden frescos en esta corrida, per `05-CONTEXT.md`.
Output: `TP2/python/benchmark.py` (nuevo), `TP2/data/plots/benchmark_tp1_vs_tp2.png`, `TP2/data/plots/benchmark_timings.csv` -- consumidos directamente por los Planes 05-02 (informe) y 05-03 (presentacion) para el punto (g).

Reversibilidad: todo lo que produce este plan es regenerable (rerun de `python3 python/benchmark.py`); no hay decisiones de una via -- no se requieren checkpoints de decision.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-benchmark-y-entregables/05-CONTEXT.md
@TP1/python/benchmark.py
@TP2/python/sweep.py
@TP2/src/main.cpp

**Nota de ejecucion (worktree):** `TP2/data/` y `TP1/data/` estan gitignoreados. Este plan solo necesita `TP1/cim` y `TP2/tp2` ya compilados (no depende de ningun dato de Fase 3/4). Si corre en un worktree aislado sin esos binarios, el Task 1 debe compilarlos primero (`make` en `TP1/` y en `TP2/`, via WSL). Los Planes 05-02/05-03/05-04 de la Wave 2 SI dependen de que `TP2/data/plots/benchmark_tp1_vs_tp2.png`/`benchmark_timings.csv` (este plan) y los 16 PNG + 1 CSV de Fase 4 ya existan en disco, y todos esos archivos estan gitignoreados -- por eso se recomienda ejecutar la Fase 5 completa en el checkout principal (sin aislamiento por worktree por plan), evitando recompilar TP1/TP2 y re-correr el barrido completo de Fase 3/4 (~15-35 min por plan) en cada worktree nuevo.
</context>

## Artifacts this phase produces

- `TP2/python/benchmark.py` -- nuevo script: `run_tp1_timings()`, `run_tp2_timings()`, `merge_and_save_csv()`, `plot_benchmark()`, `main()`
- `TP2/data/plots/benchmark_tp1_vs_tp2.png` -- grafico log-log comparativo TP1 (busqueda CIM) vs TP2 (paso completo)
- `TP2/data/plots/benchmark_timings.csv` -- columnas `N,tp1_search_mean_ms,tp1_search_std_ms,tp2_step_mean_ms,tp2_step_std_ms`
- `TP2/.gitignore` -- actualizado con patrones de archivos auxiliares de LaTeX (`informe/*.aux` etc.) anticipando la Wave 2

<tasks>

<task type="tracer">
  <name>Task 1: Pipeline de benchmark end-to-end para un solo N (TP1 CIM vs TP2 paso completo)</name>
  <files>TP2/python/benchmark.py</files>
  <precondition>TP1/cim y TP2/tp2 existen (compilar con `make` en TP1/ y TP2/ via WSL si faltan); TP1/python/benchmark.py no se modifica en ningun momento.</precondition>
  <read_first>TP1/python/benchmark.py (formato CSV `FIELDS`, funcion `run()`, `--study n`/`study_n()`, columna `study` con valores `punto4.1`/`punto4.1_brute`/`punto4.2`, default `--n-sweep`); TP2/src/main.cpp (flags `--N`/`--L`/`--rc`/`--steps`/`--seed`/`--out`, valores default); TP2/python/sweep.py (convencion `subprocess.run` con lista de args, nunca `shell=True`; patron `TP2_DIR = Path(__file__).resolve().parent.parent`; `DISCARD_OUT_PATH = os.devnull`)</read_first>
  <action>Crear TP2/python/benchmark.py siguiendo la convencion de entrypoint unico de sweep.py/analyze.py (matplotlib.use("Agg") salvo "--show" en sys.argv, constantes SCREAMING_SNAKE_CASE a nivel de modulo). Definir TP2_DIR, REPO_ROOT = TP2_DIR.parent, TP1_DIR = REPO_ROOT / "TP1", TP1_BENCHMARK_PY = TP1_DIR / "python" / "benchmark.py", TP1_BENCH_CSV = TP1_DIR / "data" / "bench_punto4.csv", TP1_BIN = TP1_DIR / "cim", TP2_BIN = TP2_DIR / "tp2", PLOTS_DIR = TP2_DIR / "data" / "plots". Definir N_SWEEP = [10, 25, 50, 100, 200, 300, 400, 500, 600, 700, 850, 1000] con un comentario explicito de que es una copia intencional del default --n-sweep de TP1/python/benchmark.py (no se importa TP1 -- PROJECT.md prohibe libreria compartida entre TP1 y TP2, decision ya tomada). Definir L_BENCH=10.0, RC_BENCH=1.0, STEPS_BENCH=200, REPEAT_BENCH=5, SEED_BENCH=1. Implementar run_tp1_timings(n_values, repeat=100): valida que TP1_BIN existe (sys.exit con mensaje "correr make en TP1/" si no), invoca subprocess.run([sys.executable, str(TP1_BENCHMARK_PY), "--study", "n", "--n-sweep", *(str(n) for n in n_values), "--repeat", str(repeat)], cwd=TP1_DIR, capture_output=True, text=True), valida returncode==0 (RuntimeError con proc.stderr si no), parsea TP1_BENCH_CSV via csv.DictReader, filtra filas donde row["study"]=="punto4.1" y int(row["N"]) esta en n_values, devuelve lista de dicts {N:int, mean_ms:float, std_ms:float} ordenada por N. Implementar run_tp2_timings(n_values, steps=STEPS_BENCH, repeat=REPEAT_BENCH): valida que TP2_BIN existe (sys.exit si no), para cada N corre `repeat` veces subprocess.run([str(TP2_BIN), "--N", str(N), "--L", f"{L_BENCH:.10g}", "--rc", f"{RC_BENCH:.10g}", "--steps", str(steps), "--seed", str(SEED_BENCH), "--out", os.devnull], capture_output=True, text=True) cronometrado con time.perf_counter() antes/despues, valida returncode==0 en cada corrida, calcula per_step_ms = (elapsed_s*1000)/steps por repeticion, agrega mean_ms/std_ms con statistics.mean/statistics.stdev (0.0 si repeat es 1), devuelve lista de dicts {N:int, mean_ms:float, std_ms:float}. Implementar merge_and_save_csv(tp1_rows, tp2_rows, out_path=PLOTS_DIR/"benchmark_timings.csv"): join por N (KeyError explicito si algun N no coincide entre ambas listas), escribe columnas N,tp1_search_mean_ms,tp1_search_std_ms,tp2_step_mean_ms,tp2_step_std_ms via csv.DictWriter (misma convencion que sweep.py::aggregate_to_csv), devuelve la lista combinada. Implementar plot_benchmark(rows, out_path=PLOTS_DIR/"benchmark_tp1_vs_tp2.png"): ax.set_xscale("log"), ax.set_yscale("log"), dos series con ax.errorbar(N, tp1_search_mean_ms, yerr=tp1_search_std_ms, label="TP1: busqueda de vecinos (CIM)") y ax.errorbar(N, tp2_step_mean_ms, yerr=tp2_step_std_ms, label="TP2: paso completo (rebuild grilla + busqueda + integracion)") -- las etiquetas deben dejar explicito que son magnitudes distintas, nunca presentarlas como la misma operacion. Guardar con fig.savefig(out_path, dpi=150, bbox_inches="tight"). Agregar main() con argparse: --n-values (nargs="+", type=int, default=N_SWEEP), --repeat-tp1 (default 100), --repeat-tp2 (default REPEAT_BENCH), --steps-tp2 (default STEPS_BENCH) -- ejecuta run_tp1_timings, run_tp2_timings, merge_and_save_csv, plot_benchmark en secuencia y printea un resumen. Para este Task, ejecutar el pipeline completo con `--n-values 100 --repeat-tp1 20 --repeat-tp2 3` via WSL para probar las 4 capas (subprocess TP1 + parseo CSV, subprocess TP2 + cronometraje, merge/CSV, plot) end-to-end antes de escalar al N-sweep completo en el Task 2.</action>
  <verify>
    <automated>wsl.exe -- bash -lc "cd '/mnt/c/Users/Lucas Di Candia/Desktop/SDS/TP2-SDS-G5/TP2' && python3 python/benchmark.py --n-values 100 --repeat-tp1 20 --repeat-tp2 3 && test -s data/plots/benchmark_timings.csv && test -s data/plots/benchmark_tp1_vs_tp2.png && python3 -c \"import csv; rows=list(csv.DictReader(open('data/plots/benchmark_timings.csv'))); assert len(rows)==1 and rows[0]['N']=='100' and float(rows[0]['tp1_search_mean_ms'])>0 and float(rows[0]['tp2_step_mean_ms'])>0; print('TRACER_OK')\""</automated>
  </verify>
  <acceptance_criteria>El comando de verificacion imprime TRACER_OK; benchmark_timings.csv tiene exactamente 1 fila de datos para N=100 con tp1_search_mean_ms y tp2_step_mean_ms ambos > 0 y no identicos entre si (son magnitudes distintas); benchmark_tp1_vs_tp2.png existe y pesa mas de 1000 bytes; `git status --short -- TP1/` queda vacio (TP1 nunca se modifica).</acceptance_criteria>
  <done>El pipeline completo (subprocess TP1 -> parseo CSV -> subprocess TP2 cronometrado -> merge CSV -> plot log-log) corre end-to-end para un solo N y produce ambos artefactos finales, probando que las 4 capas funcionan antes de escalar al sweep completo.</done>
</task>

<task type="auto">
  <name>Task 2: Escalar al N-sweep completo + hygiene de .gitignore para la Wave 2</name>
  <files>TP2/python/benchmark.py, TP2/data/plots/benchmark_tp1_vs_tp2.png, TP2/data/plots/benchmark_timings.csv, TP2/.gitignore</files>
  <read_first>TP2/.gitignore (contenido actual: build/, tp2, tp2_test, data/, __pycache__/)</read_first>
  <action>Correr `python3 python/benchmark.py` (sin overrides -- usa N_SWEEP completo de 12 valores, --repeat-tp1 default 100 igual al default de TP1/python/benchmark.py, --repeat-tp2 default REPEAT_BENCH=5) via WSL, regenerando benchmark_timings.csv (12 filas) y benchmark_tp1_vs_tp2.png con el rango completo. Agregar a TP2/.gitignore las lineas `informe/*.aux`, `informe/*.log`, `informe/*.out`, `informe/*.toc`, `presentacion/*.aux`, `presentacion/*.log`, `presentacion/*.out`, `presentacion/*.nav`, `presentacion/*.snm`, `presentacion/*.toc`, `presentacion/*.vrb` -- anticipando los artefactos intermedios de pdflatex que crearan los Planes 05-02/05-03 en la Wave 2 (evita que ambos planes tengan que tocar TP2/.gitignore en paralelo y generen un conflicto de wave por archivo compartido).</action>
  <verify>
    <automated>wsl.exe -- bash -lc "cd '/mnt/c/Users/Lucas Di Candia/Desktop/SDS/TP2-SDS-G5/TP2' && python3 python/benchmark.py && python3 -c \"import csv; rows=list(csv.DictReader(open('data/plots/benchmark_timings.csv'))); ns=sorted(int(r['N']) for r in rows); assert ns==[10,25,50,100,200,300,400,500,600,700,850,1000], ns; assert all(float(r['tp1_search_mean_ms'])>0 and float(r['tp2_step_mean_ms'])>0 for r in rows); print('FULL_SWEEP_OK', len(rows))\""</automated>
  </verify>
  <acceptance_criteria>FULL_SWEEP_OK 12 se imprime; benchmark_timings.csv tiene exactamente las 12 filas de N_SWEEP y ningun valor de tiempo es 0 o negativo; benchmark_tp1_vs_tp2.png se regenero; TP2/.gitignore contiene las 11 lineas nuevas de patrones LaTeX sin duplicar las lineas existentes.</acceptance_criteria>
  <done>benchmark_timings.csv y benchmark_tp1_vs_tp2.png cubren los 12 valores de N del sweep por defecto de TP1, listos para ser referenciados por los Planes 05-02 y 05-03; TP2/.gitignore preparado para los artefactos LaTeX de la Wave 2.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Python driver -> subprocess (TP1/python/benchmark.py, TP1/cim, TP2/tp2) | benchmark.py spawns external processes with argument lists built entirely from internal constants (N, L, rc, steps, seed) |
| Filesystem -> CSV parse (TP1/data/bench_punto4.csv) | benchmark.py reads a CSV produced by a subprocess it just launched |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Tampering | subprocess.run arg construction | low | mitigate | All subprocess calls use list-of-args (never shell=True / string concatenation), matching the established convention in TP2/python/sweep.py -- no shell metacharacter injection surface |
| T-05-02 | Information Disclosure | TP1_BENCH_CSV parse | low | mitigate | Validate returncode==0 and that filtered "punto4.1" rows cover the exact requested N set before trusting the numbers; raise RuntimeError with proc.stderr on failure instead of silently plotting stale/partial data |
| T-05-03 | Denial of Service | TP2 wall-clock subprocess loop | low | accept | REPEAT_BENCH=5 x 12 N values x tp2 process spawn is bounded and local-only (no network), acceptable run time (order of minutes) |

</threat_model>

<verification>
`python3 python/benchmark.py` (WSL, cwd=TP2/) regenerates both benchmark_timings.csv (12 rows) and benchmark_tp1_vs_tp2.png idempotently; `git status --short -- TP1/` stays empty after every run.
</verification>

<success_criteria>
BENCH-01 satisfied: real (not estimated) TP1 CIM search timings and TP2 full-step timings exist for the same 12 N values, plotted log-log with error bars and captured in a CSV, ready for Plan 05-02/05-03 to reference for the assignment's point (g).
</success_criteria>

<output>
Create `.planning/phases/05-benchmark-y-entregables/05-01-SUMMARY.md` when done
</output>
