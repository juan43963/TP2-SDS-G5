---
phase: 05-benchmark-y-entregables
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - TP2/informe/informe.tex
  - TP2/informe/informe.pdf
autonomous: true
requirements: [DELIV-01]

estimate:
  tokens: 4800
  raw_tokens: 4800
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "informe.pdf compila sin errores (pdflatex exit 0) y contiene las 6 secciones numeradas requeridas (Introduccion, Modelo, Implementacion, Simulaciones, Resultados, Conclusiones) mas una seccion final 'Referencias' sin numerar"
    - "Cada figura del informe esta numerada, referenciada en el texto ('En la Fig. N...') y acompanada de texto analitico -- nunca una seccion con figuras sueltas"
    - "La notacion matematica sigue docs/GuiaInformes.md: escalares en italica sin negrita, vectores en negrita sin italica (via \\vect{}), unidades y numeros sin negrita ni italica"
  artifacts:
    - TP2/informe/informe.tex
    - TP2/informe/informe.pdf
  key_links:
    - "informe.tex referencia TP2/data/plots/*.png (incluyendo benchmark_tp1_vs_tp2.png del Plan 05-01) con rutas relativas via \\graphicspath, nunca rutas absolutas de Windows"
    - "La tabla eta_c(rho) del informe transcribe los valores reales de TP2/data/plots/eta_c_table.csv, nunca numeros inventados"
---

<objective>
Escribir `TP2/informe/informe.tex` (LaTeX, compilado con pdflatex/MiKTeX) siguiendo la estructura de secciones y la convencion de notacion matematica de `docs/GuiaInformes.md`, reusando directamente todas las figuras ya generadas por las Fases 3/4 mas el grafico de benchmark del Plan 05-01, satisfaciendo DELIV-01.

Purpose: El enunciado (`docs/TP2_Enunciado.md`) exige "un informe con las mismas secciones que la presentacion y teniendo en cuenta el formato indicado en .../Formato_Informes.pdf" -- documento independiente y autocontenido, con hilo argumental en cada seccion (nunca figuras sueltas).
Output: `TP2/informe/informe.tex`, `TP2/informe/informe.pdf`.

Reversibilidad: el .tex/.pdf son regenerables por rerun de pdflatex; no hay decisiones de una via -- no se requieren checkpoints.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-benchmark-y-entregables/05-CONTEXT.md
@docs/GuiaInformes.md
@docs/TP2_Enunciado.md

**Nota de ejecucion (worktree):** este plan lee TP2/data/plots/*.png y eta_c_table.csv generados por la Fase 4, y TP2/data/plots/benchmark_tp1_vs_tp2.png / benchmark_timings.csv generados por el Plan 05-01 (Wave 1). Si esos archivos no existen en el checkout donde corre este plan (worktree aislado sin ese estado), regenerarlos primero: compilar TP1/TP2 (`make` via WSL), correr `python3 python/sweep.py && python3 python/analyze.py && python3 python/animate.py && python3 python/benchmark.py` en TP2/ via WSL. Se recomienda ejecutar la Fase 5 en el checkout principal (sin aislamiento por worktree) para evitar este costo repetido en cada plan de la Wave 2 (ver nota identica en 05-01-PLAN.md).

**pdflatex/MiKTeX:** confirmado disponible directamente en el shell nativo de Windows (Git Bash) sin pasar por WSL -- `pdflatex --version` funciona. Si una compilacion falla con `! LaTeX Error: File `X.sty' not found.`, correr `miktex packages install X` (confirmado que funciona sin admin ni prompts en este entorno) y recompilar.
</context>

## Artifacts this phase produces

- `TP2/informe/informe.tex` -- fuente LaTeX del informe (article, mathptmx, amsmath/amssymb/graphicx/booktabs)
- `TP2/informe/informe.pdf` -- PDF compilado, entregable DELIV-01

<tasks>

<task type="tracer">
  <name>Task 1: Skeleton LaTeX + seccion Introduccion, compilado end-to-end</name>
  <files>TP2/informe/informe.tex</files>
  <precondition>pdflatex (MiKTeX) esta disponible en el shell nativo de Windows (confirmado: `pdflatex --version`); no depende de TP1/TP2 compilados, solo lee figuras ya generadas.</precondition>
  <read_first>docs/GuiaInformes.md (estructura de secciones: Introduccion/Modelo/Implementacion/Simulaciones/Resultados/Conclusiones; convencion de figuras/ecuaciones numeradas y referenciadas "En la Fig.1..."; notacion Times New Roman italica/negrita/unidades; seccion Referencias sin numerar al final); docs/TP2_Enunciado.md (descripcion del sistema, modelos, puntos a-g requeridos, referencias [1]/[2]); .planning/PROJECT.md (seccion "What This Is", parametros L=10, rho, eta, modelos vicsek/voter)</read_first>
  <action>Crear TP2/informe/informe.tex: \documentclass[11pt]{article}, \usepackage[utf8]{inputenc}, \usepackage[T1]{fontenc}, \usepackage[a4paper,margin=2.5cm]{geometry}, \usepackage{amsmath,amssymb,graphicx,booktabs}, \usepackage{mathptmx} (fuente Times New Roman, per la notacion exigida por GuiaInformes.md), \graphicspath{{../data/plots/}} (ruta relativa desde TP2/informe/ hacia TP2/data/plots/), \newcommand{\vect}[1]{\mathbf{#1}} para vectores en negrita sin italica (ej. \vect{r}_i(t)) -- los escalares quedan en modo matematico default (italica automatica de LaTeX) y las unidades siempre en texto fuera de modo matematico o via \mathrm{} dentro de el (nunca italica, ya que las letras de unidad como "m"/"s" salen italicas por defecto en modo matematico). Titulo: "Simulacion de Bandadas: Modelo de Vicsek y Modelo de Votante", autor "Grupo 5 -- Simulacion de Sistemas", \date{} con la fecha actual. Numerar secciones con \section{}. Escribir la seccion 1 "Introduccion" (numerada) con texto tecnico real (no placeholder) describiendo el sistema real (bandadas de agentes autopropulsados en una caja periodica), el problema de estudio (transicion orden-desorden en funcion del ruido angular eta para dos reglas de interaccion, Vicsek y votante) y el objetivo del trabajo -- basado en docs/TP2_Enunciado.md, lenguaje tecnico sin coloquialismos. Si pdflatex falla con "File `X.sty' not found", correr `miktex packages install X` y recompilar.</action>
  <verify>
    <automated>cd TP2/informe && pdflatex -interaction=nonstopmode -halt-on-error informe.tex > pdflatex_task1.log 2>&1; EXIT=$?; [ "$EXIT" -eq 0 ] && test -s informe.pdf && grep -qc "section{Introduccion}" informe.tex && echo TRACER_PDF_OK || (echo "COMPILE_FAILED EXIT=$EXIT"; exit 1)
  </verify>
  <acceptance_criteria>pdflatex termina con exit 0 (verificable en pdflatex_task1.log, sin "Fatal error" ni "! LaTeX Error"); informe.pdf existe y pesa mas de 5000 bytes (no vacio); informe.tex contiene \section{Introduccion} con al menos 3 oraciones de texto tecnico real (sin "TODO"/"lorem ipsum"/placeholder).</acceptance_criteria>
  <done>El toolchain LaTeX completo (documentclass, fuentes Times, paquetes, geometria) esta probado end-to-end con una seccion real compilando a PDF, antes de escribir el resto del informe.</done>
</task>

<task type="auto">
  <name>Task 2: Secciones Modelo, Implementacion, Simulaciones</name>
  <files>TP2/informe/informe.tex</files>
  <read_first>docs/TP2_Enunciado.md (seccion "Modelo de votante", referencias [1]/[2]); TP2/src/engine/simulation.h, TP2/src/include/particle.h, TP2/src/include/grid.h, TP2/src/include/observables.h (para describir fielmente la arquitectura del motor sin inventar clases/metodos que no existen); TP2/python/sweep.py (K_SEEDS, DEFAULT_STEPS, STEADY_STATE_FRACTION, grilla de eta gruesa+fina); docs/GuiaInformes.md (notacion matematica)</read_first>
  <action>Agregar \section{Modelo}: ecuaciones del modelo estandar de Vicsek (posicion \vect{r}_i(t+1) = \vect{r}_i(t) + v_0\hat{\vect{v}}_i(t+1)\Delta t con wrap periodico; angulo theta_i(t+1) = promedio circular de las orientaciones de los vecinos dentro de rc (via atan2 de la suma de senos/cosenos) mas ruido angular uniforme en [-eta/2, eta/2]) y del modelo de votante (theta_i(t+1) = theta_j(t) para un vecino j elegido al azar dentro de rc, mas el mismo ruido) -- usar \vect{} para vectores, escalares (eta, rc, v0, N, L, rho) en italica de modo matematico por defecto, citando [1] Vicsek et al. 1995 y [2] Loscar/Baglietto/Vazquez 2021 con notacion numerada manual [1]/[2] (sin bibtex). Agregar \section{Implementacion}: arquitectura del motor C++ (Grid persistente con buffers reutilizados en rebuild(), Simulation con loop sincronico double-buffered, estrategia vicsekRule/voterRule seleccionable por flag) -- basada estrictamente en los headers reales leidos. Agregar \section{Simulaciones}: geometria (L=10 fijo, condiciones periodicas), rango de parametros (rho en {2,4,8}, grilla de eta gruesa (9 puntos en [0,2*pi]) + fina (8 puntos) dentro del bracket de transicion detectado por exploracion), K>=5 semillas independientes por punto, criterio de estado estacionario (descarta el primer 50% de los pasos como transitorio, aplicado identicamente a va y S), pasos por corrida del barrido completo (DEFAULT_STEPS=2000). Las tres secciones son texto con hilo argumental, sin figuras nuevas todavia (esas van en Resultados). Recompilar con pdflatex -interaction=nonstopmode -halt-on-error dos veces para resolver referencias cruzadas.</action>
  <verify>
    <automated>cd TP2/informe && pdflatex -interaction=nonstopmode -halt-on-error informe.tex > pdflatex_task2_pass1.log 2>&1; EXIT1=$?; pdflatex -interaction=nonstopmode -halt-on-error informe.tex > pdflatex_task2_pass2.log 2>&1; EXIT2=$?; [ "$EXIT1" -eq 0 ] && [ "$EXIT2" -eq 0 ] && test -s informe.pdf && grep -qc "section{Modelo}" informe.tex && grep -qc "section{Implementacion}" informe.tex && grep -qc "section{Simulaciones}" informe.tex && echo SECTIONS_OK || (echo "COMPILE_FAILED EXIT1=$EXIT1 EXIT2=$EXIT2"; exit 1)
  </verify>
  <acceptance_criteria>Ambas pasadas de pdflatex terminan con exit 0; informe.tex contiene exactamente una vez cada uno de \section{Modelo}, \section{Implementacion}, \section{Simulaciones}; Modelo incluye al menos una ecuacion de Vicsek y una del votante en modo matematico con \vect{} usado para al menos un vector; Simulaciones menciona explicitamente rho en {2,4,8}, L=10, y el criterio de estado estacionario (50% descartado).</acceptance_criteria>
  <done>Las tres secciones intermedias (Modelo, Implementacion, Simulaciones) tienen contenido tecnico real derivado del codigo/driver reales, sin placeholders, y el documento sigue compilando.</done>
</task>

<task type="auto">
  <name>Task 3: Resultados (todas las figuras + tabla eta_c + benchmark) + Conclusiones + Referencias</name>
  <files>TP2/informe/informe.tex, TP2/informe/informe.pdf</files>
  <read_first>TP2/data/plots/va_eta.png, S_eta.png, va_vs_S.png, chi_eta.png, eta_c_table.csv (leer el CSV real y transcribir sus valores a una tabla LaTeX -- nunca inventar numeros), va_t_vicsek_rho2.png, va_t_voter_rho2.png, S_t_vicsek_rho2.png, S_t_voter_rho2.png, benchmark_tp1_vs_tp2.png y benchmark_timings.csv (del Plan 05-01); docs/GuiaInformes.md (figuras numeradas y referenciadas "En la Fig.N...", nunca figuras sueltas)</read_first>
  <action>Agregar \section{Resultados} con, en orden, cada figura en un entorno figure con \caption numerada, \label, y texto analitico antes/despues referenciandola como "En la Fig.~\ref{fig:...}...": (1) va(eta) -- va_eta.png, describiendo el cruce orden-desorden para las 3 densidades con ambos modelos superpuestos; (2) S(eta) -- S_eta.png, mismo patron para la fraccion del cluster gigante; (3) va vs S -- va_vs_S.png, distinguiendo las 3 densidades; (4) evolucion temporal representativa de va -- va_t_vicsek_rho2.png y va_t_voter_rho2.png, explicando el criterio de estado estacionario y la linea vertical; (5) evolucion temporal representativa de S -- S_t_vicsek_rho2.png y S_t_voter_rho2.png; (6) susceptibilidad chi(eta) -- chi_eta.png (diferencial PLUS-01); (7) tabla eta_c(rho) -- leer TP2/data/plots/eta_c_table.csv y transcribir sus valores reales a una tabla LaTeX (booktabs), una fila por (modelo,rho), columnas modelo/rho/eta_c (diferencial PLUS-03); (8) benchmark CIM -- benchmark_tp1_vs_tp2.png (del Plan 05-01), con texto explicito aclarando que la serie TP1 mide solo busqueda de vecinos y la serie TP2 mide el paso completo del motor (nunca presentarlas como la misma magnitud), satisfaciendo el punto (g) del enunciado (BENCH-01). Agregar \section{Conclusiones} (sin subsecciones) con conclusiones basadas UNICAMENTE en los resultados mostrados arriba (transicion orden-desorden observada, diferencias vicsek vs votante, formacion de clusters, costo computacional del CIM). Agregar la seccion final sin numerar "Referencias" (\section*{Referencias}) listando exactamente las 2 referencias citadas en el cuerpo (Vicsek et al. 1995; Loscar, Baglietto & Vazquez 2021) en formato "[1] Autor, "Titulo", Revista, vol., nro., pp. (year)." -- ninguna referencia adicional no citada en el texto. Recompilar dos veces con pdflatex -interaction=nonstopmode -halt-on-error.</action>
  <verify>
    <automated>cd TP2/informe && pdflatex -interaction=nonstopmode -halt-on-error informe.tex > pdflatex_task3_pass1.log 2>&1; EXIT1=$?; pdflatex -interaction=nonstopmode -halt-on-error informe.tex > pdflatex_task3_pass2.log 2>&1; EXIT2=$?; [ "$EXIT1" -eq 0 ] && [ "$EXIT2" -eq 0 ] && test -s informe.pdf && python3 -c "t=open('informe.tex',encoding='utf-8').read(); n=t.count('\\includegraphics'); assert n>=8, n; assert 'section{Resultados}' in t; assert 'section{Conclusiones}' in t; assert 'section*{Referencias}' in t; print('RESULTADOS_OK', n)" || (echo "COMPILE_FAILED EXIT1=$EXIT1 EXIT2=$EXIT2"; exit 1)
  </verify>
  <acceptance_criteria>Ambas pasadas de pdflatex terminan con exit 0 y la ultima pasada no reporta "Rerun to get" (referencias resueltas); informe.tex tiene al menos 8 \includegraphics referenciando archivos reales via \graphicspath (rutas relativas, nunca absolutas de Windows); la seccion Referencias lista exactamente 2 entradas y ambas aparecen citadas en el cuerpo del texto (sin referencias huerfanas); informe.pdf pesa mas de 200000 bytes (multiples imagenes PNG embebidas).</acceptance_criteria>
  <done>informe.pdf compila limpio con las 6 secciones numeradas (Introduccion, Modelo, Implementacion, Simulaciones, Resultados, Conclusiones) mas Referencias sin numerar, todas las figuras requeridas del enunciado (a-g) presentes con caption/referencia/hilo argumental, y la comparacion estandar vs votante visible en las figuras superpuestas.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LaTeX source -> pdflatex compiler | informe.tex is authored entirely by the executor from known local figure paths and internal prose -- no untrusted external input |
| Compiled PDF -> campus submission | informe.pdf is the actual artifact submitted to the course, must not leak local machine details |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-04 | Information Disclosure | \includegraphics figure paths | low | mitigate | \graphicspath set to a relative path from TP2/informe/, all \includegraphics use bare filenames -- no absolute Windows paths (containing the developer's local username) ever appear in informe.tex or get embedded in the compiled PDF |
| T-05-05 | Tampering | eta_c_table.csv -> LaTeX table transcription | low | mitigate | Values are read directly from the real TP2/data/plots/eta_c_table.csv at write time, never hand-typed from memory/guess -- avoids silently reporting stale or fabricated numbers in a graded deliverable |

</threat_model>

<verification>
`pdflatex -interaction=nonstopmode -halt-on-error informe.tex` (run twice, from TP2/informe/) exits 0 both times with no unresolved-reference warning on the final pass; informe.pdf contains all required figures per docs/TP2_Enunciado.md points (a)-(g).
</verification>

<success_criteria>
DELIV-01 satisfied: informe.pdf follows docs/GuiaInformes.md's structure and notation, includes every required plot (including the new benchmark comparison from 05-01) with numbered/referenced captions and analytical prose, and cites exactly the 2 assignment references.
</success_criteria>

<output>
Create `.planning/phases/05-benchmark-y-entregables/05-02-SUMMARY.md` when done
</output>
