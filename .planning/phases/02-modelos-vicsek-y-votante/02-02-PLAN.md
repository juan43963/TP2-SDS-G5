---
phase: 02-modelos-vicsek-y-votante
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - TP2/src/engine/simulation.h
  - TP2/src/include/observables.h
  - TP2/src/utils/observables.cpp
  - TP2/src/include/io.h
  - TP2/src/utils/io.cpp
  - TP2/src/main.cpp
  - TP2/src/selftest.cpp
  - TP2/Makefile
autonomous: true
requirements: [OUTPUT-01, CLUSTER-01, CLUSTER-02]

estimate:
  tokens: 46000
  raw_tokens: 46000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The dynamic trajectory output file contains real vx,vy per particle per timestep (headingToVelocity of the committed theta, not TP1's hardcoded '0 0'), appended across the whole run (steps*(1+N) lines for a steps-step run of N particles, never truncated to the last frame)."
    - "A self-test proves giantComponentFraction over a small deterministic 4-particle configuration (3 mutually-reachable particles + 1 isolated) returns exactly 0.75, matching visual inspection of the giant component."
    - "A self-test proves polarization() rises above 0.5 over a short low-noise validation run for BOTH the Vicsek and voter models (phase success criterion 1), reusing Plan 02-01's model selection."
    - "Cluster detection reuses Grid::neighbors() (via Simulation::neighbors()) directly — no second neighbor search, no independent rc, computed in the same BFS/connected-components pass as CLUSTER-01 requires."
  artifacts:
    - path: "TP2/src/include/observables.h"
      provides: "polarization(particles) and giantComponentFraction(neighbors) declarations"
    - path: "TP2/src/utils/observables.cpp"
      provides: "polarization: mean-resultant-vector magnitude sqrt((Σsin/N)^2+(Σcos/N)^2), 0.0 for N=0; giantComponentFraction: iterative-stack connected-components over the existing NeighborList, largest/n, 0.0 for n=0"
    - path: "TP2/src/include/io.h"
      provides: "writeTrajectoryFrame(ofstream&, particles, t, v0) declaration — takes an already-open stream, never a path"
    - path: "TP2/src/utils/io.cpp"
      provides: "writeTrajectoryFrame implementation: t header line then one 'x y vx vy' line per particle with real velocities from headingToVelocity"
    - path: "TP2/src/main.cpp"
      provides: "--out <path> CLI flag (default data/dynamic.txt); trajectory ofstream opened once before the step loop; writeTrajectoryFrame called once per step; final report line extended with va/S from polarization()/giantComponentFraction()"
  key_links:
    - from: "TP2/src/main.cpp"
      to: "TP2/src/utils/io.cpp"
      via: "the step loop calls writeTrajectoryFrame(trajOut, sim.particles(), t, o.v0) once per step, reusing one already-open ofstream opened before the loop"
      pattern: "writeTrajectoryFrame\\("
    - from: "TP2/src/main.cpp"
      to: "TP2/src/utils/observables.cpp"
      via: "after the step loop, the report line computes S via giantComponentFraction(sim.neighbors()), reusing the engine's own adjacency rather than recomputing it"
      pattern: "giantComponentFraction\\(sim\\.neighbors"
---

<objective>
Give the engine real, decoupled trajectory output and the two observables the phase's success criteria depend on: a `writeTrajectoryFrame` writer that replaces TP1's hardcoded `"0 0"` velocity placeholder with real `vx,vy` computed from each particle's committed heading, opened once and appended per timestep (not truncated per call), plus a `giantComponentFraction`/`polarization` pair that reuses the engine's own `Grid::neighbors()` adjacency — no second neighbor search, no independent `rc`.

Purpose: Closes OUTPUT-01, CLUSTER-01, CLUSTER-02, and delivers the phase's success criterion 1 (a validation run showing va(t) rising for both models) using the model selection built in Plan 02-01. The single biggest risk this plan guards against is TP1's proven truncation bug (`writeDynamic`'s single-shot `std::ofstream(path)` inside a per-call function, RESEARCH.md Pitfall 4) — the trajectory file must contain every frame, not just the last one.
Output: `TP2/src/include/observables.h`, `TP2/src/utils/observables.cpp`, `TP2/src/include/io.h`, `TP2/src/utils/io.cpp`, extended `TP2/src/engine/simulation.h`, `TP2/src/main.cpp`, `TP2/src/selftest.cpp`, `TP2/Makefile` — `tp2` writes a real multi-frame trajectory and reports va/S; `tp2_test` proves clustering and polarization correctness.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-modelos-vicsek-y-votante/02-RESEARCH.md
@.planning/phases/02-modelos-vicsek-y-votante/02-01-SUMMARY.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Trajectory writer + polarization/clustering observables, wired end-to-end through the CLI</name>
  <read_first>
    TP1/src/utils/io.cpp
    TP1/src/include/io.h
    TP2/src/include/particle.h
    TP2/src/include/grid.h
    TP2/src/engine/simulation.h
    TP2/src/main.cpp
    .planning/phases/02-modelos-vicsek-y-votante/02-RESEARCH.md
  </read_first>
  <reversibility rating="reversible">The trajectory file's column layout and the observable formulas are internal to this project — no external tool consumes them yet (Phase 4's animation module does not exist). Format changes remain a cheap Python-side parser adjustment later, not a re-run of the C++ engine (RESEARCH.md Open Question 1).</reversibility>
  <files>TP2/src/engine/simulation.h, TP2/src/include/observables.h, TP2/src/utils/observables.cpp, TP2/src/include/io.h, TP2/src/utils/io.cpp, TP2/src/main.cpp, TP2/Makefile</files>
  <action>
    In TP2/src/engine/simulation.h: add a public accessor `const NeighborList&amp; neighbors() const { return grid_.neighbors(); }` alongside the existing `particles()` accessor — no other change to this file in this plan.

    Create TP2/src/include/observables.h (`#pragma once`, includes `&lt;vector&gt;`, `"grid.h"`, `"particle.h"`): declare `double polarization(const std::vector&lt;VicsekParticle&gt;&amp; particles);` and `double giantComponentFraction(const NeighborList&amp; neighbors);`.

    Create TP2/src/utils/observables.cpp: implement `polarization` — for `n = particles.size()`, return `0.0` if `n==0`; otherwise accumulate `sumSin += std::sin(p.theta)` and `sumCos += std::cos(p.theta)` over all particles, then return `std::sqrt((sumSin/N)*(sumSin/N) + (sumCos/N)*(sumCos/N))` where `N = static_cast&lt;double&gt;(n)` (the standard Vicsek order-parameter magnitude of the mean heading unit vector). Implement `giantComponentFraction` exactly per RESEARCH.md Pattern 3's iterative-stack connected-components: `n = neighbors.size()`, return `0.0` if `n==0`; otherwise a `std::vector&lt;bool&gt; visited(n, false)`, loop every unvisited start node pushing onto an explicit `std::vector&lt;int&gt; stack` (never `std::queue`/recursion, matching the project's no-recursion convention), track `largest = std::max(largest, size)` per component, return `static_cast&lt;double&gt;(largest) / static_cast&lt;double&gt;(n)`. Neither function throws for `n==0` — this is a valid input, not an invariant violation, per the project's "library/engine code never throws for expected inputs" convention.

    Create TP2/src/include/io.h (`#pragma once`, includes `&lt;fstream&gt;`, `&lt;vector&gt;`, `"particle.h"`): declare `void writeTrajectoryFrame(std::ofstream&amp; out, const std::vector&lt;VicsekParticle&gt;&amp; particles, double t, double v0);` — takes an ALREADY-OPEN stream by reference, never a path, so the caller controls open/append lifetime (RESEARCH.md Pitfall 4 — reopening `std::ofstream(path)` per call truncates to the last frame).

    Create TP2/src/utils/io.cpp (`#include "io.h"`, `&lt;iomanip&gt;`): implement `writeTrajectoryFrame` writing `out &lt;&lt; std::setprecision(12) &lt;&lt; t &lt;&lt; '\n';` then, for each particle, computing `double vx, vy; headingToVelocity(p.theta, v0, vx, vy);` and writing `out &lt;&lt; p.x &lt;&lt; ' ' &lt;&lt; p.y &lt;&lt; ' ' &lt;&lt; vx &lt;&lt; ' ' &lt;&lt; vy &lt;&lt; '\n';` — REAL velocities from the committed heading, replacing TP1's hardcoded `"0 0"`.

    In TP2/src/main.cpp: add `#include &lt;filesystem&gt;`, `#include &lt;fstream&gt;`, `#include "observables.h"`, `#include "io.h"`. Add `std::string out = "data/dynamic.txt";` to `Options`, `{"out", required_argument, nullptr, 'o'}` to `long_options`, `case 'o': o.out = optarg; break;` to `parseArgs`. Before the step loop in `main()`: call `std::filesystem::create_directories(std::filesystem::path(o.out).parent_path());` (guard: only call if `parent_path()` is non-empty, matching TP1's `create_directories(o.outdir)` convention in `TP1/src/main.cpp:215`), then open `std::ofstream trajOut(o.out);` and `if (!trajOut) fail("no se pudo abrir " + o.out);`. Inside the step loop, immediately after `sim.step();`, call `writeTrajectoryFrame(trajOut, sim.particles(), static_cast&lt;double&gt;(step + 1) * o.dt, o.v0);` (frame written once per step, using the SAME open stream every iteration — never reopened). After the loop, compute `const double va = polarization(sim.particles());` and `const double S = giantComponentFraction(sim.neighbors());`, and extend the final `std::printf` report line to append `va=%.4f S=%.4f` using `va, S`. Extend `usage()` with a `--out <path>` help line.

    Extend TP2/Makefile: add `$(SRC)/utils/observables.cpp` and `$(SRC)/utils/io.cpp` to `CORE_SRC` (both new source files must link into both `tp2` and `tp2_test`, matching the existing `CORE_OBJ` pattern) — this is pulled into Task 1 because Task 1's own self-test additions require these symbols to link, mirroring the deviation documented in 01-02-SUMMARY.md.
  </action>
  <verify>
    <automated>wsl.exe -e bash -lc 'cd "$(wslpath "C:\Users\Lucas Di Candia\Desktop\SDS\TP2-SDS-G5\TP2")" &amp;&amp; make test 2>&amp;1 | tail -10 &amp;&amp; ./tp2 --model vicsek --eta 0.1 --N 200 --steps 50 --seed 7 --out data/check.txt &amp;&amp; wc -l data/check.txt'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'ofstream(path)' TP2/src/utils/io.cpp` returns 0, and `grep -c 'std::ofstream&amp; out' TP2/src/include/io.h` returns exactly 1 (source assertion: `writeTrajectoryFrame` takes an already-open stream reference, never opens its own path — the Pitfall 4 truncation shape is structurally absent).
    - `wc -l data/check.txt` (after the verify run above, from `TP2/`) equals `50 * (1 + 200) = 10050` lines (steps × (1 header line + N particle lines) — proves every one of the 50 frames was appended, not just the last).
    - `cd TP2 &amp;&amp; make test` exits 0 with zero reported failures.
    - `./tp2 --model vicsek --steps 5 --seed 1` (default `--out data/dynamic.txt`) exits 0 and its final stdout line contains both `va=` and `S=`.
  </acceptance_criteria>
  <done>The trajectory writer appends one real-velocity frame per step to a single open stream (no truncation); `Simulation::neighbors()`, `polarization()`, and `giantComponentFraction()` are wired end-to-end into the CLI's final report line; `make test` passes.</done>
</task>

<task type="auto">
  <name>Task 2: Clustering/polarization self-tests (deterministic giant-component value, va(t) rising for both models)</name>
  <read_first>
    TP2/src/selftest.cpp
    TP2/src/include/observables.h
    TP2/src/utils/observables.cpp
    TP2/src/engine/simulation.h
  </read_first>
  <reversibility rating="reversible">Adds self-test coverage only — no production code changes, fully reversible.</reversibility>
  <files>TP2/src/selftest.cpp</files>
  <action>
    Extend TP2/src/selftest.cpp with two new test functions, each registered in `main()`:

    `testGiantComponentFraction()` — reuse `testGridStructural`'s exact 4-particle deterministic configuration (p0={0,1.0,1.0,0.0}, p1={1,2.0,1.0,0.0}, p2={2,1.0,2.4,0.0}, p3={3,8.0,8.0,0.0}, L=10.0, rc=1.5, periodic=false — p0/p1/p2 form one connected component via chained adjacency, p3 is isolated). Build a `Grid`, call `rebuild()`, call `giantComponentFraction(grid.neighbors())`, and assert the result equals `0.75` within `1e-9` (3 of 4 particles in the giant component: `3.0/4.0 = 0.75` exactly — hand-verified, not read back from the function under test). Also assert `giantComponentFraction({})` (an empty `NeighborList`) returns `0.0` (the `n==0` guard, per Pitfall-avoidance for the isolated-particle-adjacent edge case at ρ=2).

    `testPolarizationRisesForBothModels()` — for each of `Model::Vicsek` and `Model::Voter`: generate `N=200` particles via `generateVicsekParticles(200, 10.0, 7)`, record `vaStart = polarization(particles)` BEFORE constructing the `Simulation`, build `Simulation sim(std::move(particles), /*L=*/10.0, /*rc=*/1.0, /*v0=*/0.5, /*dt=*/1.0, /*M=*/maxValidGridM(10.0,1.0), /*periodic=*/true, model, /*eta=*/0.1, /*seed=*/7)`, run `step()` 300 times, then `vaEnd = polarization(sim.particles())`. Assert `vaEnd &gt; vaStart` AND `vaEnd &gt; 0.5` for BOTH models (this is the self-test proof of the phase's success criterion 1 — a single validation run per model at low eta shows va(t) rising toward a high value; empirically Vicsek reaches ≈0.9995 and voter reaches ≈0.9703 under these exact parameters).
  </action>
  <verify>
    <automated>wsl.exe -e bash -lc 'cd "$(wslpath "C:\Users\Lucas Di Candia\Desktop\SDS\TP2-SDS-G5\TP2")" &amp;&amp; make test 2>&amp;1 | tail -12'</automated>
  </verify>
  <acceptance_criteria>
    - `cd TP2 &amp;&amp; make test` exits 0 with a summary line reporting zero failures across the full suite.
    - TP2/src/selftest.cpp contains the literal substring `0.75` as an asserted expected value (source assertion the giant-component test encodes the hand-derived fraction, not a value read back from the function under test).
    - TP2/src/selftest.cpp contains a call to a function whose name contains `PolarizationRises` and that function references both `Model::Vicsek` and `Model::Voter` (source assertion: `grep -c 'Model::Voter' TP2/src/selftest.cpp` returns at least 1 in addition to any `Model::Vicsek` references, proving both models are exercised by this test, not just one).
  </acceptance_criteria>
  <done>A deterministic self-test proves giantComponentFraction returns the hand-verified 0.75 on a small 4-particle configuration; a second self-test proves va(t) rises above 0.5 for both Vicsek and voter models under the same low-noise parameters, satisfying the phase's success criterion 1; make test passes with zero failures.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `--out <path>` CLI flag → `std::filesystem::create_directories` + `std::ofstream` | Locally-supplied file path from the same trusted operator running the binary; the only new external input this plan introduces beyond Plan 02-01's `--model`/`--eta` |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-03 | Tampering | `TP2/src/main.cpp` `--out` path handling | low | accept | This is an offline, single-user, local-only CLI where the operator already has whatever filesystem write access their own OS account grants — `--out` writing to an arbitrary path the operator chooses crosses no privilege boundary (same acceptance rationale as T-01-02/T-01-04 in Phase 1's plans); `if (!trajOut) fail(...)` still surfaces open failures cleanly rather than crashing |
| T-02-04 | Denial of Service | Trajectory writer called once per step for the full run duration | low | accept | Unbounded `--steps`/`--N` combined with per-step trajectory writing only consumes the local operator's own disk space and time; no other user or shared resource is affected. Gating trajectory writing off for bulk sweep runs (to avoid the I/O explosion `.planning/research/ARCHITECTURE.md` Anti-Pattern 4 warns about) is explicitly Phase 3's OUTPUT-02 scope, not this phase's — this plan's writer stays simple and always-on by design, per RESEARCH.md Pattern 4 |
</threat_model>

<verification>
`cd TP2 &amp;&amp; make test` exits 0 after both tasks. A run with `--steps 50` produces a trajectory file with exactly `50 * (1 + N)` lines (no truncation). Self-tests prove `giantComponentFraction` returns the hand-verified `0.75` on a deterministic 4-particle case, and `polarization()` rises above 0.5 for both Vicsek and voter models under matching low-noise parameters.
</verification>

<success_criteria>
The dynamic output file contains real `vx,vy` per particle per timestep, appended across the whole run — no longer TP1's `"0 0"` placeholder (OUTPUT-01). Cluster detection reuses the engine's own `Grid::neighbors()` adjacency with no second neighbor search (CLUSTER-01) and returns `S = largest/N` verified against hand inspection (CLUSTER-02). A single validation run per model at low eta shows va(t) rising toward a high value (phase success criterion 1), proven for both Vicsek and voter.
</success_criteria>

<output>
Create `.planning/phases/02-modelos-vicsek-y-votante/02-02-SUMMARY.md` when done
</output>

## Artifacts This Phase Produces

Full manifest across both plans in Phase 2 (Modelos Vicsek y Votante):

**From Plan 01 (02-01-PLAN.md):**
- `TP2/src/engine/simulation.h` / `TP2/src/engine/simulation.cpp` (modified) — `Model` enum, `addAngularNoise(theta, eta, rng)`, `voterHeading(i, particles, neighbors, rng)`, `Simulation` constructor extended with trailing defaulted `model`/`eta`/`seed`, `step()` Pass 2 branches on `model_` through the shared noise call
- `TP2/src/main.cpp` (modified) — `--model vicsek|voter`, `--eta <real>` CLI flags with validation
- `TP2/src/selftest.cpp` (modified) — `testModelDefaultsReproducePhase1`, `testCircularMeanNearPi`, `testVoterZeroNeighborSelfInclusion`, `testVoterCandidatePoolInvariant`

**From Plan 02 (this plan):**
- `TP2/src/engine/simulation.h` (modified) — `Simulation::neighbors()` accessor
- `TP2/src/include/observables.h` / `TP2/src/utils/observables.cpp` (new) — `polarization(particles)`, `giantComponentFraction(neighbors)`
- `TP2/src/include/io.h` / `TP2/src/utils/io.cpp` (new) — `writeTrajectoryFrame(ofstream&, particles, t, v0)` (append-mode, real velocities)
- `TP2/src/main.cpp` (modified) — `--out <path>` CLI flag; trajectory stream opened once before the step loop; final report line extended with `va=`/`S=`
- `TP2/src/selftest.cpp` (modified) — `testGiantComponentFraction`, `testPolarizationRisesForBothModels`
- `TP2/Makefile` (modified) — `observables.cpp` and `io.cpp` added to `CORE_SRC`

**Explicitly out of scope for Phase 2** (deferred to Phase 3 per REQUIREMENTS.md SWEEP-*/OUTPUT-02): the parameter-sweep driver, multi-seed statistics, the scalar-only `(t, va, S)` log format for bulk sweep runs (as opposed to this phase's full per-particle trajectory writer), and the steady-state detection criterion. This phase's `polarization()`/`giantComponentFraction()` functions are the exact building blocks Phase 3's scalar logger will call every timestep — no rework expected, only a new caller.
</output>
