---
phase: 02-modelos-vicsek-y-votante
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - TP2/src/engine/simulation.h
  - TP2/src/engine/simulation.cpp
  - TP2/src/main.cpp
  - TP2/src/selftest.cpp
autonomous: true
requirements: [VICSEK-01, VOTER-01, VOTER-02]

estimate:
  tokens: 44000
  raw_tokens: 44000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A --model vicsek|voter CLI flag selects the interaction rule on the same Simulation engine, the same eta noise function, and the same rc; --model garbage is rejected with a validation error, matching the existing --steps/--N/--M validation style."
    - "Vicsek's neighbor average and voter's neighbor copy both draw noise from the exact same addAngularNoise(theta, eta, rng) function — no second, independent noise expression exists anywhere in simulation.cpp (VOTER-02)."
    - "With eta=0.0 and the new constructor parameters left at their defaults, Phase 1's three original self-tests (testSynchronousUpdateNoBias, testWallsDoNotWrap, testLongRunStaysWrapped) plus a new eta=0 regression test all pass unmodified."
    - "The voter rule's zero-external-neighbor case (an isolated particle) is well-defined (self-inclusive candidate pool) and produces no undefined behavior, verified across repeated steps."
    - "The Vicsek circular mean does not collapse to 0 for headings straddling the +-pi branch cut — a hand-computed two-particle case at 179 deg / -179 deg converges to +-pi, not 0 (the arithmetic-mean pathology)."
  artifacts:
    - path: "TP2/src/engine/simulation.h"
      provides: "Model enum {Vicsek, Voter}; addAngularNoise/voterHeading declarations; Simulation ctor extended with trailing defaulted model/eta/seed params; model_/eta_/rng_ members"
    - path: "TP2/src/engine/simulation.cpp"
      provides: "addAngularNoise (Uniform(-eta/2, eta/2)) and voterHeading (self-inclusive uniform pick over {i} union neighbors[i]) implementations; step() Pass 2 branches on model_ then funnels both branches through the identical addAngularNoise call"
    - path: "TP2/src/main.cpp"
      provides: "--model and --eta CLI flags with validation (fail() on invalid model string or negative eta); Model resolved from o.model and passed into the Simulation constructor alongside o.eta/o.seed"
  key_links:
    - from: "TP2/src/main.cpp"
      to: "TP2/src/engine/simulation.h"
      via: "parseArgs resolves o.model/o.eta; main() maps o.model to a Model enum value and passes model, o.eta, o.seed as the Simulation constructor's three new trailing arguments"
      pattern: "Model\\s+model\\s*="
    - from: "TP2/src/engine/simulation.cpp"
      to: "TP2/src/engine/simulation.cpp"
      via: "step()'s Pass 2 loop computes raw heading via circularMeanHeading (Vicsek) or voterHeading (Voter), then both paths call the identical addAngularNoise(raw, eta_, rng_)"
      pattern: "addAngularNoise\\(raw"
---

<objective>
Extend the Phase 1 engine (`Simulation::step()`) with a pluggable interaction rule — Vicsek's existing self-inclusive circular mean, plus a new self-inclusive voter rule (uniform random pick over `{i} ∪ neighbors[i]`) — both routed through one shared `addAngularNoise(theta, eta, rng)` function, selectable end-to-end via a `--model vicsek|voter` / `--eta <value>` CLI flag pair. New constructor parameters (`model`, `eta`, `seed`) are appended with defaults that exactly reproduce Phase 1's behavior, so all three of Phase 1's existing self-tests keep passing unmodified.

Purpose: Closes VICSEK-01/VOTER-01/VOTER-02. The highest-risk failure mode in this phase is convention drift between two near-duplicate rule paths (different noise amplitude, different self-inclusion policy) — RESEARCH.md's Pattern 1/Pitfall 1 — so both rules are structurally forced through one noise call, and the voter rule's zero-neighbor edge case is handled without undefined behavior (RESEARCH.md Pitfall 2), not a special-cased branch.
Output: `TP2/src/engine/simulation.h`, `TP2/src/engine/simulation.cpp`, `TP2/src/main.cpp`, extended `TP2/src/selftest.cpp` — `tp2` and `tp2_test` both build and pass with both models selectable via CLI.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-modelos-vicsek-y-votante/02-RESEARCH.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Pluggable Vicsek/Voter model selection with shared noise, wired end-to-end through the CLI</name>
  <read_first>
    TP2/src/engine/simulation.h
    TP2/src/engine/simulation.cpp
    TP2/src/main.cpp
    TP2/src/utils/generator.cpp
    .planning/phases/02-modelos-vicsek-y-votante/02-RESEARCH.md
  </read_first>
  <reversibility rating="costly">The noise convention Uniform(-eta/2, eta/2) and the voter rule's self-inclusive candidate pool ({i} union neighbors[i], analogous to Vicsek's already-established self-inclusion) fix the exact numeric output of every future run. Getting either wrong means re-running Phase 3's eventual parameter sweep on top of a changed convention — costly, but not a one-way data migration or a published external contract, matching the precedent set for the analogous Vicsek self-inclusion decision in 01-02-PLAN.md Task 1.</reversibility>
  <files>TP2/src/engine/simulation.h, TP2/src/engine/simulation.cpp, TP2/src/main.cpp, TP2/src/selftest.cpp</files>
  <action>
    In TP2/src/engine/simulation.h: add `#include &lt;random&gt;` to the includes. Add `enum class Model { Vicsek, Voter };` above the existing `circularMeanHeading` declaration. Declare `double addAngularNoise(double theta, double eta, std::mt19937_64&amp; rng);` and `double voterHeading(int i, const std::vector&lt;VicsekParticle&gt;&amp; particles, const NeighborList&amp; neighbors, std::mt19937_64&amp; rng);`. Extend the `Simulation` constructor declaration by appending exactly three new trailing parameters with defaults reproducing Phase 1's exact behavior — `Model model = Model::Vicsek, double eta = 0.0, unsigned long long seed = 1` — never insert them before the existing `periodic` parameter (would break Phase 1's three positional call sites in selftest.cpp per RESEARCH.md Pitfall 3). Add private members `Model model_;`, `double eta_;`, `std::mt19937_64 rng_;` after the existing `periodic_` member.

    In TP2/src/engine/simulation.cpp: implement `addAngularNoise(theta, eta, rng)` as `std::uniform_real_distribution&lt;double&gt; noiseDist(-eta / 2.0, eta / 2.0); return theta + noiseDist(rng);` — this is the ONLY place `uniform_real_distribution` is constructed in this file. Implement `voterHeading(i, particles, neighbors, rng)`: `const auto&amp; row = neighbors[static_cast&lt;size_t&gt;(i)];` then `std::uniform_int_distribution&lt;size_t&gt; pick(0, row.size());` (inclusive range `[0, row.size()]`, well-defined even when `row` is empty — this is the fix for Pitfall 2's UB, do not write `pick(0, row.size() - 1)`), `const size_t choice = pick(rng);`, `const int chosen = (choice == row.size()) ? i : row[choice];`, return `particles[static_cast&lt;size_t&gt;(chosen)].theta` (the candidate's OLD heading — no noise applied inside this function). Update the `Simulation` constructor's member-initializer list to append `model_(model), eta_(eta), rng_(seed)` after the existing `periodic_(periodic)` initializer. In `step()`'s Pass 2 loop, replace the single `circularMeanHeading` call with: compute `const double raw = (model_ == Model::Vicsek) ? circularMeanHeading(i, particles_, neighbors) : voterHeading(i, particles_, neighbors, rng_);` then `thetaNew_[static_cast&lt;size_t&gt;(i)] = addAngularNoise(raw, eta_, rng_);` — both branches must funnel through this identical call; never write two separate noise expressions.

    In TP2/src/main.cpp: add to `Options`: `std::string model = "vicsek";` and `double eta = 0.0;`. Add to `long_options`: `{"model", required_argument, nullptr, 'm'}` and `{"eta", required_argument, nullptr, 'e'}`. Add to the `parseArgs` switch: `case 'm': o.model = optarg; break;` and `case 'e': o.eta = std::stod(optarg); break;`. Add validation after the existing `if` checks, in the same `fail("...")` style already used for `--steps`/`--N`/`--M`: `if (o.model != "vicsek" &amp;&amp; o.model != "voter") fail("--model debe ser 'vicsek' o 'voter'");` and `if (o.eta &lt; 0.0) fail("--eta debe ser >= 0");`. In `main()`, before constructing `Simulation`, add `const Model model = (o.model == "voter") ? Model::Voter : Model::Vicsek;` and extend the `Simulation sim(...)` call by appending the three trailing arguments `model, o.eta, o.seed`. Extend `usage()`'s help text with `--model vicsek|voter` and `--eta <real>` lines, and extend the final `std::printf` report to also print `model=%s eta=%.4f` using `o.model.c_str()` and `o.eta`.

    Extend TP2/src/selftest.cpp: add `testModelDefaultsReproducePhase1()` — reconstruct exactly `testSynchronousUpdateNoBias`'s three-particle hand-computed case (p0={0,1.0,1.0,0.0}, p1={1,2.0,1.0,pi/2}, p2={2,1.0,2.0,pi}, L=10.0, rc=2.0, periodic=false) via the OLD 7-positional-argument `Simulation` constructor call (no model/eta/seed arguments supplied — relying purely on the new trailing defaults), call `step()` once, and assert every resulting theta is within 1e-9 of `pi/2` (the same hand-derived value Phase 1 already proved) — this is the literal regression proof that appending defaulted trailing parameters left existing call-site behavior unchanged. Register it in `main()`.
  </action>
  <verify>
    <automated>wsl.exe -e bash -lc 'cd "$(wslpath "C:\Users\Lucas Di Candia\Desktop\SDS\TP2-SDS-G5\TP2")" &amp;&amp; make test 2>&amp;1 | tail -10 &amp;&amp; ./tp2 --model vicsek --eta 0.3 --steps 20 --seed 42 &amp;&amp; ./tp2 --model voter --eta 0.3 --steps 20 --seed 42'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'uniform_real_distribution' TP2/src/engine/simulation.cpp` returns exactly 1 (only `addAngularNoise` constructs a noise distribution — no second, independent noise expression exists anywhere else in the file).
    - `grep -c 'addAngularNoise(' TP2/src/engine/simulation.cpp` returns exactly 2 (the function definition plus its single call site inside `step()` — proving both `Model::Vicsek` and `Model::Voter` branches funnel through the same call, not two separate ones).
    - `cd TP2 &amp;&amp; make test` exits 0, and `testModelDefaultsReproducePhase1` plus Phase 1's original three self-tests (`testSynchronousUpdateNoBias`, `testWallsDoNotWrap`, `testLongRunStaysWrapped`) all report zero failures.
    - `./tp2 --model vicsek --eta 0.3 --steps 20 --seed 42` and `./tp2 --model voter --eta 0.3 --steps 20 --seed 42` both exit 0 (run from `TP2/`).
    - `./tp2 --model garbage --steps 1` exits 1 with a stderr message containing the substring `--model`.
  </acceptance_criteria>
  <done>`--model vicsek|voter` and `--eta` select between the two rules on the same `Simulation`/CLI, both funneling through one shared `addAngularNoise` call; Phase 1's three original self-tests plus the new eta=0 regression test all pass unmodified; both models run successfully end-to-end via the CLI.</done>
</task>

<task type="auto">
  <name>Task 2: Circular-mean +-pi pathology, voter self-inclusion, and zero-neighbor safety self-tests</name>
  <read_first>
    TP2/src/selftest.cpp
    TP2/src/engine/simulation.h
    TP2/src/engine/simulation.cpp
    .planning/phases/02-modelos-vicsek-y-votante/02-RESEARCH.md
  </read_first>
  <reversibility rating="reversible">Adds self-test coverage only — no production code changes, fully reversible.</reversibility>
  <files>TP2/src/selftest.cpp</files>
  <action>
    Extend TP2/src/selftest.cpp with three new test functions, each registered in `main()`:

    `testCircularMeanNearPi()` — two mutually-neighboring particles, p0={0, 1.0, 1.0, 179.0*kPi/180.0} and p1={1, 1.5, 1.0, -179.0*kPi/180.0}, L=10.0, rc=2.0, periodic=false, v0=0.0, eta=0.0 (isolates the heading math, matching RESEARCH.md Pitfall 5's exact recommended case). Build a `Simulation` via the 7-arg-plus-defaults constructor, call `step()` once, and assert both particles' resulting theta is within 1e-6 of `kPi` (verified analytically: the self-inclusive sums are `sumSin = sin(t0)+sin(t1) = 0` exactly and `sumCos = cos(t0)+cos(t1) ≈ -1.9997` for both particles by symmetry, so `atan2(0, negative) = +pi` exactly — not a rough estimate). Also assert the value is NOT within 0.1 of `0.0`, making explicit that this is the exact pathology an arithmetic mean of the same two headings would produce (arithmetic mean of `179°` and `-179°` in radians is `0.0`, radically wrong).

    `testVoterZeroNeighborSelfInclusion()` — a single isolated particle (`std::vector&lt;VicsekParticle&gt;` of size 1, e.g. `{0, 5.0, 5.0, 1.234}`), `L=10.0, rc=1.0, periodic=false, v0=0.0, model=Model::Voter, eta=0.0`. Build the `Simulation`, call `step()` 20 times in a loop, and after every call assert `particles()[0].theta` equals the original `1.234` within 1e-9 (the empty-neighbor candidate range `[0,0]` always self-selects, so the heading never changes and no `uniform_int_distribution` construction is ever malformed) — this is the runtime proof that Pitfall 2's UB case cannot occur.

    `testVoterCandidatePoolInvariant()` — two mutually-neighboring particles p0={0, 1.0, 1.0, 0.0} and p1={1, 1.5, 1.0, kPi/2.0}, `L=10.0, rc=2.0, periodic=false, v0=0.0, model=Model::Voter, eta=0.0, seed=42`. Build the `Simulation`, run `step()` 100 times, and after every step assert every particle's theta is within 1e-12 of either `0.0` or `kPi/2.0` (no third value is ever possible with eta=0.0, since both particles only ever copy from the exact old-snapshot value set `{0.0, kPi/2.0}`, which is closed under the voter update) — proving `voterHeading` only ever returns a genuine candidate value from the neighbor set, never a corrupted or out-of-range result.
  </action>
  <verify>
    <automated>wsl.exe -e bash -lc 'cd "$(wslpath "C:\Users\Lucas Di Candia\Desktop\SDS\TP2-SDS-G5\TP2")" &amp;&amp; make test 2>&amp;1 | tail -12'</automated>
  </verify>
  <acceptance_criteria>
    - `cd TP2 &amp;&amp; make test` exits 0 with a summary line reporting zero failures across the full suite (Phase 1's tests plus Task 1's plus this task's three new tests).
    - TP2/src/selftest.cpp contains the literal substring `179.0` (source assertion that the ±π hand-computed case uses the exact degree values RESEARCH.md Pitfall 5 specifies, not a rounder placeholder angle).
    - TP2/src/selftest.cpp contains a call to a function whose name contains `ZeroNeighbor` (source assertion that the Pitfall 2 UB-safety test exists and is registered).
  </acceptance_criteria>
  <done>Three new self-tests prove the circular mean avoids the arithmetic-mean pathology near ±π, the voter rule's zero-neighbor case is well-defined with no UB, and the voter candidate pool never returns an out-of-set value; `make test` passes with zero failures.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI flags (`--model`, `--eta`) → `main.cpp` → `Simulation` construction | Locally-supplied string/numeric CLI input parsed via `getopt_long`; a malformed `--model` string or a negative `--eta` are the only new external inputs this task introduces |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering | `TP2/src/main.cpp` `--model` parsing | low | mitigate | `--model` is validated against the exact string set `{"vicsek","voter"}` via `fail()` before any `Model` enum mapping occurs, matching the existing `--steps`/`--N`/`--M` validation style — an invalid value can never silently fall through to `Simulation` construction |
| T-02-02 | Tampering | `TP2/src/main.cpp` `--eta` parsing → `addAngularNoise`'s `uniform_real_distribution(-eta/2, eta/2)` | medium | mitigate | A negative `--eta` would construct `uniform_real_distribution` with its lower bound greater than its upper bound (`-eta/2 > eta/2` when `eta<0`), which is undefined behavior per the C++ standard — `main.cpp` rejects `eta &lt; 0.0` via `fail()` before construction, closing this off entirely at the CLI boundary |
</threat_model>

<verification>
`cd TP2 &amp;&amp; make test` exits 0 after both tasks, covering Phase 1's original three tests plus this plan's four new tests (regression, ±π pathology, voter zero-neighbor safety, voter candidate-pool invariant). `./tp2 --model vicsek --eta 0.3 --steps 20 --seed 42` and `./tp2 --model voter --eta 0.3 --steps 20 --seed 42` both exit 0.
</verification>

<success_criteria>
`--model vicsek|voter` selects the interaction rule on the same engine/noise/rc; both rules funnel through one shared `addAngularNoise` call (VOTER-02, structurally enforced, not by convention); the circular mean correctly avoids the ±π arithmetic-mean pathology; the voter rule's zero-neighbor case is well-defined with no undefined behavior; Phase 1's three original self-tests continue to pass unmodified with `eta=0.0` defaults.
</success_criteria>

<output>
Create `.planning/phases/02-modelos-vicsek-y-votante/02-01-SUMMARY.md` when done
</output>

## Artifacts This Phase Produces

**From this plan (02-01):**
- `TP2/src/engine/simulation.h` (modified) — `Model` enum (`Vicsek`, `Voter`); `addAngularNoise(theta, eta, rng)` and `voterHeading(i, particles, neighbors, rng)` declarations; `Simulation` constructor extended with trailing defaulted `model`/`eta`/`seed` params; private `model_`/`eta_`/`rng_` members
- `TP2/src/engine/simulation.cpp` (modified) — `addAngularNoise` (Uniform(-η/2, η/2)) and `voterHeading` (self-inclusive uniform pick over `{i} ∪ neighbors[i]`, UB-safe for the empty-neighbor case) implementations; `step()`'s Pass 2 branches on `model_`, both branches call the identical `addAngularNoise`
- `TP2/src/main.cpp` (modified) — `--model vicsek|voter` and `--eta <real>` CLI flags with validation; `Simulation` constructed with `model`, `eta`, `seed`
- `TP2/src/selftest.cpp` (modified) — `testModelDefaultsReproducePhase1`, `testCircularMeanNearPi`, `testVoterZeroNeighborSelfInclusion`, `testVoterCandidatePoolInvariant`

**Deferred to 02-02 (this phase's other plan)** per REQUIREMENTS.md OUTPUT-01/CLUSTER-01/CLUSTER-02: the trajectory writer (real `vx,vy` per timestep, append-mode), `Simulation::neighbors()` accessor, `polarization()`, and `giantComponentFraction()` — none of which are needed by VICSEK-01/VOTER-01/VOTER-02, which this plan completes in full.
</output>
