---
phase: 04-an-lisis-gr-ficos-y-animaci-n
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - TP2/python/analyze.py
autonomous: true
requirements: [VIZ-03, VIZ-05, VIZ-06, VIZ-07]

estimate:
  tokens: 75000
  raw_tokens: 75000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "TP2/data/sweep/summary.csv exists with real (non-smoke) data: rho in {2.0,4.0,8.0}, both models present, n_seeds >= 5 on every row"
    - "va(eta) and S(eta) plots each overlay vicsek (solid line) and voter (dashed line) across all 3 densities with genuine multi-seed error bars -- 6 series per plot"
    - "The va vs S plot visually distinguishes the 3 densities"
  artifacts:
    - TP2/data/sweep/summary.csv
    - TP2/python/analyze.py
    - TP2/data/plots/va_eta.png
    - TP2/data/plots/S_eta.png
    - TP2/data/plots/va_vs_S.png
  key_links:
    - "sweep.py main() -> tp2 subprocess batch (multiprocessing.Pool) -> aggregate_to_csv -> TP2/data/sweep/summary.csv"
    - "analyze.py load_summary() -> summary.csv rows -> plot_va_eta()/plot_S_eta()/plot_va_vs_S()"
---

<objective>
Produce the real full parametric sweep data (Phase 3 built the driver but never ran it for real -- only a reduced smoke test exists) and stand up `TP2/python/analyze.py`, the single entrypoint script that all of Phase 4's static plots live in, with its first three summary.csv-driven plot types wired end-to-end: va(eta), S(eta), and va vs S.

Purpose: This is the phase's tracer slice -- it proves the complete pipeline (sweep execution -> CSV aggregation -> Python parsing -> matplotlib rendering -> PNG artifact) works for real, on real data, before any other plot type is added. Every later plan in this phase depends on `TP2/data/sweep/summary.csv` existing and on `analyze.py`'s established conventions (module constants, `load_summary()`, the Axes-returning plot function pattern).
Output: `TP2/data/sweep/summary.csv` (real full sweep), `TP2/python/analyze.py` (new), `TP2/data/plots/va_eta.png`, `TP2/data/plots/S_eta.png`, `TP2/data/plots/va_vs_S.png`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-an-lisis-gr-ficos-y-animaci-n/04-CONTEXT.md

@TP2/python/sweep.py
@TP1/python/visualize.py
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Run the real full parametric sweep + analyze.py skeleton + va(eta) plot, end-to-end</name>
  <files>TP2/python/analyze.py</files>
  <precondition>WSL with the built `TP2/tp2` binary, python3, matplotlib>=3.11, numpy>=2.5, and Pillow available (confirmed present in this environment). All commands in this task run via `wsl.exe -- bash -lc "cd '/mnt/c/Users/Lucas Di Candia/Desktop/SDS/TP2-SDS-G5/TP2' && ..."`, never a bare Windows shell.</precondition>
  <read_first>
    TP2/python/sweep.py (summary.csv schema: model,rho,eta,va_mean,va_std,S_mean,S_std,n_seeds -- written as raw floats via csv.DictWriter, so re-read as strings and must be cast; main()'s default CLI args: rhos [2,4,8], models [vicsek,voter], k-seeds=5, steps=2000, k-explore=2, steps-explore=500)
    TP1/python/visualize.py (single-entrypoint convention: matplotlib.use("Agg") unless "--show" in sys.argv, module-level SCREAMING_SNAKE_CASE color constants, argparse main())
    .planning/phases/04-an-lisis-gr-ficos-y-animaci-n/04-CONTEXT.md (va(eta)/S(eta) decisions: read directly from summary.csv, no recompute; solid=vicsek, dashed=voter; 6 series per plot)
  </read_first>
  <action>
    Step 0 -- ensure the engine binary is current: run `make` in TP2/ (rebuilds tp2/tp2_test warning-clean under -Wall -Wextra -pedantic per the existing Makefile; no source changes needed, this phase does not touch TP2/src/).
    Step 1 -- run the real full sweep: `python3 python/sweep.py` with NO extra flags (uses every documented default: rhos {2,4,8}, models {vicsek,voter}, k-seeds=5, steps=2000, k-explore=2, steps-explore=500). This invokes explore_transition per (model,rho) then the full eta-grid sweep across a multiprocessing.Pool sized to os.cpu_count() -- roughly 600-700 total tp2 subprocess calls, each well under 3 seconds even at the worst case (rho=8, steps=2000 measured at ~2.6s serial), so the whole run parallelizes to well under 10 minutes on this machine. Give the command a generous timeout (up to 600000ms) rather than the default 2-minute Bash timeout. This overwrites the prior smoke-test-only data under TP2/data/sweep/ with the real dataset; it produces TP2/data/sweep/summary.csv (the frozen schema from Phase 3) and per-seed scalar logs under TP2/data/sweep/{model}/rho{rho}/eta{eta}/seed{seed}.txt that later plans in this phase depend on.
    Step 2 -- create TP2/python/analyze.py as a new script following TP1/python/visualize.py's entrypoint convention: matplotlib.use("Agg") guarded by `"--show" not in sys.argv`, executed before importing pyplot. Define module-level path constants TP2_DIR (parent of python/), SWEEP_SUMMARY_CSV = TP2_DIR/"data"/"sweep"/"summary.csv", PLOTS_DIR = TP2_DIR/"data"/"plots". Insert TP2_DIR/"python" onto sys.path (or resolve via Path(__file__).parent) so `from sweep import L_DEFAULT` works when analyze.py is invoked from either TP2/ or TP2/python/.
    Step 3 -- define module-level SCREAMING_SNAKE_CASE style constants (matching TP1's COLOR_OTHER-style convention): COLOR_VICSEK, COLOR_VOTER (two distinct hex colors), LINESTYLE_VICSEK = "-", LINESTYLE_VOTER = "--", RHO_COLORS = {2.0: <color>, 4.0: <color>, 8.0: <color>} (3 distinct hex colors), RHO_MARKERS = {2.0: "o", 4.0: "s", 8.0: "^"}. These constants are reused by every later plan's plot functions -- do not redefine them elsewhere.
    Step 4 -- implement load_summary(csv_path=SWEEP_SUMMARY_CSV) -> list[dict]: reads the CSV via csv.DictReader, returns a list of dicts with "model" left as-is (str) and "rho","eta","va_mean","va_std","S_mean","S_std" cast to float and "n_seeds" cast to int (csv.DictReader always returns strings, so every numeric field must be explicitly cast here, once, so every downstream function receives real numbers).
    Step 5 -- implement plot_va_eta(rows, out_path=None) -> matplotlib.axes.Axes: groups rows by (model,rho), sorts each group by eta ascending (unsorted x-values would zigzag the line), draws one ax.errorbar per group using yerr=va_std, color=RHO_COLORS[rho], linestyle=LINESTYLE_VICSEK if model=="vicsek" else LINESTYLE_VOTER, marker=RHO_MARKERS[rho], label=f"{model} rho={rho:g}" -- exactly 6 series on one Axes. Sets xlabel "eta", ylabel "va", a legend, and a title. Saves to out_path (default PLOTS_DIR/"va_eta.png") via fig.savefig(..., dpi=150, bbox_inches="tight") when not --show, and RETURNS the Axes object (not None) so later acceptance checks can introspect ax.containers.
    Step 6 -- implement main(): argparse with --show (matches TP1 convention) and --summary (Path, default SWEEP_SUMMARY_CSV); ensures PLOTS_DIR exists via mkdir(parents=True, exist_ok=True); calls load_summary() once and plot_va_eta(rows); prints the output path. Add the `if __name__ == "__main__": main()` guard.
  </action>
  <verify>
    <automated>wsl.exe -- bash -lc "cd '/mnt/c/Users/Lucas Di Candia/Desktop/SDS/TP2-SDS-G5/TP2' && python3 python/analyze.py && test -s data/plots/va_eta.png && echo VA_ETA_OK"</automated>
  </verify>
  <acceptance_criteria>
    - `wsl.exe -- bash -lc "cd TP2 && wc -l data/sweep/summary.csv"` reports at least 90 lines (header + real rows spanning 3 densities x ~15-17 eta points x 2 models, not the ~15-row smoke test).
    - `wsl.exe -- bash -lc "cd TP2 && python3 -c \"import csv; rows=list(csv.DictReader(open('data/sweep/summary.csv'))); models={r['model'] for r in rows}; rhos={r['rho'] for r in rows}; assert models=={'vicsek','voter'}, models; assert rhos=={'2.0','4.0','8.0'}, rhos; assert all(int(r['n_seeds'])>=5 for r in rows); print('SWEEP_REAL_OK', len(rows))\""` prints `SWEEP_REAL_OK <n>` with no assertion error.
    - `data/plots/va_eta.png` exists, is non-empty, and its dimensions exceed 300x200px (`python3 -c "from PIL import Image; im=Image.open('data/plots/va_eta.png'); assert im.size[0]>300 and im.size[1]>200"`).
    - Calling `plot_va_eta(load_summary())` directly (via `python3 -c` inside TP2/python/) returns an Axes with `len(ax.containers) == 6` (3 densities x 2 models, confirming both models and all 3 densities are genuinely plotted, not a subset).
  </acceptance_criteria>
  <done>TP2/data/sweep/summary.csv contains the real full-sweep dataset (not the smoke test); TP2/python/analyze.py exists with load_summary() and plot_va_eta() wired end-to-end through main(); TP2/data/plots/va_eta.png is a real, correctly-shaped 6-series plot.</done>
</task>

<task type="auto">
  <name>Task 2: S(eta) and va-vs-S plots</name>
  <files>TP2/python/analyze.py</files>
  <read_first>
    TP2/python/analyze.py (Task 1's plot_va_eta, RHO_COLORS/LINESTYLE_* constants, load_summary(), main() structure -- reuse, do not redefine)
    .planning/phases/04-an-lisis-gr-ficos-y-animaci-n/04-CONTEXT.md (S(eta): "mismo procedimiento que va(eta)"; va vs S: "distinguiendo las tres densidades")
  </read_first>
  <action>
    Implement plot_S_eta(rows, out_path=None) -> Axes: identical structure to plot_va_eta (same grouping/sorting/color/linestyle/marker logic, same 6-series-per-plot shape) but plots S_mean with yerr=S_std, ylabel "S", saved to PLOTS_DIR/"S_eta.png" by default.
    Implement plot_va_vs_S(rows, out_path=None) -> Axes: groups rows by rho only (not by model -- both models' points share one color per density so the density distinction is the primary visual signal per CONTEXT.md), draws a scatter (ax.scatter) of va_mean (x) vs S_mean (y) for every row, colored by RHO_COLORS[rho], using a different marker per model (RHO_MARKERS for density is already used for eta-plots; for this plot use a fixed marker "o" for vicsek and "x" for voter to keep density=color, model=marker-shape orthogonal and both readable), with a legend that lists all 6 (rho,model) combinations. xlabel "va", ylabel "S", saved to PLOTS_DIR/"va_vs_S.png" by default. Returns the Axes.
    Wire both into main(): after plot_va_eta(rows), call plot_S_eta(rows) and plot_va_vs_S(rows); print both output paths.
  </action>
  <verify>
    <automated>wsl.exe -- bash -lc "cd '/mnt/c/Users/Lucas Di Candia/Desktop/SDS/TP2-SDS-G5/TP2' && python3 python/analyze.py && test -s data/plots/S_eta.png && test -s data/plots/va_vs_S.png && echo S_ETA_VS_OK"</automated>
  </verify>
  <acceptance_criteria>
    - `data/plots/S_eta.png` and `data/plots/va_vs_S.png` both exist, are non-empty, and exceed 300x200px (same PIL size check pattern as Task 1's va_eta.png).
    - Calling `plot_S_eta(load_summary())` directly returns an Axes with `len(ax.containers) == 6`.
    - Calling `plot_va_vs_S(load_summary())` directly returns an Axes whose scattered points, when grouped by the row's `rho` field, resolve to exactly 3 distinct `RHO_COLORS` values used across the plotted collections (`len({c for pc in ax.collections for c in map(tuple, pc.get_facecolors())}) == 3` or equivalent) -- proving the 3 densities are genuinely color-distinguished, not collapsed into one series.
    - Re-running `python3 python/analyze.py` a second time is idempotent: it overwrites the same 3 PNG paths without error and without accumulating duplicate files in `data/plots/`.
  </acceptance_criteria>
  <done>TP2/data/plots/S_eta.png and TP2/data/plots/va_vs_S.png exist and are correctly shaped; TP2/python/analyze.py's main() now produces all three of this plan's plots from a single invocation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| `TP2/data/sweep/summary.csv` / per-seed scalar logs -> `analyze.py` | Locally generated by our own `sweep.py` subprocess batch; not externally supplied or network-sourced, but a partially-failed run could leave malformed/truncated rows |
| `analyze.py` -> `tp2` subprocess (indirectly, via `sweep.py` reuse in Task 1's full sweep run) | Local binary invocation with internally-constructed numeric args, no external/user input surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Tampering | `load_summary()` reading `summary.csv` | low | accept | File is produced by our own trusted `sweep.py` run in the same task; malformed numeric fields raise `ValueError` on `float()`/`int()` cast rather than silently corrupting a plot |
| T-04-02 | Denial of Service | Task 1's full-sweep `multiprocessing.Pool` batch | low | accept | Each `tp2` subprocess is already bounded by `sweep.py`'s existing `RUN_TIMEOUT_S=300` and per-combination failure isolation (proven in Phase 3); a hung or failing combination cannot abort the batch |
| T-04-03 | Information Disclosure | N/A | low | accept | Offline academic deliverable; no secrets, credentials, or network exposure anywhere in this task |

</threat_model>

<verification>
- `python3 python/analyze.py` (single invocation, no flags) regenerates `va_eta.png`, `S_eta.png`, and `va_vs_S.png` from the real `summary.csv` without error.
- `TP2/data/sweep/summary.csv` is the genuine full-sweep dataset (row count and model/rho coverage confirmed in Task 1's acceptance criteria), not the Phase 3 smoke test file.
- Every plot correctly overlays both models (solid vicsek / dashed vicsek-style linestyle convention established here) and all 3 densities, confirmed structurally via `ax.containers`/`ax.collections` introspection, not just visual inspection.
</verification>

<success_criteria>
- ROADMAP Phase 4 Success Criterion 3 (partial): "va(eta) y S(eta) con barras de error genuinas (multi-semilla) muestran un cruce orden-desorden reconocible para las tres densidades, con el modelo estandar y el votante superpuestos" is satisfied by `plot_va_eta`/`plot_S_eta`.
- ROADMAP Phase 4 Success Criterion 4 (partial): "El grafico va vs S distingue las tres densidades" is satisfied by `plot_va_vs_S`.
- The underlying data dependency every other Phase 4 plan needs (`TP2/data/sweep/summary.csv`, real, not smoke-test) exists on disk.
</success_criteria>

<output>
Create `.planning/phases/04-an-lisis-gr-ficos-y-animaci-n/04-01-SUMMARY.md` when done.
</output>

## Artifacts this phase produces (this plan's contribution)

- `TP2/data/sweep/summary.csv` -- real full-sweep dataset (regenerated by running `python3 python/sweep.py`, not new code)
- `TP2/python/analyze.py` (new file) -- module constants `TP2_DIR`, `SWEEP_SUMMARY_CSV`, `PLOTS_DIR`, `COLOR_VICSEK`, `COLOR_VOTER`, `LINESTYLE_VICSEK`, `LINESTYLE_VOTER`, `RHO_COLORS`, `RHO_MARKERS`; functions `load_summary()`, `plot_va_eta()`, `plot_S_eta()`, `plot_va_vs_S()`, `main()`; CLI flags `--show`, `--summary`
- `TP2/data/plots/va_eta.png`
- `TP2/data/plots/S_eta.png`
- `TP2/data/plots/va_vs_S.png`