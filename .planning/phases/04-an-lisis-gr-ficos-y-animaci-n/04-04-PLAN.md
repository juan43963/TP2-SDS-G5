---
phase: 04-an-lisis-gr-ficos-y-animaci-n
plan: 04
type: execute
wave: 3
depends_on: ["04-01", "04-03"]
files_modified:
  - TP2/python/analyze.py
autonomous: true
requirements: [VIZ-02, VIZ-04, VIZ-07]

estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "va(t) and S(t) plots exist for all 3 densities x both models (12 PNGs total), each with a vertical line at the exact same steady-state cutoff index sweep.py's summarize_run() uses"
    - "The vertical line's post-cutoff window mean matches sweep.summarize_run()'s returned (va_mean,S_mean) for that same log file within floating-point tolerance"
    - "VIZ-07 is satisfied by two complementary mechanisms: per-model overlay on one axes (04-01/04-03) and per-model-repeated separate artifacts (this plan's 12 PNGs plus 04-02's 2 GIFs) -- both verified explicitly"
  artifacts:
    - "TP2/data/plots/va_t_{model}_rho{rho}.png (x6)"
    - "TP2/data/plots/S_t_{model}_rho{rho}.png (x6)"
  key_links:
    - "analyze.py steady_state_index() imports sweep.STEADY_STATE_FRACTION (never re-derives it) -> plot_scalar_timeseries() vertical line placement"
    - "pick_representative_eta() -> sweep.derive_seed()/sweep_output_path() -> the exact per-seed scalar log file 04-01's full sweep run produced"
---

<objective>
Add the final Phase 4 static-plot artifacts to `TP2/python/analyze.py`: va(t) and S(t), each with a vertical line marking steady-state onset using the EXACT SAME fixed-cutoff detector `sweep.py`'s `summarize_run()` uses, produced for all 3 densities and BOTH models (12 PNGs). This is the last analyze.py plan; it also finalizes `main()` to regenerate the complete Phase 4 static-artifact set from one invocation and explicitly verifies VIZ-07 coverage across the whole phase.

Purpose: ROADMAP Success Criterion 2 requires the steady-state vertical line to coincide with "el detector programatico usado en la Fase 3" -- this plan proves that numerically, not just visually. CONTEXT.md's explicit sequencing ("primero vicsek, luego voter") is followed literally: Task 1 proves the mechanism on vicsek alone, Task 2 extends it to voter.
Output: 12 PNGs under `TP2/data/plots/` (`va_t_{model}_rho{rho}.png`, `S_t_{model}_rho{rho}.png` for model in {vicsek,voter}, rho in {2,4,8}); `analyze.py`'s `main()` finalized as the single entrypoint for every Phase 4 static plot.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-an-lisis-gr-ficos-y-animaci-n/04-CONTEXT.md

@TP2/python/analyze.py
@TP2/python/sweep.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: steady_state_index() + read_scalar_log() + plot_scalar_timeseries() -- vicsek only</name>
  <files>TP2/python/analyze.py</files>
  <precondition>TP2/data/sweep/ contains the real per-seed scalar log files 04-01's full sweep run produced (not just summary.csv) -- required to locate the raw (t,va,S) time series for the representative points plotted here.</precondition>
  <read_first>
    TP2/python/analyze.py (existing constants/`load_summary()`/plot functions -- reuse `RHO_COLORS`, `PLOTS_DIR`)
    TP2/python/sweep.py (`STEADY_STATE_FRACTION = 0.5`; `summarize_run(log_path, steady_fraction)`'s exact cutoff formula `cutoff = int(len(rows) * steady_fraction)`, `window = rows[cutoff:] if cutoff < len(rows) else rows[-1:]`; `derive_seed(rho, eta, model, repeat_index)`; `sweep_output_path(model, rho, eta, seed)`)
    .planning/phases/04-an-lisis-gr-ficos-y-animaci-n/04-CONTEXT.md ("la linea vertical ... reimplementa en Python el mismo criterio de corte fijo ... que usa summarize_run, documentado una sola vez y reusado")
  </read_first>
  <action>
    Import `STEADY_STATE_FRACTION`, `summarize_run`, `derive_seed`, `sweep_output_path` from `sweep` alongside `analyze.py`'s existing `sweep` imports (`L_DEFAULT` etc.) -- these are imported, never re-derived, so there is structurally one source for the steady-state cutoff formula (per CONTEXT.md's explicit instruction to avoid drift).
    Implement `steady_state_index(n_rows, fraction=STEADY_STATE_FRACTION) -> int`: returns `int(n_rows * fraction)`, textually identical to the cutoff line inside `sweep.summarize_run`.
    Implement `read_scalar_log(path) -> list[tuple[float,float,float]]`: reads the space-separated `t va S` scalar-log format (one row per line, no header -- the same format `sweep.py`'s `run_one`/`--scalar-log` writes) into a list of `(t,va,S)` float triples.
    Implement `pick_representative_eta(rows_summary, rho, model) -> float`: filters `rows_summary` to matching `(rho, model)`, sorted by `eta` ascending; returns the smallest `eta` whose `va_mean >= 0.8` (a clearly-ordered state with a visible convergence transient, not the trivial `eta=0` case); if no row in the group reaches that threshold, falls back to the `eta` of the row with the maximum `va_mean` in that group. Deterministic and reproducible -- sourced only from `summary.csv` data already loaded.
    Implement `plot_scalar_timeseries(rho, model, column, rows_summary, out_path=None) -> Axes`: `column` is the literal string `"va"` or `"S"`. Resolves `eta = pick_representative_eta(rows_summary, rho, model)`, `seed = derive_seed(rho, eta, model, 0)` (`repeat_index=0`, matching `sweep.py`'s own formula exactly), `log_path = sweep_output_path(model, rho, eta, seed)`. Calls `read_scalar_log(log_path)` to get the full series; computes `cutoff = steady_state_index(len(series))`; plots the full `t` vs `column` series as a line (color `RHO_COLORS[rho]`); draws `ax.axvline` at `t = series[cutoff][0] if cutoff < len(series) else series[-1][0]` (mirrors `summarize_run`'s own `rows[-1:]` edge-case fallback) labeled "estado estacionario"; sets title `f"{column}(t) -- {model} rho={rho:g} eta={eta:.4f}"`; saves to `out_path` (default `PLOTS_DIR / f"{column}_t_{model}_rho{rho:g}.png"`). Returns the Axes.
    Wire into `main()`: after `04-03`'s `write_eta_c_table(table)` call, add a loop `for rho in (2.0, 4.0, 8.0): for column in ("va", "S"): plot_scalar_timeseries(rho, "vicsek", column, rows)` (voter is added in Task 2, not here).
  </action>
  <verify>
    <automated>wsl.exe -- bash -lc "cd '/mnt/c/Users/Lucas Di Candia/Desktop/SDS/TP2-SDS-G5/TP2' && python3 python/analyze.py && test -s data/plots/va_t_vicsek_rho2.png && test -s data/plots/S_t_vicsek_rho8.png && echo VICSEK_TIMESERIES_OK"</automated>
  </verify>
  <acceptance_criteria>
    - All 6 vicsek timeseries PNGs exist and are non-empty: `va_t_vicsek_rho{2,4,8}.png`, `S_t_vicsek_rho{2,4,8}.png`.
    - Numeric parity check (run via `python3 -c` inside `TP2/python/`): for `rho=2.0, model="vicsek"`, compute `eta = pick_representative_eta(rows, 2.0, "vicsek")`, `seed = derive_seed(2.0, eta, "vicsek", 0)`, `log_path = sweep_output_path("vicsek", 2.0, eta, seed)`; call `sweep.summarize_run(log_path)` to get `(va_mean_expected, S_mean_expected)`; separately call `read_scalar_log(log_path)`, `cutoff = steady_state_index(len(series))`, compute `statistics.mean(r[1] for r in series[cutoff:])` and `statistics.mean(r[2] for r in series[cutoff:])`; assert both match `(va_mean_expected, S_mean_expected)` within `1e-9` -- this proves the vertical line's window is the exact same window Phase 3's detector used.
  </acceptance_criteria>
  <done>steady_state_index(), read_scalar_log(), pick_representative_eta(), and plot_scalar_timeseries() are wired into analyze.py's main() for the vicsek model across all 3 densities; the steady-state cutoff is numerically proven identical to sweep.summarize_run()'s.</done>
</task>

<task type="auto">
  <name>Task 2: Extend to voter model + finalize main() + verify VIZ-07 coverage across the whole phase</name>
  <files>TP2/python/analyze.py</files>
  <read_first>
    TP2/python/analyze.py (Task 1's plot_scalar_timeseries()/main() loop -- extend, do not duplicate)
    .planning/phases/04-an-lisis-gr-ficos-y-animaci-n/04-CONTEXT.md ("Casos caracteristicos: ... primero para el modelo vicsek, luego repetido para voter (cubriendo VIZ-07)")
  </read_first>
  <action>
    Extend `main()`'s Task-1 loop from iterating only `"vicsek"` to iterating `("vicsek", "voter")`, so `plot_scalar_timeseries` is called for both models across all 3 densities and both columns (12 total PNGs).
    Add a final summary print at the end of `main()` listing every artifact path the complete `analyze.py` run produces (the accumulated set from `04-01`, `04-03`, and this plan: `va_eta.png`, `S_eta.png`, `va_vs_S.png`, `chi_eta.png`, `eta_c_table.csv`, plus the 12 timeseries PNGs -- 16 PNGs + 1 CSV = 17 files), so a single `python3 python/analyze.py` invocation is confirmed to regenerate the complete Phase 4 static-artifact set end-to-end.
    Do not modify `TP2/python/animate.py` or `TP2/python/sweep.py` in this task.
  </action>
  <verify>
    <automated>wsl.exe -- bash -lc "cd '/mnt/c/Users/Lucas Di Candia/Desktop/SDS/TP2-SDS-G5/TP2' && python3 python/analyze.py >/dev/null && find data/plots -name '*.png' | wc -l && find data/plots -name '*.csv' | wc -l"</automated>
  </verify>
  <acceptance_criteria>
    - The 6 additional voter timeseries PNGs exist and are non-empty: `va_t_voter_rho{2,4,8}.png`, `S_t_voter_rho{2,4,8}.png`.
    - `find data/plots -name '*.png' | wc -l` reports exactly 16 (`va_eta`, `S_eta`, `va_vs_S`, `chi_eta` = 4, plus 12 timeseries PNGs). `find data/plots -name '*.csv' | wc -l` reports exactly 1 (`eta_c_table.csv`). Animation GIFs are produced by the separate `animate.py` script (04-02), not counted here.
    - VIZ-07 coverage is explicitly confirmed, not assumed: (a) `va_eta.png`/`S_eta.png`/`va_vs_S.png`/`chi_eta.png` each overlay both models on one Axes (`len(ax.containers) == 6` or equivalent, already proven in 04-01/04-03's own acceptance criteria); (b) the 12 timeseries PNGs here and the 2 `animate.py` GIFs from 04-02 each exist once per model separately (`va_t_vicsek_*` and `va_t_voter_*` both present, `animation_vicsek_rho2.gif` and `animation_voter_rho2.gif` both present) -- together (a)+(b) cover VIZ-01 through VIZ-06 for both models, satisfying VIZ-07 literally rather than by assumption. Run: `python3 -c "from pathlib import Path; p=Path('data/plots'); required=['va_eta.png','S_eta.png','va_vs_S.png','chi_eta.png','animation_vicsek_rho2.gif','animation_voter_rho2.gif']+[f'{c}_t_{m}_rho{r:g}.png' for c in ('va','S') for m in ('vicsek','voter') for r in (2.0,4.0,8.0)]; missing=[f for f in required if not (p/f).exists()]; assert not missing, missing; print('VIZ07_COMPLETE', len(required))"` prints `VIZ07_COMPLETE 20` with no missing files.
  </acceptance_criteria>
  <done>All 12 timeseries PNGs exist (both models, all 3 densities, both observables); a single python3 python/analyze.py invocation regenerates the complete 16-PNG + 1-CSV static artifact set; VIZ-07 coverage is verified file-by-file across all of Phase 4's plot-producing plans, not assumed from the overlay design alone.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Per-seed scalar log files (`TP2/data/sweep/{model}/rho{rho}/eta{eta}/seed{seed}.txt`) -> `read_scalar_log()` | Locally generated by 04-01's sweep run; not externally supplied |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Tampering | `read_scalar_log()` parsing per-seed scalar logs | low | accept | Files are produced by our own trusted `sweep.py` run in 04-01; a malformed row raises `ValueError` on `float()` cast rather than silently corrupting a plot |
| T-04-02 | Repudiation | Choice of representative eta per (rho,model) | low | accept | `pick_representative_eta()` is a pure, deterministic function of already-generated `summary.csv` data -- fully reproducible, no hidden state |

</threat_model>

<verification>
- `python3 python/analyze.py` (single invocation, no flags) regenerates all 16 PNGs and the 1 CSV from `04-01`, `04-03`, and this plan combined.
- The steady-state vertical line's window is numerically proven identical to `sweep.summarize_run()`'s window for the same log file.
- VIZ-07 coverage is verified file-by-file across the whole phase's artifact set (this plan's Task 2 acceptance criteria), not asserted narratively.
</verification>

<success_criteria>
- ROADMAP Phase 4 Success Criterion 2: "Los graficos va(t) y S(t) muestran una linea vertical en el inicio del estado estacionario, coincidente con el detector programatico usado en la Fase 3" is satisfied and numerically proven.
- ROADMAP Phase 4 Success Criterion 5: "Los 6 tipos de grafico requeridos existen para ambos modelos (estandar y votante), con comparaciones visibles entre ambos" is satisfied and explicitly file-checked across the full phase.
</success_criteria>

<output>
Create `.planning/phases/04-an-lisis-gr-ficos-y-animaci-n/04-04-SUMMARY.md` when done.
</output>

## Artifacts this phase produces (this plan's contribution)

- `TP2/python/analyze.py` (finalized) -- functions `steady_state_index()`, `read_scalar_log()`, `pick_representative_eta()`, `plot_scalar_timeseries()`; `main()` now the single entrypoint for all 16 PNGs + 1 CSV of Phase 4's static plots
- `TP2/data/plots/va_t_vicsek_rho2.png`, `va_t_vicsek_rho4.png`, `va_t_vicsek_rho8.png`
- `TP2/data/plots/va_t_voter_rho2.png`, `va_t_voter_rho4.png`, `va_t_voter_rho8.png`
- `TP2/data/plots/S_t_vicsek_rho2.png`, `S_t_vicsek_rho4.png`, `S_t_vicsek_rho8.png`
- `TP2/data/plots/S_t_voter_rho2.png`, `S_t_voter_rho4.png`, `S_t_voter_rho8.png`