---
phase: 04-an-lisis-gr-ficos-y-animaci-n
plan: 03
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - TP2/python/analyze.py
autonomous: true
requirements: [PLUS-01, PLUS-03]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "chi(eta) = N * va_std^2 (N = round(rho * L^2), L=10) is computed directly from summary.csv rows already loaded by load_summary(), with no reopening of per-seed raw scalar log files"
    - "eta_c_table.csv has exactly one row per (model,rho) combination present in summary.csv, each eta_c being the eta of that group's maximum chi over the already-sampled grid (no interpolation between grid points)"
  artifacts:
    - TP2/data/plots/chi_eta.png
    - TP2/data/plots/eta_c_table.csv
  key_links:
    - "compute_chi() reusing summary.csv's va_std column (via load_summary(), already established in 04-01) -> compute_eta_c_table() argmax-per-group -> write_eta_c_table()"
---

<objective>
Add the two differentiator artifacts to `TP2/python/analyze.py`: the susceptibility curve chi(eta) and the eta_c(rho) comparison table, both derived purely from the already-generated `summary.csv` replicate statistics (no raw per-seed data, no new simulation runs).

Purpose: PLUS-01 and PLUS-03 are explicitly free byproducts of the multi-seed replicate data `04-01`'s sweep run already produced (`va_std` per (model,rho,eta) point is exactly what chi needs) -- this plan wires that reuse instead of recomputing anything from scratch.
Output: `TP2/data/plots/chi_eta.png`, `TP2/data/plots/eta_c_table.csv`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-an-lisis-gr-ficos-y-animaci-n/04-CONTEXT.md

@TP2/python/analyze.py
@TP2/python/sweep.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: compute_chi() + plot_chi_eta()</name>
  <files>TP2/python/analyze.py</files>
  <read_first>
    TP2/python/analyze.py (existing `load_summary()`, `plot_va_eta()`/`plot_S_eta()` as the pattern to mirror: group-by-(model,rho), sort by eta, use `RHO_COLORS`/`LINESTYLE_VICSEK`/`LINESTYLE_VOTER`/`RHO_MARKERS`, return the Axes; `main()`'s current call sequence)
    TP2/python/sweep.py (`L_DEFAULT = 10.0` -- already importable, reuse rather than hardcoding 10.0 a second time)
    .planning/phases/04-an-lisis-gr-ficos-y-animaci-n/04-CONTEXT.md (PLUS-01: "chi(eta) = N*va_std^2 calculado directamente desde summary.csv ... con N = round(rho*L^2), L=10 fijo -- no requiere reabrir datos crudos por semilla")
  </read_first>
  <action>
    Import `L_DEFAULT` from `sweep` alongside `analyze.py`'s existing `sweep` imports (if not already imported).
    Implement `compute_chi(rows) -> list[dict]`: returns a NEW list where each element is a shallow copy of the corresponding input row's dict plus one added key `"chi"`, computed as `N * (row["va_std"] ** 2)` with `N = round(row["rho"] * L_DEFAULT ** 2)` -- matching `TP2/src/main.cpp`'s own `N = round(rho * L * L)` formula exactly (`L_DEFAULT` fixed at 10.0). Does not mutate the input `rows` list.
    Implement `plot_chi_eta(rows_with_chi, out_path=None) -> Axes`: same grouping/sorting/color/linestyle/marker structure as `plot_va_eta` (group by (model,rho), sort by eta, one line per group, `RHO_COLORS[rho]`, `LINESTYLE_VICSEK`/`LINESTYLE_VOTER`, `RHO_MARKERS[rho]`, label `f"{model} rho={rho:g}"`) but plots `chi` on the y-axis with NO error bars (chi is itself a derived point estimate from `va_std`, `summary.csv` carries no std-of-chi), a plain `ax.plot` line rather than `ax.errorbar`. xlabel "eta", ylabel "chi = N*va_std^2", saved to `out_path` (default `PLOTS_DIR/"chi_eta.png"`). Returns the Axes.
    Wire into `main()`: after the existing `plot_va_vs_S(rows)` call, add `rows_chi = compute_chi(rows)` then `plot_chi_eta(rows_chi)`; print the output path.
  </action>
  <verify>
    <automated>wsl.exe -- bash -lc "cd '/mnt/c/Users/Lucas Di Candia/Desktop/SDS/TP2-SDS-G5/TP2' && python3 python/analyze.py && test -s data/plots/chi_eta.png && echo CHI_OK"</automated>
  </verify>
  <acceptance_criteria>
    - `data/plots/chi_eta.png` exists, is non-empty, and exceeds 300x200px.
    - Numeric cross-check (run via `python3 -c` inside `TP2/python/`): `rows = load_summary(); rows_chi = compute_chi(rows)`; pick any row with `rho == 2.0` and `model == "vicsek"`; assert `abs(picked_chi_row["chi"] - 200 * picked_row["va_std"]**2) < 1e-9` (N=round(2.0*100)=200) -- proves the exact formula, not an approximation.
    - `compute_chi(rows)` does not mutate its input: `original_rows == rows` (by value, e.g. `"chi" not in rows[0]`) still holds after the call.
  </acceptance_criteria>
  <done>compute_chi() and plot_chi_eta() are wired into analyze.py's main(); TP2/data/plots/chi_eta.png exists and its values are numerically verified against the chi=N*va_std^2 formula.</done>
</task>

<task type="auto">
  <name>Task 2: compute_eta_c_table() + write_eta_c_table()</name>
  <files>TP2/python/analyze.py</files>
  <read_first>
    TP2/python/analyze.py (Task 1's compute_chi(); existing `main()` structure)
    TP2/python/sweep.py (`aggregate_to_csv`/`write_failures_csv`'s `csv.DictWriter` + fixed-`fieldnames` + `writeheader()` convention -- reuse this exact pattern)
    .planning/phases/04-an-lisis-gr-ficos-y-animaci-n/04-CONTEXT.md (PLUS-03: "eta correspondiente al maximo de chi(eta) por (rho, modelo) sobre la grilla ya muestreada ... no se hace un ajuste/interpolacion adicional"; output path `TP2/data/plots/eta_c_table.csv`, columns `model,rho,eta_c`)
  </read_first>
  <action>
    Define `ETA_C_TABLE_CSV = PLOTS_DIR / "eta_c_table.csv"` as a module-level constant alongside `analyze.py`'s existing path constants.
    Implement `compute_eta_c_table(rows_with_chi) -> list[dict]`: groups `rows_with_chi` by `(model, rho)`; for each group, selects the row with the maximum `"chi"` value (ties broken by the smallest `eta` among tied rows, for determinism); returns a list of `{"model": ..., "rho": ..., "eta_c": <that row's eta>}` dicts, one per group, sorted by `(model, rho)`. This is a pure argmax over the already-sampled grid -- no interpolation, no curve fitting.
    Implement `write_eta_c_table(table, out_path=ETA_C_TABLE_CSV) -> None`: opens `out_path` (creating `PLOTS_DIR` if needed) and writes via `csv.DictWriter` with `fieldnames = ["model", "rho", "eta_c"]` and `writeheader()`, mirroring `sweep.py`'s `aggregate_to_csv`/`write_failures_csv` convention exactly.
    Wire into `main()`: after `plot_chi_eta(rows_chi)`, add `table = compute_eta_c_table(rows_chi)` then `write_eta_c_table(table)`; print the output path and the number of rows written.
  </action>
  <verify>
    <automated>wsl.exe -- bash -lc "cd '/mnt/c/Users/Lucas Di Candia/Desktop/SDS/TP2-SDS-G5/TP2' && python3 python/analyze.py && test -s data/plots/eta_c_table.csv && echo ETA_C_OK"</automated>
  </verify>
  <acceptance_criteria>
    - `data/plots/eta_c_table.csv` exists and has a header row plus exactly one data row per distinct (model,rho) combination present in `data/sweep/summary.csv`. Verify via: `python3 -c "import csv; summary_combos = {(r['model'], r['rho']) for r in csv.DictReader(open('data/sweep/summary.csv'))}; table_rows = list(csv.DictReader(open('data/plots/eta_c_table.csv'))); table_combos = {(r['model'], r['rho']) for r in table_rows}; assert summary_combos == table_combos, (summary_combos, table_combos); assert len(table_rows) == len(table_combos); print('ETA_C_TABLE_MATCH', len(table_rows))"` prints `ETA_C_TABLE_MATCH 6` (3 densities x 2 models) with no assertion error -- this is the exact "eta_c_table.csv has a row per (model,rho) combination present in summary.csv" check.
    - Every `eta_c` value in the table is one of the actual `eta` values present in `summary.csv` for that (model,rho) group (verified by set-membership, e.g. `float(row["eta_c"]) in {float(r["eta"]) for r in summary_rows if r["model"]==row["model"] and r["rho"]==row["rho"]}`) -- proves no interpolation was performed.
  </acceptance_criteria>
  <done>eta_c_table.csv exists with model,rho,eta_c columns, exactly one row per (model,rho) combination in summary.csv, each eta_c a genuine grid point (argmax of chi, no interpolation).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| `summary.csv` (already-loaded `rows`) -> `compute_chi()`/`compute_eta_c_table()` | Same locally-generated, already-validated data source as 04-01; no new external input surface introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Tampering | `compute_chi()`/`compute_eta_c_table()` arithmetic over already-validated `rows` | low | accept | Operates on the same `rows` list `04-01`'s `load_summary()` already cast to numeric types; no new file I/O beyond writing `eta_c_table.csv` |
| T-04-02 | Repudiation | `eta_c_table.csv` provenance | low | accept | Fully regeneratable byte-for-byte from `summary.csv` via a pure function; no audit-trail requirement for this academic deliverable |

</threat_model>

<verification>
- `python3 python/analyze.py` (single invocation) now additionally produces `chi_eta.png` and `eta_c_table.csv` alongside `04-01`'s three plots.
- chi values are numerically verified against the exact `N*va_std^2` formula.
- `eta_c_table.csv` row-for-row matches the (model,rho) combinations in `summary.csv`, with every `eta_c` a genuine sampled grid point.
</verification>

<success_criteria>
- ROADMAP Phase 4 Success Criterion 4 (partial): "la susceptibilidad chi(eta) y la tabla comparativa de eta_c(rho) se derivan de los mismos datos de replicas ya generados" is satisfied -- both derive purely from `summary.csv`'s already-computed `va_std` column, no new simulation runs.
</success_criteria>

<output>
Create `.planning/phases/04-an-lisis-gr-ficos-y-animaci-n/04-03-SUMMARY.md` when done.
</output>

## Artifacts this phase produces (this plan's contribution)

- `TP2/python/analyze.py` (extended) -- constant `ETA_C_TABLE_CSV`; functions `compute_chi()`, `plot_chi_eta()`, `compute_eta_c_table()`, `write_eta_c_table()`
- `TP2/data/plots/chi_eta.png`
- `TP2/data/plots/eta_c_table.csv`
