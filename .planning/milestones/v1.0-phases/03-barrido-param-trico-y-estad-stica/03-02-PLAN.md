---
phase: 03-barrido-param-trico-y-estad-stica
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - TP2/python/sweep.py
autonomous: true
requirements: [SWEEP-01, SWEEP-02, SWEEP-03]

estimate:
  tokens: 44000
  raw_tokens: 44000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "explore_transition(model, rho) runs a coarse, low-resolution eta grid (K=1-2 seeds, a few hundred steps) and returns the bracket [eta_low, eta_high] where mean steady-state va first crosses below VA_THRESHOLD, located independently per (model, rho) pair (SWEEP-02)."
    - "build_eta_grid(eta_low, eta_high) returns a sorted grid combining the fixed coarse global points (0..2*pi) with additional fine points inside the detected transition bracket -- finer resolution near the order-disorder transition than elsewhere in the range."
    - "run_sweep(tasks, ...) executes every (model,rho,eta,repeat_index) combination across a multiprocessing.Pool, and a single failing combination (tp2 exiting non-zero) is captured into the returned failures list rather than aborting the pool or discarding the other tasks' results."
    - "aggregate_to_csv writes columns model,rho,eta,va_mean,va_std,S_mean,S_std,n_seeds -- one row per (model,rho,eta) point, mean and sample standard deviation computed across that point's K seed results."
    - "The default CLI sweeps rho in {2,4,8} x both models x K=5 seeds per point (SWEEP-01/SWEEP-03 defaults), and a run containing one or more failed combinations still produces a complete summary.csv for every combination that succeeded, plus a persisted failures.csv naming exactly which combinations failed and why."
  artifacts:
    - path: "TP2/python/sweep.py"
      provides: "build_coarse_eta_grid, explore_transition(model,rho,k_explore,steps_explore,va_threshold), build_eta_grid(eta_low,eta_high), _run_and_summarize (picklable multiprocessing worker), run_sweep(tasks,steps,workers), aggregate_to_csv(results,csv_path), write_failures_csv(failures,csv_path), finalized main() CLI (argparse: --rhos, --models, --k-seeds, --steps, --k-explore, --steps-explore, --workers, --out, --selftest)"
    - path: "TP2/data/sweep/summary.csv"
      provides: "final aggregated CSV output of a full sweep run (runtime artifact, gitignored, not committed) -- consumed by Phase 4's plotting scripts"
    - path: "TP2/data/sweep/failures.csv"
      provides: "runtime artifact listing any (model,rho,eta,seed,error) combinations that failed during the sweep, written only when failures occurred"
  key_links:
    - from: "sweep.py main()"
      to: "sweep.py explore_transition / build_eta_grid / run_sweep / aggregate_to_csv"
      via: "for each (model, rho) pair: explore_transition locates the transition bracket, build_eta_grid expands it into the point list, all (model,rho,eta,repeat_index) tuples are collected into one shared task list, one run_sweep() call processes the whole list, aggregate_to_csv(results, args.out) writes the summary"
      pattern: "run_sweep\\("
    - from: "sweep.py run_sweep"
      to: "sweep.py run_one / summarize_run (Plan 03-01)"
      via: "the multiprocessing worker _run_and_summarize calls run_one then summarize_run per task, converting a raised RuntimeError into a failure dict instead of letting it cross the process-pool boundary"
      pattern: "_run_and_summarize"
---

<objective>
Complete the sweep driver: locate the order-disorder transition per (model, rho) via a low-resolution exploratory mini-sweep, build the coarse-far/fine-near eta grid from that bracket, execute the full `{model x rho x eta x seed}` grid in parallel via `multiprocessing.Pool` with per-combination failure isolation, and aggregate the K-seed results into the report-ready summary CSV -- finishing what Plan 03-01's reproducibility core (`derive_seed`/`run_one`/`summarize_run`) was built to support.

Purpose: Closes SWEEP-01, SWEEP-02, SWEEP-03, and delivers the phase's remaining success criteria: the full parameter grid actually runs, K>=5 independent seeds back every plotted point (not single-run temporal fluctuation, RESEARCH.md Pitfall 7), and a run with a mid-sweep failure still produces usable output for everything that succeeded (CONTEXT.md's explicit "log and continue" requirement) instead of losing hours of an overnight sweep to one bad combination (RESEARCH.md Pitfall 4).
Output: `TP2/python/sweep.py` extended to a complete, runnable sweep driver -- `python3 python/sweep.py` (with no further code changes) runs the full phase-3 parameter sweep and produces `TP2/data/sweep/summary.csv`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-barrido-param-trico-y-estad-stica/03-CONTEXT.md
@.planning/phases/03-barrido-param-trico-y-estad-stica/03-01-SUMMARY.md
@.planning/research/ARCHITECTURE.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Eta-grid exploration + full parallel sweep executor + CSV aggregation, wired end-to-end through the CLI</name>
  <reversibility rating="reversible">The eta-grid resolution constants and VA_THRESHOLD are internal tuning knobs -- no external contract depends on their exact values, and they are freely re-tunable in a later run without invalidating already-aggregated summary.csv rows for other (model,rho) points.</reversibility>
  <read_first>
    TP2/python/sweep.py
    TP1/python/benchmark.py
    .planning/phases/03-barrido-param-trico-y-estad-stica/03-01-SUMMARY.md
    .planning/research/ARCHITECTURE.md
  </read_first>
  <files>TP2/python/sweep.py</files>
  <action>
    Add module-level constants to TP2/python/sweep.py: `COARSE_ETA_POINTS = 9` (9 points spanning `0..2*pi`, spacing `pi/4`), `FINE_ETA_POINTS = 8` (extra points inserted inside the detected transition bracket), `K_EXPLORE = 2`, `STEPS_EXPLORE = 500`, `VA_THRESHOLD = 0.5`, `DEFAULT_RHOS = [2.0, 4.0, 8.0]` (SWEEP-01), `DEFAULT_MODELS = ["vicsek", "voter"]`. Import `math`, `csv`, `multiprocessing` at module top (alongside Task 2's existing stdlib imports).

    Implement `build_coarse_eta_grid() -&gt; list[float]` returning `[i * (2.0 * math.pi) / (COARSE_ETA_POINTS - 1) for i in range(COARSE_ETA_POINTS)]`.

    Implement `explore_transition(model: str, rho: float, k_explore: int = K_EXPLORE, steps_explore: int = STEPS_EXPLORE, va_threshold: float = VA_THRESHOLD) -&gt; tuple[float, float]`: build `coarse = build_coarse_eta_grid()`; for each `eta` in `coarse`, run `k_explore` repeats (`repeat_index` 0..k_explore-1), each via `seed = derive_seed(rho, eta, model, repeat_index)` then `log_path = run_one(model, rho, eta, seed, steps=steps_explore)` then `va_mean, _ = summarize_run(log_path)`, and average the `k_explore` va means with `statistics.mean`; scan consecutive coarse points for the first `i` where `means[i] &gt;= va_threshold &gt; means[i + 1]` and return `(coarse[i], coarse[i + 1])`; if no crossing is found, return `(coarse[-2], coarse[-1])` (the highest-eta bracket, treated as the fully-disordered fallback).

    Implement `build_eta_grid(eta_low: float, eta_high: float) -&gt; list[float]`: `coarse = build_coarse_eta_grid()`; `fine = [eta_low + i * (eta_high - eta_low) / (FINE_ETA_POINTS - 1) for i in range(FINE_ETA_POINTS)]`; return `sorted({round(e, 6) for e in coarse + fine})` (deduped, sorted, rounded to avoid float-noise duplicates).

    Implement the picklable, module-level multiprocessing worker `_run_and_summarize(args: tuple) -&gt; dict`, where `args` unpacks to `(model, rho, eta, repeat_index, steps)`: compute `seed = derive_seed(rho, eta, model, repeat_index)`; inside a `try` block call `log_path = run_one(model, rho, eta, seed, steps=steps)` then `va_mean, s_mean = summarize_run(log_path)` and return `{"ok": True, "model": model, "rho": rho, "eta": eta, "seed": seed, "va_mean": va_mean, "S_mean": s_mean}`; on `except Exception as exc:` return `{"ok": False, "model": model, "rho": rho, "eta": eta, "seed": seed, "error": str(exc)}` -- this function must be module-level (never a nested closure/lambda) so `multiprocessing.Pool` can pickle it.

    Implement `run_sweep(tasks: list, steps: int = DEFAULT_STEPS, workers: int | None = None) -&gt; tuple[list, list]`: resolve `workers = workers or os.cpu_count() or 1`; build `args = [(model, rho, eta, repeat_index, steps) for (model, rho, eta, repeat_index) in tasks]`; run `with multiprocessing.Pool(workers) as pool: raw = pool.map(_run_and_summarize, args)`; split `results = [r for r in raw if r["ok"]]` and `failures = [r for r in raw if not r["ok"]]`; return `(results, failures)`.

    Implement `aggregate_to_csv(results: list, csv_path: Path) -&gt; None`: group `results` by `(model, rho, eta)` key into per-key lists of `va_mean`/`S_mean` values; create `csv_path.parent`; write via `csv.DictWriter` with `fieldnames=["model","rho","eta","va_mean","va_std","S_mean","S_std","n_seeds"]`, `writeheader()`, then one row per group sorted by key, where `n = len(group)`, `va_std`/`S_std` use `statistics.stdev(...)` when `n &gt;= 2` else `0.0` (sample stdev requires n&gt;=2), and `eta` is written with `f"{eta:.6f}"` formatting for reproducibility.

    Implement `write_failures_csv(failures: list, csv_path: Path) -&gt; None`: same `csv.DictWriter` pattern with `fieldnames=["model","rho","eta","seed","error"]`.

    Extend `main()`: add `argparse` arguments `--rhos` (`type=float, nargs="+", default=DEFAULT_RHOS`), `--models` (`nargs="+", default=DEFAULT_MODELS, choices=["vicsek","voter"]`), `--k-seeds` (`type=int, default=DEFAULT_K_SEEDS`), `--steps` (`type=int, default=DEFAULT_STEPS`), `--k-explore` (`type=int, default=K_EXPLORE`), `--steps-explore` (`type=int, default=STEPS_EXPLORE`), `--workers` (`type=int, default=None`), `--out` (`type=Path, default=SWEEP_DATA_DIR / "summary.csv"`), keeping the existing `--selftest` flag (still handled first, returning immediately). When not `--selftest`: guard `if not TP2_BIN.exists(): sys.exit(f"error: no existe {TP2_BIN}. Correr \`make\` primero.")` (mirrors `TP1/python/benchmark.py`'s own `os.path.exists(CIM)` guard); for each `model` in `args.models`, for each `rho` in `args.rhos`, call `explore_transition(model, rho, k_explore=args.k_explore, steps_explore=args.steps_explore)`, print a one-line progress report naming the model/rho/detected bracket, call `build_eta_grid(*bracket)`, and extend one shared `tasks` list with every `(model, rho, eta, repeat_index)` for `repeat_index in range(args.k_seeds)`; after the double loop, call `run_sweep(tasks, steps=args.steps, workers=args.workers)`, then `aggregate_to_csv(results, args.out)` and print a one-line summary (`"resumen: {args.out} ({len(results)} corridas OK)"`); if `failures` is non-empty, write them via `write_failures_csv(failures, args.out.parent / "failures.csv")` and print a warning line naming the failure count and the failures-file path.
  </action>
  <verify>
    <automated>wsl.exe -e bash -lc 'cd "$(wslpath "C:\Users\Lucas Di Candia\Desktop\SDS\TP2-SDS-G5\TP2")" &amp;&amp; make 2>&amp;1 | tail -5 &amp;&amp; python3 python/sweep.py --rhos 2 --models vicsek --k-seeds 2 --k-explore 1 --steps 200 --steps-explore 100 --out data/sweep/summary_smoke.csv &amp;&amp; cat data/sweep/summary_smoke.csv'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'def explore_transition' TP2/python/sweep.py`, `grep -c 'def build_eta_grid' TP2/python/sweep.py`, `grep -c 'def run_sweep' TP2/python/sweep.py`, and `grep -c 'def aggregate_to_csv' TP2/python/sweep.py` each return exactly 1.
    - `grep -c 'multiprocessing.Pool' TP2/python/sweep.py` returns exactly 1 (process-level parallelism per ARCHITECTURE.md's explicit "no in-engine threading" recommendation, a single pool construction site).
    - The smoke-run verify command exits 0; `data/sweep/summary_smoke.csv`'s first line equals exactly `model,rho,eta,va_mean,va_std,S_mean,S_std,n_seeds`; the file has at least 2 lines total (header plus at least one data row); every data row's first field equals `vicsek` and its last field (`n_seeds`) equals `2`.
  </acceptance_criteria>
  <done>`explore_transition`/`build_eta_grid`/`run_sweep`/`aggregate_to_csv` exist and are wired through `main()`'s CLI; a real small sweep invocation (one density, one model, reduced seeds/steps) runs end-to-end and produces a correctly-formatted, non-empty `summary.csv`.</done>
</task>

<task type="auto">
  <name>Task 2: Failure-isolation contract proof + failures.csv persistence + full-default-grid finalization</name>
  <reversibility rating="reversible">Adds selftest coverage and a diagnostics artifact (failures.csv) on top of Task 1's already-complete pipeline -- no change to the pipeline's core behavior.</reversibility>
  <read_first>
    TP2/python/sweep.py
    .planning/phases/03-barrido-param-trico-y-estad-stica/03-CONTEXT.md
  </read_first>
  <files>TP2/python/sweep.py</files>
  <action>
    Extend the `_selftest()` function already present in TP2/python/sweep.py (from Plan 03-01) with two more checks appended to the same function -- do not create a second, competing selftest entrypoint.

    Check A (pure, no `tp2` needed): call `build_eta_grid(1.0, 1.5)` and assert `grid[0] == 0.0` (the combined grid must include the coarse grid's low endpoint) and `abs(grid[-1] - 2.0 * math.pi) &lt; 1e-9` (and its high endpoint), and assert at least one returned value falls strictly inside `[1.0, 1.5]` and is NOT a member of `build_coarse_eta_grid()` (proving fine points were actually inserted, not just the coarse grid echoed back).

    Check B (real `tp2`, only when `TP2_BIN.exists()`): call `run_sweep([("vicsek", 2.0, 0.3, 0), ("vicsek", -1.0, 0.3, 0)], steps=20)` (one valid combination, one deliberately invalid `rho=-1.0`) and assert `len(results) == 1` and `len(failures) == 1` and `failures[0]["rho"] == -1.0` -- this is the runtime proof that one failing combination inside a `multiprocessing.Pool` batch is isolated into the `failures` list rather than crashing the pool or silently dropping the valid result alongside it.

    Print `"sweep.py selftest OK"` on success (keep this exact final line -- it is what downstream verify commands and Task 1's own selftest already check for).
  </action>
  <verify>
    <automated>wsl.exe -e bash -lc 'cd "$(wslpath "C:\Users\Lucas Di Candia\Desktop\SDS\TP2-SDS-G5\TP2")" &amp;&amp; python3 python/sweep.py --selftest &amp;&amp; python3 python/sweep.py --rhos 2 4 8 --models vicsek voter --k-seeds 2 --k-explore 1 --steps 150 --steps-explore 80 --out data/sweep/summary_smoke_full.csv 2>&amp;1 | tail -20 &amp;&amp; wc -l data/sweep/summary_smoke_full.csv'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'def write_failures_csv' TP2/python/sweep.py` returns exactly 1.
    - `grep -c '"rho2.0' TP2/python/sweep.py` returns 0 and `grep -c 'rho=-1.0' TP2/python/sweep.py` returns at least 1 (source assertion the deliberate-failure case from Check B is present).
    - The verify command's `--selftest` run exits 0 with final output line `sweep.py selftest OK`.
    - The full-default-grid smoke run (all 3 densities x both models, reduced seeds/steps for speed) exits 0 and produces `data/sweep/summary_smoke_full.csv` with strictly more than 1 line (header plus at least one data row per (model,rho) combination actually explored).
  </acceptance_criteria>
  <done>`_selftest()` proves `build_eta_grid` genuinely inserts fine points inside the detected bracket and that `run_sweep` isolates one failing combination from a mixed valid/invalid batch without losing the valid result; a full default-grid smoke invocation (all 3 densities, both models) runs end-to-end via the finalized CLI and produces a complete summary CSV.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `multiprocessing.Pool` worker processes spawned by `run_sweep` | Local child processes running the same `sweep.py`/`tp2` code the parent already trusts -- no new external input surface beyond Plan 03-01's `subprocess.run` boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-04 | Denial of Service | `run_sweep`'s `multiprocessing.Pool` processing the full `{model x rho x eta x seed}` grid | low | accept | Same local-resource-only rationale as Plan 03-01's T-03-03: unbounded `--rhos`/`--models`/`--k-seeds` only consumes the operator's own CPU/disk/time; per-task failure isolation (`_run_and_summarize`'s try/except returning `ok`/`fail` dicts, never raising across the pool boundary) additionally prevents one bad combination from leaving a stuck or leaked worker |
| T-03-05 | Repudiation | `failures.csv` / stdout failure summary | low | accept | Local diagnostic output only, no external audit requirement; every failure record includes `model,rho,eta,seed,error`, and SWEEP-04's deterministic seed derivation means any failing combination can always be reproduced standalone via `run_one` for debugging |
</threat_model>

<verification>
`cd TP2 && python3 python/sweep.py --selftest` exits 0 with final line `sweep.py selftest OK`, covering both plans' checks: reproducibility core (Plan 03-01), eta-grid construction, and mixed-batch failure isolation (this plan). A full-default-grid smoke invocation (`--rhos 2 4 8 --models vicsek voter`, reduced seeds/steps) runs end-to-end and produces a non-trivial `summary.csv`.
</verification>

<success_criteria>
The sweep covers rho in {2,4,8} x a finer-near-transition eta grid x {vicsek,voter} x K&gt;=5 seeds per point, each seed deterministically derived from (rho,eta,model,repeat_index) (phase success criterion 1). Sweep runs write only the scalar `(t,va,S)` log, never full trajectories (criterion 2, inherited from Plan 03-01). A documented fixed-cutoff steady-state window applies identically to va and S (criterion 3, inherited from Plan 03-01). The summary CSV aggregates every run into mean+std per (model,rho,eta) point, ready for Phase 4's plotting (criterion 4). A run with individual failures still completes and reports them, never aborting the whole sweep.
</success_criteria>

<output>
Create `.planning/phases/03-barrido-param-trico-y-estad-stica/03-02-SUMMARY.md` when done
</output>

## Artifacts This Phase Produces

Full manifest across both plans in Phase 3 (Barrido Paramétrico y Estadística):

**From Plan 01 (03-01-PLAN.md):**
- `TP2/src/main.cpp` (modified) -- `--scalar-log <path>` CLI flag; per-step `(t va S)` scalar log write with an explicit `sim.syncNeighbors()` resync before each S computation; `--out`'s existing behavior unchanged
- `TP2/python/sweep.py` (new) -- `derive_seed`, `sweep_output_path`, `run_one`, `summarize_run`, `_selftest()`, reproducibility-core module constants

**From Plan 02 (this plan):**
- `TP2/python/sweep.py` (extended) -- `build_coarse_eta_grid`, `explore_transition`, `build_eta_grid`, `_run_and_summarize`, `run_sweep`, `aggregate_to_csv`, `write_failures_csv`, finalized `main()` CLI with `--rhos`/`--models`/`--k-seeds`/`--steps`/`--k-explore`/`--steps-explore`/`--workers`/`--out`
- `TP2/data/sweep/{model}/rho{rho}/eta{eta}/seed{seed}.txt` (runtime artifact, per-run scalar log, gitignored)
- `TP2/data/sweep/summary.csv` (runtime artifact, final aggregated output, gitignored) -- the direct input Phase 4's plotting scripts consume
- `TP2/data/sweep/failures.csv` (runtime artifact, written only when failures occur, gitignored)

**Explicitly out of scope for Phase 3** (deferred to Phase 4 per REQUIREMENTS.md VIZ-01..07/PLUS-01..03): reading `summary.csv` to produce any plot, the animation module, susceptibility chi(eta), and the eta_c(rho) comparison table. This phase's `summary.csv` schema (`model,rho,eta,va_mean,va_std,S_mean,S_std,n_seeds`) is the exact, frozen input contract Phase 4's analysis scripts are expected to read -- no rework expected there, only a new consumer.
</output>
