---
phase: 05-benchmark-y-entregables
plan: 04
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - package_tp2.py
  - TP2_codigo.zip
  - .gitignore
autonomous: true
requirements: [DELIV-03]

estimate:
  tokens: 1300
  raw_tokens: 1300
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "TP2_codigo.zip contiene exactamente TP2/src/** (todos los .cpp/.h reales), TP2/Makefile y los 4 scripts TP2/python/*.py (sweep.py, analyze.py, animate.py, benchmark.py) -- ningun otro archivo"
    - "TP2_codigo.zip no contiene TP2/data/, TP2/build/, TP2/tp2, TP2/tp2_test, .git, __pycache__, TP2/informe/ ni TP2/presentacion/"
    - "Un chequeo automatico advierte (sin bloquear) si el .zip supera ~500KB"
  artifacts:
    - package_tp2.py
    - TP2_codigo.zip
  key_links:
    - "package_tp2.py se ejecuta despues de que el Plan 05-01 crea TP2/python/benchmark.py, para que el zip incluya los 4 scripts .py completos"
---

<objective>
Empaquetar unicamente la version final del motor de TP2 (`TP2/src/`, `TP2/Makefile`, `TP2/python/*.py`) en `TP2_codigo.zip` en la raiz del repositorio, sin datos/historial/binarios/documentos, con un chequeo automatico de tamano (~500KB), satisfaciendo DELIV-03.

Purpose: El enunciado exige "el codigo fuente implementado en un archivo *.zip. Solo version final del motor de simulacion (Tamano del archivo del orden de los kb. No adjuntar historial, documentos, output de simulaciones, etc.)".
Output: `package_tp2.py` (raiz del repo), `TP2_codigo.zip` (raiz del repo).

Reversibilidad: el zip es regenerable en cualquier momento por rerun del script; no hay decisiones de una via -- no se requieren checkpoints.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-benchmark-y-entregables/05-CONTEXT.md
@TP2/Makefile

**Nota de ejecucion (worktree):** este plan depende de que TP2/python/benchmark.py (creado por el Plan 05-01, Wave 1) ya exista, para que el .zip incluya los 4 scripts .py completos. No depende de ningun dato gitignoreado de Fase 3/4 -- solo empaqueta codigo fuente versionado en git, asi que corre igual en un worktree aislado siempre que 05-01 ya haya mergeado TP2/python/benchmark.py.
</context>

## Artifacts this phase produces

- `package_tp2.py` -- script de empaquetado (raiz del repo, fuera de TP2/ para no incluirse a si mismo): `collect_files()`, `build_zip()`, `check_size()`, `verify_contents()`, `main()`
- `TP2_codigo.zip` -- entregable DELIV-03 (raiz del repo)
- `.gitignore` (raiz del repo, nuevo) -- ignora `/TP2_codigo.zip`

<tasks>

<task type="tracer">
  <name>Task 1: Script de empaquetado + primer zip + verificacion basica</name>
  <files>package_tp2.py, TP2_codigo.zip</files>
  <precondition>TP2/python/benchmark.py existe (creado por el Plan 05-01, Wave 1).</precondition>
  <read_first>TP2/Makefile (confirmar que es el unico archivo "Makefile" de TP2 a incluir); estructura real de TP2/src/ (engine/, include/, methods/, utils/, main.cpp, selftest.cpp -- 13 archivos .cpp/.h); TP2/python/ (debe contener exactamente sweep.py, analyze.py, animate.py, benchmark.py -- ningun otro .py)</read_first>
  <action>Crear package_tp2.py en la raiz del repositorio (fuera de TP2/, para no aparecer en su propio glob de empaquetado): REPO_ROOT = Path(__file__).resolve().parent, TP2_DIR = REPO_ROOT / "TP2", OUT_ZIP = REPO_ROOT / "TP2_codigo.zip", SIZE_WARNING_BYTES = 500*1024. Implementar collect_files(): junta sorted((TP2_DIR/"src").rglob("*")) filtrado a is_file(), [TP2_DIR/"Makefile"], sorted((TP2_DIR/"python").glob("*.py")) filtrado a is_file() -- ningun otro directorio de TP2/ (nunca TP2/data, TP2/build, TP2/tp2, TP2/tp2_test, TP2/informe, TP2/presentacion). Implementar build_zip(files, out_path): zipfile.ZipFile(out_path, "w", zipfile.ZIP_DEFLATED), para cada archivo zf.write(f, arcname=f.relative_to(REPO_ROOT)) (preserva el prefijo "TP2/..." dentro del zip). Implementar check_size(out_path, warn_bytes=SIZE_WARNING_BYTES): lee out_path.stat().st_size, imprime "ADVERTENCIA: ... > 500KB" si excede, sino "OK: ... bytes", devuelve el tamano (informativo, nunca aborta el empaquetado). Implementar main(): collect_files() -> build_zip() -> check_size(), imprime la ruta final y la cantidad de archivos. Crear un nuevo archivo .gitignore en la raiz del repositorio (no existe todavia) con la linea `/TP2_codigo.zip`, para que el zip regenerable no se trackee por git. Ejecutar `python3 package_tp2.py` via WSL para la primera generacion.</action>
  <verify>
    <automated>wsl.exe -- bash -lc "cd '/mnt/c/Users/Lucas Di Candia/Desktop/SDS/TP2-SDS-G5' && python3 package_tp2.py && test -s TP2_codigo.zip && python3 -c \"import zipfile; names=zipfile.ZipFile('TP2_codigo.zip').namelist(); assert any(n.endswith('benchmark.py') for n in names), names; assert any(n.endswith('Makefile') for n in names); print('ZIP_TRACER_OK', len(names))\""</automated>
  </verify>
  <acceptance_criteria>ZIP_TRACER_OK se imprime con un conteo de archivos mayor a 0; TP2_codigo.zip contiene sweep.py, analyze.py, animate.py, benchmark.py, Makefile y al menos los .cpp/.h de src/; el .gitignore de raiz existe y contiene la linea /TP2_codigo.zip.</acceptance_criteria>
  <done>El script de empaquetado genera un zip real con el contenido esperado, probando la logica de coleccion+escritura end-to-end antes de agregar el chequeo de tamano/integridad completo.</done>
</task>

<task type="auto">
  <name>Task 2: Chequeo de tamano + verificacion de integridad (allowlist exacto, sin fugas, sin dependencia rota de 05-01)</name>
  <files>package_tp2.py, TP2_codigo.zip</files>
  <read_first>package_tp2.py (funciones ya escritas en Task 1)</read_first>
  <action>Confirmar/ajustar check_size() para que emita la advertencia de forma inequivoca (prefijo literal "ADVERTENCIA:") sin abortar el empaquetado (el enunciado pide "orden de kb", el chequeo es informativo, no bloqueante). Agregar verify_contents(files, out_path) llamada desde main(): reabre el zip recien creado, compara set(zf.namelist()) contra el set esperado de arcnames de collect_files(), levanta AssertionError explicito si no coinciden exactamente (ni de mas ni de menos) -- y ademas verifica explicitamente que los 4 nombres base sweep.py/analyze.py/animate.py/benchmark.py esten todos presentes en TP2/python dentro del zip (si benchmark.py faltara por una dependencia rota con 05-01, esto debe fallar fuerte en vez de empaquetar silenciosamente solo 3 de 4 scripts). Ejecutar `python3 package_tp2.py` de nuevo via WSL para confirmar regeneracion idempotente (mismo conteo de archivos, sin acumulacion de entradas duplicadas).</action>
  <verify>
    <automated>wsl.exe -- bash -lc "cd '/mnt/c/Users/Lucas Di Candia/Desktop/SDS/TP2-SDS-G5' && python3 package_tp2.py && python3 -c \"import zipfile,os; names=set(zipfile.ZipFile('TP2_codigo.zip').namelist()); forbidden=[n for n in names if n.startswith('TP2/data/') or n.startswith('TP2/build/') or n.startswith('TP2/informe/') or n.startswith('TP2/presentacion/') or n in ('TP2/tp2','TP2/tp2_test') or '.git' in n or '__pycache__' in n]; assert not forbidden, forbidden; py=[n for n in names if n.startswith('TP2/python/')]; bases={n.rsplit('/',1)[-1] for n in py}; assert bases=={'sweep.py','analyze.py','animate.py','benchmark.py'}, bases; size=os.path.getsize('TP2_codigo.zip'); print('ZIP_INTEGRITY_OK', len(names), size)\""</automated>
  </verify>
  <acceptance_criteria>ZIP_INTEGRITY_OK se imprime con cero archivos prohibidos (sin TP2/data/, TP2/build/, TP2/informe/, TP2/presentacion/, sin binarios tp2/tp2_test, sin .git, sin __pycache__) y con el conjunto exacto de 4 scripts Python (sweep.py, analyze.py, animate.py, benchmark.py); el tamano impreso queda muy por debajo de 500KB (solo .cpp/.h/.py/Makefile); correr el script dos veces consecutivas produce el mismo conteo de archivos ambas veces (idempotente, sin duplicados).</acceptance_criteria>
  <done>TP2_codigo.zip contiene exactamente el conjunto permitido de archivos (motor final: src/, Makefile, los 4 scripts Python incluyendo benchmark.py de 05-01), con verificacion automatica de que no se filtro ningun archivo prohibido ni falta ninguno esperado, y un chequeo de tamano que advierte (sin bloquear) si supera ~500KB.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Filesystem -> zip archive (package_tp2.py) | Script walks the local TP2/ tree and writes a new zip -- no untrusted/external input, all paths are internally enumerated |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-08 | Information Disclosure | zip content selection | medium | mitigate | Allowlist-only collection (TP2/src, TP2/Makefile, TP2/python/*.py) rather than a blacklist-exclude approach -- structurally impossible to accidentally leak TP2/data/, .git/, or informe/presentacion drafts even if new files appear later in those directories; Task 2's integrity check asserts zero forbidden-prefix entries |
| T-05-09 | Tampering | zip arcname construction | low | accept | arcnames are derived via Path.relative_to(REPO_ROOT) on internally-enumerated real filesystem paths, never from external/user input -- no zip-slip surface since this script only creates archives, never extracts untrusted ones |

</threat_model>

<verification>
`python3 package_tp2.py` (WSL, cwd=repo root) regenerates TP2_codigo.zip idempotently; the integrity check confirms zero forbidden-prefix entries, the exact 4-script python allowlist, and reports the final size against the ~500KB advisory threshold.
</verification>

<success_criteria>
DELIV-03 satisfied: TP2_codigo.zip at the repo root contains only TP2/src/**, TP2/Makefile, and TP2/python/*.py (including benchmark.py from 05-01), verified by an automated allowlist check, with an automated size warning if it exceeds ~500KB.
</success_criteria>

<output>
Create `.planning/phases/05-benchmark-y-entregables/05-04-SUMMARY.md` when done
</output>
