---
phase: 01-motor-y-grid-persistente
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - TP2/src/engine/simulation.h
  - TP2/src/engine/simulation.cpp
  - TP2/src/main.cpp
  - TP2/src/selftest.cpp
  - TP2/Makefile
autonomous: true
requirements: [ENGINE-03, ENGINE-04, ENGINE-05]

estimate:
  tokens: 38000
  raw_tokens: 38000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A self-test proves the synchronous double-buffered heading update reproduces a hand-computed 3-particle result and is independent of particle array insertion order (no in-place mutation bias)."
    - "A self-test proves that after thousands of integration steps, every particle position stays within [0, L) under periodic boundary conditions at L=10."
    - "The tp2 binary builds and runs independently from TP2/ via make and ./tp2, and git diff shows no changes inside TP1/."
  artifacts:
    - path: "TP2/src/engine/simulation.h"
      provides: "Simulation class declaration (step(), particles() accessor), circularMeanHeading declaration"
    - path: "TP2/src/engine/simulation.cpp"
      provides: "Synchronous double-buffered step() implementation: grid rebuild -> circular-mean heading -> position integrate + wrap"
    - path: "TP2/src/main.cpp"
      provides: "CLI entry point (tp2 binary): --N/--rho, --L, --rc, --M, --steps, --seed, --v0, --dt, --periodic flags"
  key_links:
    - from: "TP2/src/main.cpp"
      to: "TP2/src/engine/simulation.h"
      via: "main constructs one Simulation and calls step() in a steps-count loop"
      pattern: "Simulation\\s+sim|sim\\.step\\("
    - from: "TP2/src/engine/simulation.cpp"
      to: "TP2/src/methods/cell_index_grid.cpp"
      via: "Simulation::step() calls grid_.rebuild(particles_) every step before computing new headings"
      pattern: "grid_\\.rebuild\\("
    - from: "TP2/src/engine/simulation.cpp"
      to: "TP2/src/include/particle.h"
      via: "position integration calls periodicWrap after advancing x,y with the new heading's velocity"
      pattern: "periodicWrap\\("
---

<objective>
Complete the TP2/ motor: a synchronous, double-buffered heading-update primitive (circular mean of old neighbor headings, self-inclusive, proven order-independent) wired into a per-step integrator that advances position and re-wraps under periodic boundary conditions every step, exposed through a standalone CLI binary that never touches TP1/.

Purpose: This closes ENGINE-03/04/05 — the update-order and PBC-wrap bugs flagged in PITFALLS.md Pitfall 1/2/3 are the single most expensive class of bug to discover after Phase 3's multi-hour sweep has already run on top of them, so they must be nailed down here with hand-computed self-tests before Phase 2 wires in the actual Vicsek/voter rules.
Output: TP2/src/engine/simulation.h, TP2/src/engine/simulation.cpp, TP2/src/main.cpp, extended TP2/src/selftest.cpp, extended TP2/Makefile — a runnable `tp2` binary and a `tp2_test` binary whose full suite passes.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-motor-y-grid-persistente/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Synchronous double-buffered heading update (circular mean, self-inclusive)</name>
  <read_first>
    .planning/research/ARCHITECTURE.md
    .planning/research/PITFALLS.md
    TP2/src/include/grid.h
    TP2/src/include/particle.h
  </read_first>
  <reversibility rating="costly">The self-inclusion convention for the neighbor-average (each particle includes its own old heading in the sum before averaging, matching Vicsek 1995's original definition) determines the exact numeric output of every future run. If this convention is wrong, Phase 2's Vicsek/voter rules inherit it and any parameter sweep already run in Phase 3 on top of it needs re-running — costly, though not a data migration or a published contract.</reversibility>
  <files>TP2/src/engine/simulation.h, TP2/src/engine/simulation.cpp, TP2/src/selftest.cpp</files>
  <behavior>
    - circularMeanHeading(i, particles, neighbors) for particle i with self theta and neighbor thetas {t1, t2, ...} returns atan2(sin(theta_i)+Σsin(t_j), cos(theta_i)+Σcos(t_j)) — self-inclusive, matching Vicsek 1995's original neighborhood-sum convention (guarantees a well-defined result even when the external neighbor set is empty, per PITFALLS.md Pitfall 10).
    - Three-particle hand-computed case: p0={x=1,y=1,theta=0}, p1={x=2,y=1,theta=pi/2}, p2={x=1,y=2,theta=pi}, L=10, rc=2.0, periodic=false (mutual distances 1.0, 1.0, sqrt(2) are all < rc=2.0, so all three are mutual neighbors). Simulation::step() with v0=0 (no position change, isolates the heading math) must set all three particles' new theta to atan2(1, 0) = pi/2 (exactly, since each particle's self+2-neighbor sum is sin: 0+1+0=1, cos: 1+0-1=0), each within 1e-9 tolerance.
    - Order-independence: constructing a second Simulation with the same three particles inserted in reverse array order (p2, p1, p0) and calling step() once produces the same resulting set of (theta) values as the forward-order Simulation, proving no iteration-order bias (no in-place mutation) exists.
  </behavior>
  <action>
    Create TP2/src/engine/simulation.h (#pragma once, includes &lt;vector&gt;, "particle.h", "grid.h") declaring free function double circularMeanHeading(int i, const std::vector&lt;VicsekParticle&gt;&amp; particles, const NeighborList&amp; neighbors); and class Simulation with: public constructor Simulation(std::vector&lt;VicsekParticle&gt; particles, double L, double rc, double v0, double dt, int M, bool periodic); public void step(); public const std::vector&lt;VicsekParticle&gt;&amp; particles() const; private members std::vector&lt;VicsekParticle&gt; particles_; Grid grid_; std::vector&lt;double&gt; thetaNew_; double L_, rc_, v0_, dt_;.

    Create TP2/src/engine/simulation.cpp implementing circularMeanHeading(i, particles, neighbors): accumulate double sumSin = std::sin(particles[i].theta), sumCos = std::cos(particles[i].theta) (self-inclusion — start the sum with the particle's own old heading before adding neighbors, per PITFALLS.md Pitfall 10's recommended fallback), then for each j in neighbors[i] add std::sin(particles[j].theta)/std::cos(particles[j].theta), and return std::atan2(sumSin, sumCos).

    Implement the Simulation constructor: move particles into particles_, construct grid_(M, L, rc, periodic), resize thetaNew_ to particles_.size() once (this buffer is allocated exactly once, in the constructor, and reused by every step() call — do not resize or reallocate it inside step()), store L_,rc_,v0_,dt_.

    Implement Simulation::step() as exactly three sequential passes, each a separate loop over all particles (never combined into one loop — this separation is what makes the update synchronous): Pass 1: grid_.rebuild(particles_). Pass 2: for i in [0, particles_.size()): thetaNew_[i] = circularMeanHeading(i, particles_, grid_.neighbors()) — this reads only particles_[].theta, which is not written anywhere during Pass 2. Pass 3: for i in [0, particles_.size()): compute double vx, vy; headingToVelocity(thetaNew_[i], v0_, vx, vy); then particles_[i].x = periodicWrap(particles_[i].x + vx*dt_, L_); particles_[i].y = periodicWrap(particles_[i].y + vy*dt_, L_); particles_[i].theta = thetaNew_[i] (theta is written last, after position uses the new heading, matching the standard Vicsek convention that position and orientation update together in one synchronous step).

    Extend TP2/src/selftest.cpp: add testSynchronousUpdateNoBias() implementing exactly the three-particle hand-computed case from &lt;behavior&gt; above (construct particles in forward order p0,p1,p2, build a Simulation with v0=0, dt=1, M=maxValidGridM(10.0,2.0), periodic=false, call step() once, assert each resulting theta is within 1e-9 of pi/2 using std::abs), then repeat with a second Simulation built from the same three particles inserted in order p2,p1,p0, call step() once, and assert the resulting theta values (matched by particle id, not array position) are within 1e-9 of the forward-order run's results — this is the order-independence proof. Register testSynchronousUpdateNoBias() in selftest.cpp's main().
  </action>
  <verify>
    <automated>cd TP2 && make test 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - TP2/src/engine/simulation.cpp contains three textually separate loops over the particle range inside step() (source assertion: the pass-1/pass-2/pass-3 structure is not collapsed into a single loop that both reads and writes theta in the same iteration).
    - `cd TP2 && make test` exits 0, and its output includes a line confirming testSynchronousUpdateNoBias ran with no reported failures for that test's checks.
    - The hand-computed expected heading value pi/2 (or "1.5707963" truncated) appears in TP2/src/selftest.cpp as the asserted expected value for the three-particle case (source assertion the test encodes a real, hand-derived number, not a value read back from the function under test).
  </acceptance_criteria>
  <done>Simulation::step() performs a genuinely double-buffered update (three sequential passes, thetaNew_ allocated once in the constructor); the self-test proves the three-particle hand-computed result and order-independence within 1e-9 tolerance.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Position integration under PBC + standalone CLI + TP1-untouched verification</name>
  <read_first>
    TP1/src/main.cpp
    .planning/research/PITFALLS.md
    TP2/src/engine/simulation.h
  </read_first>
  <files>TP2/src/main.cpp, TP2/src/selftest.cpp, TP2/Makefile</files>
  <behavior>
    - testLongRunStaysWrapped: a Simulation of N=50 particles generated via generateVicsekParticles(50, 10.0, 123), L=10.0, rc=1.0, v0=0.5, dt=1.0, M=maxValidGridM(10.0,1.0), periodic=true, run for 5000 steps; after every step, every particle's x and y satisfy 0.0 &lt;= x &lt; 10.0 and 0.0 &lt;= y &lt; 10.0 (strict half-open interval, matching TP1's containment-check convention).
    - `./tp2 --rho 4 --steps 20 --seed 42` (run from inside TP2/) exits with status 0 and prints a completion report to stdout.
  </behavior>
  <action>
    Create TP2/src/main.cpp mirroring TP1/src/main.cpp's structure (Options struct, parseArgs via getopt_long, usage(), a function-try-block main wrapping the whole body and converting any std::exception into "error: %s\n" on stderr with exit code 1 — copy this exact error-boundary pattern, do not invent a different one). Options fields: double rho (default 4.0), int N (default 0, meaning "derive from rho": N = static_cast&lt;int&gt;(std::round(rho * L * L))), double L (default 10.0), double rc (default 1.0), int M (default 0, meaning "derive via maxValidGridM(L,rc)"), int steps (default 100), unsigned long long seed (default 42 — an explicit constant, never time(nullptr) or clock-seeded, per PITFALLS.md Pitfall 5), double v0 (default 0.03), double dt (default 1.0), bool periodic (default true). Long options: --rho, --N, --L, --rc, --M, --steps, --seed, --v0, --dt, --periodic (flag, sets true), --no-periodic (flag, sets false). In main(): parse args, resolve N from rho if N==0, resolve M via maxValidGridM(L,rc) if M==0, call generateVicsekParticles(N, L, seed), construct one Simulation(std::move(particles), L, rc, v0, dt, M, periodic), loop calling sim.step() exactly `steps` times, then std::printf a one-line human report: "TP2 motor: N=%d L=%.2f rc=%.2f M=%d steps=%d seed=%llu -- OK\n" with the resolved values.

    Extend TP2/src/selftest.cpp: add testLongRunStaysWrapped() implementing exactly the &lt;behavior&gt; case above — generate 50 particles via generateVicsekParticles(50, 10.0, 123), construct a Simulation, loop 5000 times calling step() then immediately checking (via the existing check() helper) that every particle's x is in [0,10.0) and y is in [0,10.0) using the exact comparisons x &gt;= 0.0 &amp;&amp; x &lt; 10.0 (do not use a tolerance-padded range — periodicWrap must produce values strictly inside [0,L)). Register testLongRunStaysWrapped() in main()'s test sequence.

    Extend TP2/Makefile: add $(BUILD)/main.o to a new `tp2` target (linking CORE_OBJ + $(BUILD)/main.o + $(BUILD)/engine/simulation.o), add $(SRC)/engine/simulation.cpp to CORE_SRC, change the `all` target from `tp2_test` (Plan 01's placeholder) to `all: tp2 tp2_test`.
  </action>
  <verify>
    <automated>cd TP2 && make && make test 2>&1 | tail -8 && ./tp2 --rho 4 --steps 20 --seed 42 && cd .. && git status --porcelain -- TP1/ && git diff --stat -- TP1/</automated>
  </verify>
  <acceptance_criteria>
    - `cd TP2 && make` produces a `tp2` executable file (test -x TP2/tp2 succeeds).
    - `cd TP2 && ./tp2 --rho 4 --steps 20 --seed 42` exits with status 0.
    - `cd TP2 && make test` exits 0 with a summary line reporting 0 failures across the full suite (structural, persistent-buffer, generator, synchronous-update, long-run-wrap tests combined).
    - `git status --porcelain -- TP1/` produces empty output and `git diff --stat -- TP1/` produces empty output (TP1/ has zero changes from this entire phase).
    - TP2/src/main.cpp contains "seed" as a parsed CLI option name and does not call std::time or std::rand anywhere (source assertion: grep -c 'std::time\|std::rand' TP2/src/main.cpp returns 0).
  </acceptance_criteria>
  <done>tp2 builds and runs a full simulation end-to-end from TP2/ via CLI flags; the long-run self-test proves positions never leave [0,L) across 5000 steps; TP1/ shows zero diff for the entire phase.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI flags (--N, --rho, --M, --steps, --seed, --rc, --L) → main.cpp → Simulation/Grid | Locally-supplied numeric CLI input parsed via getopt_long; a malformed or adversarial value (negative steps, huge N, non-numeric string) is the only external input this binary accepts |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-03 | Tampering | TP2/src/main.cpp CLI parsing + Grid/Simulation construction | low | mitigate | Invalid grid parameters (M/L/rc) throw std::invalid_argument from the Grid constructor built in Plan 01; main() wraps its body in a function-try-block that converts any std::exception to a stderr "error: ..." message and exit code 1, exactly matching TP1's existing error-boundary convention — no unhandled crash path |
| T-01-04 | Denial of Service | --steps / --N CLI flags | low | accept | This is an offline, single-user, local-only C++ simulation engine with no network exposure and no persisted secrets. A user requesting an extreme --steps or --N value only consumes their own local process's time/memory; there is no other user, service, or shared resource for this to affect, so no ASVS L1-relevant threat beyond the input-validation covered by T-01-03 exists for this phase. |
</threat_model>

<verification>
`cd TP2 && make && make test` exits 0. `./tp2 --rho 4 --steps 20 --seed 42` runs successfully from TP2/. `git status --porcelain -- TP1/` and `git diff --stat -- TP1/` both produce empty output for the entire phase (Plans 01 and 02 combined).
</verification>

<success_criteria>
The synchronous double-buffered heading update reproduces the hand-computed 3-particle case exactly (within 1e-9) and is provably order-independent; positions remain in [0,L) after 5000 integration steps under periodic boundary conditions; the tp2 binary builds and runs independently from TP2/; TP1/ is untouched by the whole phase.
</success_criteria>

<output>
Create `.planning/phases/01-motor-y-grid-persistente/01-02-SUMMARY.md` when done
</output>

## Artifacts This Phase Produces

Full manifest across both plans in Phase 1 (Motor y Grid Persistente):

**From Plan 01 (01-01-PLAN.md):**
- `TP2/src/include/particle.h` — `VicsekParticle` struct (id, x, y, theta), `periodicWrap`, `periodicDelta`, `withinRadius`, `headingToVelocity`
- `TP2/src/include/grid.h` — `NeighborList` alias, `maxValidGridM(L, rc)`, `Grid` struct (M, L, rc, periodic, cells, neighbors_, constructor, `rebuild()`, `neighbors()`)
- `TP2/src/methods/cell_index_grid.cpp` — `Grid::rebuild()` persistent-buffer implementation
- `TP2/src/include/generator.h` / `TP2/src/utils/generator.cpp` — `generateVicsekParticles(N, L, seed)`
- `TP2/src/selftest.cpp` — `testGridStructural`, `testGridPersistentBuffers`, `testGenerator`
- `TP2/Makefile`, `TP2/.gitignore`

**From Plan 02 (this plan):**
- `TP2/src/engine/simulation.h` / `TP2/src/engine/simulation.cpp` — `circularMeanHeading(...)`, `Simulation` class (`step()`, `particles()`)
- `TP2/src/main.cpp` — CLI entry point building the `tp2` binary (flags: `--rho`/`--N`, `--L`, `--rc`, `--M`, `--steps`, `--seed`, `--v0`, `--dt`, `--periodic`/`--no-periodic`)
- `TP2/src/selftest.cpp` (extended) — `testSynchronousUpdateNoBias`, `testLongRunStaysWrapped`
- `TP2/Makefile` (extended) — `tp2` target added, `all: tp2 tp2_test`

**Explicitly out of scope for Phase 1** (deferred to Phase 2 per REQUIREMENTS.md VICSEK-01/VOTER-01/VOTER-02/OUTPUT-01): the `--model vicsek|voter` CLI flag, angular noise η, the voter rule's random-neighbor-copy strategy, and any text-file output (`writeDynamic`-equivalent with real vx,vy). The `circularMeanHeading` primitive built here is a generic engine building block proving the double-buffer mechanic — Phase 2 wires it into the actual noised Vicsek path and adds the distinct voter strategy alongside it.
